/**
 * EntityGraph — 已迁移至 OntologyGraph 组件
 * 此文件保留为向后兼容的重新导出入口
 */
export { default } from "../../components/OntologyGraph";
export type { GraphNode, GraphEdge } from "../../components/OntologyGraph";
