interface MatrixPhaseDetail {
  role: string;
  roleLabel: string;
  desc: string;
  code?: string;
  antiPattern?: string;
}

export interface MatrixItem {  toolId: string;  toolName: string;  toolIcon: string;  phases: Record<number, MatrixPhaseDetail>;}

export const matrixData: MatrixItem[] = [  {    toolId: 'pipeline-builder',    toolName: 'Pipeline Builder (图形算子)',    toolIcon: 'Workflow',    phases: {      1: {        role: 'SECONDARY',        roleLabel: '辅助协作',        desc: '支持在图形画布中直接导入由 Data Connection 托管的物理原始表。支持可视化触发一次性全量同步。',        antiPattern: '避免在 Pipeline Builder 中手动编写大量底层的原始 JDBC 读取通道，应由 Magritte/Data Ingest 同步任务作为标准源导入。'      },      2: {        role: 'PRIMARY',        roleLabel: '绝对主导',        desc: '低代码/免代码数据清洗的首选核心阵地。支持一键拖拽完成 30 余组流批一体标准清洗算子（Trim, Cast, Regex, Coalesce, DateFormat）并执行 Colocate Joins 融合成宽表。',        code: `// [Pipeline Builder 算子图元物理下推配置]{  "type": "expression_projection",  "nodeId": "node_active_flights",  "transforms": [    { "column": "flight_num", "type": "trim_and_upper" },    { "column": "dep_airport", "type": "uppercase" },    { "column": "pilot_name", "type": "coalesce", "args": ["'未知飞行员'"] }  ]}`,        antiPattern: '严禁在 Pipeline Builder 节点中堆叠极其复杂的统计分析、多段复杂数学建模（如气象动力预测）。该场景应由 Code Repositories 高代码或 Python 建模节点承接。'      },      3: {        role: 'PRIMARY',        roleLabel: '深度主导',        desc: '提供百分之百可视化的全链路数据血缘（Lineage DAG）呈现。每一个图形节点都自带开发分支（Branching）属性，可直接在当前画布拉起独立分支进行冷配置与运行测试，完全不破坏线上生产血缘依赖。',        code: `# Pipeline Builder 拓扑图元分支自验- 镜像生产线: [main] -> [dev/add-weather-enrichment]- 差异运行分析: 评估 [ds_flights_clean] schema 新增字段对下游 SLA 影响- 校验结论: 静态 DAG 无循环依赖，通过！`,        antiPattern: '避免在未拉起开发分支的前提下直接在 main 分支修改拓扑。ECOS 强制要求任何修改必须提交 PR 经合并后由系统自动跑 CI。'      },      4: {        role: 'PRIMARY',        roleLabel: '绝对主导',        desc: '提供平台原生、全可视化的数据质量健康判定（Data Health Checks）。支持一键在任意数据节点绑定：行数底线、特定字段空值率界限、SLA 物理更新延迟阈值，并自动触发熔断与警报。',        code: `healthChecks:  - dataset: "/aviation/silver/ds_flights_clean"    rules:      - rule: ROW_COUNT_NOT_EMPTY (触发条件: 行数 < 1000)      - rule: NULL_CHECK (限制 pilot_id 缺失占比 <= 1.0%)      - rule: FRESHNESS_CHECK (主表延迟更新时效上限为 120 分钟)`,        antiPattern: '严禁只配置调度而不挂载任何质量 Data Health 监控。裸奔的管道在遇到物理源 Schema 突变时会导致全网脏数据污染，引起 Ontology 实体灾难。'      },      5: {        role: 'PRIMARY',        roleLabel: '绝对主导',        desc: '一键建立物理表与逻辑实体的映射通道。Pipeline Builder 生成的 Gold（黄金级）生产大宽表，可以被 Ontology Manager 选作标准物理支撑表，并直接进行主键绑定和关系建立。',        antiPattern: '不要将未经 Pipeline Builder 清洗聚合的 Bronze 原始脏表直接投射到 Ontology 实体。Ontology 必须绑定高可用、高净度的数据。'      }    }  },  {    toolId: 'code-repositories',    toolName: 'Code Repositories (代码仓库)',    toolIcon: 'FileCode',    phases: {      1: {        role: 'SECONDARY',        roleLabel: '辅助支持',        desc: '支持通过编写高代码 Doris Ingress 驱动（如复杂的文件加密解析、非结构化 JSON 级联扁平化）来直接抽取特殊的自定义物理数据。',        antiPattern: '不要针对常规的标准 JDBC PostgreSQL/MySQL 编写复杂的 Java/Scala 同步代码，这会增加长线维护成本，应优先使用 Data Connections 配置。'      },      2: {        role: 'PRIMARY',        roleLabel: '绝对主导',        desc: '高代码（Code-First）场景下的终极清洗与关联武器。针对百亿级极速计算体量、涉及大规模级联哈希 Join、高级聚合窗口函数（row_number、window_lead）或需引入机器学习预测模型的数据集。',        code: `# [Doris & Python 生产级极速转换算子规范]import pandas as pd# 平台全自动下推至 Doris 极速计算引擎，或在内存中执行高速矢量计算def process_flight_maint_lag(flights_df, pilots_df):    # 1. 物理字段清洗    clean_f = flights_df[flights_df["flight_id"].notna()]    clean_f["flight_num"] = clean_f["flight_num"].str.strip().str.upper()        # 2. 内存高速 Hash Join (Doris / Vectorized)    joined = pd.merge(clean_f, pilots_df, on="pilot_id", how="left")        # 3. 内存聚合与排序计算：计算每个飞行员的前序航班时刻    joined["prev_flight_time"] = joined.groupby("pilot_id")["scheduled_departure"].shift(1)    return joined`,        antiPattern: '严禁将易读、简单的过滤或拼接逻辑用高度复杂的 Repositories 代码层层包裹。保持“简单逻辑低代码化，算法逻辑高代码化”是金牌策划案的最高原则。'      },      3: {        role: 'PRIMARY',        roleLabel: '绝对主导',        desc: '代码级 Git 协作的核心闭环。基于内置标准 Git 分支架构（Git Branching as Code），支持团队多人并行编写 Doris/SQL 管道。任何合并动作必须提交 PR 并自动触发系统内置 YARN/Doris 编译器静态校验、运行测试单元。',        code: `# [Repositories Code-First CI 静态编译与自验规范]$ git checkout -b dev/flight-weather-enrichment$ py-compile src/pipeline_logic.py  >> Loading dependency libraries: doris-connector, numpy, pandas...  >> Running Unit Test Cases: test_schema_match(), test_delay_metric()...  >> Checking SLA downstream dependency impact map...  >> STATUS: 🟢 ALL TEST CASES PASSED. PR APPROVED BY PEER G.XIAO.`,        antiPattern: '坚决杜绝不跑静态测试、不提交 PR 就将代码直接推送合并到 master 分支的行为。CI 系统具有强制拦截权。'      },      4: {        role: 'SECONDARY',        roleLabel: '辅助协作',        desc: '支持在代码级别通过特定的配置文件（如 pipeline.yml 或 schedules.json）声明复杂的联动调度策略或 Job Groups，以便进行细粒度控制。',        antiPattern: '不要在代码中手写复杂的 cron 计时线程。应当使用平台内置的 Job Scheduler 进行调度托管，以保证统一的运维观测性。'      },      5: {        role: 'PRIMARY',        roleLabel: '绝对主导',        desc: '利用強类型 SDK 的终极赋能。Code Repositories 支持直接编写 Functions on Objects（对象函数）。通过绑定 Ontology 实体类型，开发者可以使用 TypeScript/Java 编写安全、类型受控、具备自动补全的业务指标算子，向平台下游应用输送纯净计算服务。',        code: `// [TypeScript Functions on Objects 标准规范]import { Function, Objects, Flight } from "@ecos/functions-api";export class FlightAnalyticsService {    @Function()    public getHighRiskFlights(flights: Flight[]): Flight[] {        // 利用 Ontology 强类型直接进行高级业务筛选过滤        return flights.filter(f => {            const delay = f.delayMinutes ?? 0;            return delay > 60 && f.status === 'ACTIVE';        });    }}`,        antiPattern: '严禁在应用前端（如 Slate 或 Workshop）编写大量散落的、无版本控制的 JS 脚本来计算业务核心指标。应当统一收拢在 Repositories 实体方法中，确保单一事实来源。'      }    }  },  {    toolId: 'code-workbooks',    toolName: 'Code Workbooks (探索工作簿)',    toolIcon: 'BookOpen',    phases: {      1: {        role: 'NONE',        roleLabel: '无直接关联',        desc: '不具备物理采集、拉取和注册物理源的功能。',        antiPattern: '不要尝试在 Workbook 中编写临时的 JDBC 连接去手动拉取数据，这不仅会绕过平台的数据安全审计，还会带来严重的账号明文泄露风险。'      },      2: {        role: 'SECONDARY',        roleLabel: '辅助支持',        desc: '数据清洗前中期的“沙箱敏捷验证”和“机器学习原型设计”。允许使用 SQL、Python、R 混写。可在沙箱中实时输出中间表、柱状图、箱线图，帮助数据科学家探查质量，验证关联逻辑是否合理。',        code: `# [Code Workbooks 混写探查与统计分布打印]import pandas as pdimport numpy as npdef analyze_maint_cycle(flights_with_pilots_df):    # 即时沙箱环境，可以打印 dataframe 的多维统计分布图并进行异常检测    pandas_df = flights_with_pilots_df.toPandas()    desc = pandas_df.describe()    print("Maint hours distribution:\\n", desc)    return flights_with_pilots_df`,        antiPattern: '严禁将 Code Workbooks 作为定时运行、大规模高并发的线上生产级 ETL 管道。Workbook 的底层容器是交互式的，高并发调度会极易触发 JVM 堆内存溢出。'      },      3: {        role: 'SECONDARY',        roleLabel: '辅助协作',        desc: '支持通过拖拽式分支（Interactive Tree Graph）查看本 Workbook 内部节点的逻辑依赖和数据流动，主要用于数据科学探查的思路梳理。',        antiPattern: '不要混淆 Workbook 内部的草稿依赖树与线上正式的生产 DAG 血缘依赖。Workbook 内部修改不会对生产血缘图腾产生任何 SLA 影响。'      },      4: {        role: 'SECONDARY',        roleLabel: '辅助协同',        desc: '当线上调度出现数据健康异常（如 pilot_id 缺失占比超过 1%）时，开发人员可通过一键克隆现场物理快照，在 Code Workbooks 中启动沙箱现场排查，利用 Python 分析缺失空值的概率分布。',        antiPattern: '严禁在 Workbook 交互节点中直接挂载日常高频监控报警，由于 Workbook 底层服务的非长驻性，会导致监控警报极易丢失。'      },      5: {        role: 'SECONDARY',        roleLabel: '辅助支持',        desc: '支持将 Workbook 中训练好的算法模型作为 ML Model Object 注册进 Ontology，作为“模型实体”以供下游决策系统调用。'      }    }  },  {    toolId: 'contour',    toolName: 'Contour (分析下钻)',    toolIcon: 'Layers',    phases: {      1: {        role: 'NONE',        roleLabel: '无直接关联',        desc: '纯业务侧分析产品，无法直连物理底层数据库。',        antiPattern: '业务分析师不需要关心物理数据源。物理源的注册与托管应当完全交给数据工程团队在 Ingest 和 Connections 环节中隔离完成。'      },      2: {        role: 'SECONDARY',        roleLabel: '辅助协作',        desc: '业务分析师可以通过点击交互卡片（Cards）对已入湖的 Bronze/Silver 数据集执行亚秒级的多维汇总和过滤（Curation），从而快速获得一份特定业务场景下的临时数据集或数据报告，不产生生产级物理落湖表。',        code: `[Contour 敏捷分析板卡流 (Analysis Board Path)]- 加载: /aviation/silver/ds_flights_clean [4,500 行]- 卡片 1 (Filter): 过滤 departure_airport == 'PEK' 且延迟时间 > 15分钟- 卡片 2 (Group By): 按 pilot_id 进行聚合，计算平均延迟次数- 卡片 3 (Join): 级联加载 pilots_raw 宽表，获取飞行员实名- 输出: 形成分析路径并在 0.5 秒内呈现可视化热力图`,        antiPattern: '严禁依靠 Contour 进行长时序、大规模、包含深度算法回溯的后台物理 ETL 任务。Contour 是为了业务即时多维下钻设计的 Presto 式临时视图，不能取代物理大宽表的加工。'      },      3: {        role: 'SECONDARY',        roleLabel: '辅助协同',        desc: '业务分析师可以极其直观地在 Contour 画布中一键追溯其所引用数据集的上游所有依赖及数据 SLA，实现端到端的业务质检合规。'      },      4: {        role: 'SECONDARY',        roleLabel: '辅助协助',        desc: '作为数据质量审计的“第二双眼睛”。业务主管和质检合规官可以通过 Contour 配置多维时效、异常占比看板，并在日常业务巡检中直接监督生产数据的健康情况。',        antiPattern: '严禁在 Workbook 交互节点中直接挂载日常高频监控报警，由于 Workbook 底层服务的非长驻性，会导致监控警报极易丢失。'      },      5: {        role: 'PRIMARY',        roleLabel: '绝对主导',        desc: '业务消费 Ontology 实体的主流分析通道。业务人员无需知道任何物理表名和 SQL 代码，直接在 Contour 中加载已被 Ontology 映射好的逻辑实体（如“航班”与“飞行员”），双击建立多维交叉对比路径，并在 1 秒内完成百亿级实体的决策分析与看板分享。',        antiPattern: '杜绝在 Contour 中通过手动编写复杂的原始数据 Join 和硬编码来拼装业务实体。应当优先在 Ontology Manager 中完成统一实体逻辑建模，业务人员只需在 Contour 直接拉取标准对象即可。'      }    }  }];
