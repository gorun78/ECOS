# 02-security 修复记录

> 来源：ECOS-PMO | 日期：2026-08-24
> 依据：04-PMO指令-测试执行.md + 06-验收驳回意见.md

## 缺陷 1：审计写入端点缺失

| 项 | 值 |
|----|-----|
| 严重度 | P0 |
| 根因 | AuditController 仅有 GET /logs + GET /stats，缺少 POST 写入端点。前端 SecurityAudit 组件的审计日志只能查不能写，各引擎无法通过 API 写审计日志。 |
| 修复 | AuditController.java 新增 `POST /log` 端点，接收 {userId, action, resource, result, detail}，异步调用 `auditLogService.log()` 写入 ecos_audit_log 表，不阻塞调用方。返回 `ApiResponse.success(Map.of("status", "accepted"))`。 |
| Commit | `2c9f426` |
| 复验 | curl POST /api/v1/security/audit/log → code=0, data={status:"accepted"}, 响应 60ms（异步）。GET /api/v1/audit/logs 可查到写入的记录。✅ |

## 缺陷 2：datapermission 死代码

| 项 | 值 |
|----|-----|
| 严重度 | P3 |
| 根因 | security-engine-impl/datapermission/ 包下 7 个旧 impl 类（RowLevelSecurityServiceImpl / ColumnLevelSecurityServiceImpl / DataMaskingServiceImpl / DynamicPolicyServiceImpl / DataPermissionPolicyServiceImpl / DataPermissionEnforcerImpl / DataPermissionPolicyDaoImpl）无 @Service 注解（除 DaoImpl 有 @Repository），不被 Spring 容器管理，不被任何 Controller 注入。Active impl 在 security/service/ 包下（有 @Service 注解，被 RlsController/ClsController 使用）。 |
| 修复 | grep 确认 7 个类零外部引用 → git rm 删除 7 文件 + 空目录 → 编译 BUILD SUCCESS |
| Commit | `2c9f426` |
| 复验 | 编译通过 ✅，13 Controller 端点全 200 ✅，前端 3 Tab 正常 ✅ |

## 缺陷 3：操作手册端点路径核实

| 项 | 值 |
|----|-----|
| 严重度 | P2 |
| 根因 | 操作手册中部分端点路径需与实际 Controller 核实一致性 |
| 修复 | 逐条验证 8 类能力端点，全部 200 一致。修正记录：DataPermissionController 实际路径为 /api/v1/data-permission/policies（手册中未明确）；SecurityEngineStatusController 实际路径为 /api/v1/engine/security/health（手册中未明确）。 |
| Commit | 无代码变更，手册内容已验证准确 |
| 复验 | 8/8 能力端点全验证一致 ✅ |

## 缺陷 4：安全管控3项逻辑未落地（驳回项1）

| 项 | 值 |
|----|-----|
| 严重度 | P0 |
| 根因 | CRUD 已就绪（td_user_security_profile 表 + SecurityProfile 实体），但管控逻辑未落地：AuthController 无 workstation 校验、AuditAspect 无 audit_mode 分级、ToolExecutorService 无 sandbox_mandatory 审查。 |
| 修复 | 三项管控逻辑落地：|
| | **R1.1 工作站校验**：AuthController 登录成功后查 linked_workstation，校验客户端 IP，不匹配→403+审计 workstation_mismatch |
| | **R1.2 审计力度分级**：AuditAspect 按 audit_mode 分级——basic 只记关键操作、detailed +请求参数、full 联动哈希链双写 |
| | **R1.3 高危指令沙盒审查**：ToolExecutorService 检查 sandbox_mandatory，高危指令（DELETE/DROP/批量UPDATE/DDL）→审查→拒绝+审计 sandbox_block |
| Commit | (BE subagent commit — 待编译验证后确认) |
| 复验 | 待编译+curl验证 |

## 缺陷 5：裁决正确性核心测试跳过（驳回项2）

| 项 | 值 |
|----|-----|
| 严重度 | P0 |
| 根因 | TC-RLS-03（多租户隔离）+ TC-CLS-03（角色差异）被跳过，理由"仅 admin 单用户环境"。测试数据准备是测试方案的明确要求。 |
| 修复 | 构造测试数据并补跑：|
| | **TC-RLS-03**：创建 test_rls_a(tenant_a) + test_rls_b(tenant_b) 两条 RLS 策略，apply 同表 → test_tenant_a 返回 `(tenant_id='tenant_a')`，test_tenant_b 返回 `(tenant_id='tenant_b')`，**WHERE 条件不同 ✅** |
| | **TC-CLS-03**：创建 test_cls_admin(全列) + test_cls_limited(限列) 两条 CLS 策略，columns 同表 → admin 返回 7 列，test_limited_user 返回 2 列，**敏感列被过滤 ✅** |
| 数据清理 | 测试后删除所有 test_ 前缀策略（4条），DB 验证 0 行 test_ 数据 |
| 复验 | TC-RLS-03 ✅ TC-CLS-03 ✅ |

## 缺陷 6：前端审计力度下拉缺少 basic/detailed 选项

| 项 | 值 |
|----|-----|
| 严重度 | P2 |
| 根因 | UserEditModal 审计力度下拉只有 full/standard 两个选项，缺少 basic/detailed |
| 修复 | UserEditModal.tsx 增加 basic(基础-仅关键操作) + detailed(详细-含请求参数) 选项；common/zh-CN.json + en.json 新增对应 i18n keys |
| Commit | `5939a63` |
| 复验 | 前端审计力度下拉 4 选项可用 ✅ |
