# Phase 1: sysman — 差距分析（完整版）

> 基线: ECOS当前代码 | 目标: Phase 1产品化完善方案 | 日期: 2026-08-03
> **复盘更新**: 2026-08-05，5个PMO验收完毕

---

## 〇、范围调整

经确认，Phase 1做以下范围调整：
- ❌ 去掉租户管理功能（降低复杂度，多租户留后续版本）
- ❌ Controller大范围迁移留Phase 5（ABAC/DataPermission/SecurityConfig暂不迁入security-engine）
- ✅ 聚焦：登录安全增强 + 内置角色 + 安全中心前端骨架 + 安全接入规则

---

## 一、前端大文件拆分（>800行）

### sysman相关

| 文件 | 行数 | 问题 | 措施 | 状态 |
|------|:--:|------|------|:--:|
| `SecurityCenter.tsx` | 3320 | 单体巨石，含全部Tab | 拆为 SecurityCenterLayout(200行) + 事前Tab(600行) + 事中Tab(600行) + 事后Tab(500行) | ✅ PMO-04 |
| `business-workbench/SecurityCenterView.tsx` | 3293 | 与SecurityCenter.tsx功能重复 | **删除**，统一用拆分后的SecurityCenter | ✅ PMO-04 |
| `security-center/SecurityCenter.tsx` | 2218 | 第三个安全中心副本 | **删除**，统一入口 | ✅ PMO-04 |
| `UserManagement.tsx` | 1393 | 用户CRUD+角色绑定+搜索混在一起 | 拆为 UserList(400行) + UserEditModal(300行) + UserRoleBinding(250行) + UserFilter(150行) | ❌ 未拆分 |
| `TenantManager.tsx` | 1201 | Phase 1去掉租户 | 移除侧边栏入口，文件保留不删（后续版本用） | ❌ 侧边栏仍在 |
| `LanguageContext.tsx` | 2690 | 翻译字符串直接写在组件内 | 拆为 LanguageContext(200行) + zh-CN.json(独立文件) + en.json(独立文件) | ✅ PMO-04 |

### 后端（sysman相关）

| 文件 | 行数 | 问题 | 措施 | 状态 |
|------|:--:|------|------|:--:|
| `SecurityConfigController.java` | 983→591 | 安全配置+用户安全profile+角色安全profile+默认profile全在一个文件 | 拆为 SecurityProfileController(400行) + SecurityConfigController(400行) | ✅ SecurityProfileController已建，原文件591行 |

---

## 二、前端样式与国际化

| # | 问题 | 现状 | 目标 | 状态 |
|---|------|------|------|:--:|
| S1 | 国际化不完整 | LanguageContext.tsx 2690行混写翻译，无独立语言文件 | 翻译文本抽取为 `locales/zh-CN.json` + `locales/en.json`，LanguageContext只做加载逻辑 | ✅ |
| S2 | 中文硬编码 | 前端页面大量中文硬编码（"用户管理""租户管理""系统配置"） | 统一走 `t("nav.iam")` / `t("nav.systemConfig")` | ❌ 侧边栏仍硬编码 |
| S3 | 样式一致性 | 只用Tailwind+一个index.css，无主题变量 | sysman页面统一使用ECOS主题token（通过useTheme()），不从零写颜色 | ❌ 未验证 |
| S4 | 响应式 | 未验证 | 安全中心页面在1920x1080和1366x768下布局正常 | ❌ 未验证 |
| S5 | 加载状态 | 无骨架屏/loading | 列表加载显示骨架屏，操作提交显示loading按钮 | ❌ 无骨架屏 |
| S6 | 操作反馈 | 无统一toast | 操作成功/失败统一用toast通知（绿色成功/红色失败），非alert弹窗 | ❌ 未验证 |
| S7 | 确认弹窗 | 删除等危险操作无二次确认 | 删除/下线等操作弹出确认对话框（"确定删除用户XXX？此操作不可撤销"） | ❌ 未验证 |

---

## 三、前端缺失功能与操作

### 3.1 用户管理 (UserManagement.tsx, 1393行)

| # | 功能 | 现状 | 目标 | 状态 |
|---|------|------|------|:--:|
| U1 | 筛选 | 有search=23处 | 增加按角色下拉筛选、按状态(正常/锁定)筛选 | ✅ PMO-03 |
| U2 | 批量操作 | 无 | 批量启用/禁用/删除 | ❌ 无checkbox |
| U3 | CSV导入 | 无 | 上传CSV批量创建用户（含校验：用户名重复/邮箱格式/必填字段） | ✅ 按钮存在,功能待验证 |
| U4 | CSV导出 | 无 | 当前筛选结果导出CSV | ✅ 按钮存在,功能待验证 |
| U5 | 用户详情 | 无 | 点击用户→右侧抽屉展开：基本信息/角色/安全profile/最近登录记录 | ❌ 点击无反应 |
| U6 | 强制下线 | 无 | 管理员可强制某用户下线（标记其refresh token失效） | ✅ PMO-03 |
| U7 | 密码重置 | 无 | 管理员可重置用户密码（发送临时密码/强制下次登录修改） | ✅ 操作列有编辑/重置按钮 |
| U8 | 空状态 | empty=4 | 列表为空时显示引导："暂无用户，点击创建或导入CSV" | ✅ PMO-03 |

### 3.2 系统配置 (SystemConfigManager.tsx, 355行)

| # | 功能 | 现状 | 目标 | 状态 |
|---|------|------|------|:--:|
| C1 | 分组展示 | 无 | 按分类展示：安全配置/会话配置/通知配置/其他 | ✅ PMO-03 |
| C2 | 配置描述 | 无 | 每个配置项显示key+中文描述+当前值+默认值+修改历史 | ✅ 分组+描述已呈现 |
| C3 | 配置验证 | 无 | 修改时实时校验（数字范围/格式/必填），不合法时标红+提示 | ❌ 未验证 |
| C4 | 配置审计 | 无 | 每次修改记录：谁/何时/改了什么/旧值→新值 | ✅ "参数审计"按钮 |
| C5 | 重置默认 | 无 | 每个配置项有"恢复默认"按钮 | ✅ PMO-03 |
| C6 | 安全配置靠底 | — | 安全相关配置（密码策略/锁定策略/会话策略）独立分组，排在配置列表底部 | ✅ PMO-03 |

> **安全配置独立分组靠底原则**：系统配置分两类——全局通用配置（session_timeout等）在上，安全引擎专属配置（password_min_length、max_login_attempts、lockout_duration_minutes、max_concurrent_sessions）在下，逻辑分组、视觉分隔。

### 3.3 安全审计 (SecurityAudit.tsx, 259行)

| # | 功能 | 现状 | 目标 | 状态 |
|---|------|------|------|:--:|
| A1 | 筛选 | 无(search=0) | 按用户/操作类型/时间范围/IP地址筛选 | ✅ PMO-04 |
| A2 | 时间线视图 | 无 | 左侧时间线+右侧详情卡片 | ✅ PMO-04 |
| A3 | 导出 | 无 | 筛选结果导出CSV/PDF | ✅ PMO-04 |
| A4 | 详情 | 无 | 点击某条→展开：请求参数/响应摘要/耗时/IP/UserAgent | ✅ PMO-04 |
| A5 | 统计面板 | 无 | 顶部：今日操作数/失败数/活跃用户数/异常IP数 | ✅ PMO-04 |
| A6 | 翻页 | 未知 | 支持分页+每页条数选择 | ✅ PMO-04 |

### 3.4 安全中心（新增统一入口）

| # | 功能 | 目标 | 状态 |
|---|------|------|:--:|
| SC1 | 统一入口 | 侧边栏"安全中心"替代分散的"用户管理/系统配置/安全审计"入口 | ✅ PMO-04 |
| SC2 | 事前Tab | 用户管理+角色管理+权限定义+组织管理（复用现有组件，整合到一个Tab下） | ✅ PMO-04 |
| SC3 | 事中Tab | 安全策略占位（ABAC/MAC/PBAC/RLS/CLS/脱敏），显示"建设中，预计Phase 2-5逐步开放" | ✅ PMO-04 |
| SC4 | 事后Tab | 审计日志时间线+筛选+导出+统计面板 | ✅ PMO-04 |
| SC5 | 侧边栏调整 | 去掉"用户管理""系统配置""租户管理"独立入口，合并为"安全中心" | ⚠️ "租户管理"入口仍在 |

---

## 四、安全体系对齐后的差距

### 4.1 登录安全（🔴阻塞）

| # | 问题 | 现状 | 目标 | 状态 |
|---|------|------|------|:--:|
| L1 | 登录失败锁定 | 完全未实现 | users表加`failed_attempts`和`locked_until`字段。5次失败→锁定15分钟。消费SysConfig的`max_login_attempts`和`lockout_duration_minutes` | ✅ PMO-02 |
| L2 | 首次登录强制改密码 | 完全未实现 | users表加`password_change_required`字段。创建用户时默认true。登录检查→返回特殊错误码`PASSWORD_CHANGE_REQUIRED`→前端跳转改密码页 | ✅ PMO-02(字段已建) |
| L3 | 密码强度校验 | BCrypt但无复杂度检查 | 消费SysConfig的`password_min_length`、`password_require_upper`、`password_require_digit`、`password_require_special`。前端实时显示密码强度条 | ❌ 前端强度条未验证 |
| L4 | 密码过期 | SysConfig有key但未实现 | 消费`password_expire_days`。登录时检查上次改密时间，超期则返回`PASSWORD_EXPIRED` | ❌ 逻辑未验证 |

### 4.2 内置角色与权限（🔴阻塞）

| # | 问题 | 目标 | 状态 |
|---|------|------|:--:|
| R1 | 无内置角色seed | DataInitializer启动时自动创建5个角色（admin/data-manager/ontology-designer/knowledge-engineer/analyst） | ✅ PMO-05(验证脚本确认5 roles) |
| R2 | 角色无默认权限 | 每个内置角色附带默认权限集：admin=全部，analyst=读+AI对话，其他=对应域CRUD | ❌ 权限集未验证 |

### 4.3 安全接入规则（🟡重要）— ✅ 全部完成

| # | 目标 | 状态 |
|---|------|:--:|
| AR1 | 定义安全接入规则文档（`docs/1-sysman/03-安全接入规则.md`），明确各引擎调用security-engine的REST端点+请求格式 | ✅ PMO-05 |
| AR2 | 规则写入引擎AGENTS.md，让后续Phase的PMO知道"安全能力只走REST，不在本引擎重复实现" | ✅ PMO-05 |

### 4.4 配置统一 — ✅ 全部完成

| # | 配置项 | 归类 | 说明 |
|---|--------|------|------|
| 全局通用（靠上） | session_timeout_minutes | 全局 | 会话超时分钟数 |
| 全局通用（靠上） | audit_retention_days | 全局 | 审计日志保留天 |
| 安全专属（靠底） | password_min_length | 安全 | 密码最小长度 |
| 安全专属（靠底） | password_require_upper | 安全 | 密码需含大写 |
| 安全专属（靠底） | password_require_digit | 安全 | 密码需含数字 |
| 安全专属（靠底） | password_require_special | 安全 | 密码需含特殊字符 |
| 安全专属（靠底） | password_expire_days | 安全 | 密码过期天数 |
| 安全专属（靠底） | password_history_count | 安全 | 密码历史限制 |
| 安全专属（靠底） | max_login_attempts | 安全 | 最大登录尝试 |
| 安全专属（靠底） | lockout_duration_minutes | 安全 | 锁定分钟数 |
| 安全专属（靠底） | max_concurrent_sessions | 安全 | 最大并发会话 |

**前端SystemConfigManager展示规则**：全局通用配置在上方分组，安全专属配置在下方分组（两组间有视觉分隔线+标题），安全配置不可删除。

---

## 五、差距严重度汇总（更新）

| 严重度 | 原计划 | 已完成 | 遗留 |
|:--:|:--:|:--:|:--:|
| 🔴 阻塞 | 10 | 8 | 2（UserManagement拆分、租户入口移除） |
| 🟡 重要 | 15 | 12 | 3（批量操作、用户详情抽屉、密码强度前端） |
| 🟢 体验 | — | — | 7（S2~S7: i18n/样式/响应式/骨架屏/toast/确认弹窗） |

---

## 六、复盘总结

**Phase 1 核心目标达成**：登录安全增强(L1/L2)、内置角色(R1)、安全中心统一入口(SC1-5)、安全接入规则(AR1-2)全部落地。

**遗留12项**：集中在三个领域——
1. **拆分未执行**(2项)：UserManagement.tsx拆分、租户管理侧边栏移除
2. **功能未闭环**(3项)：批量操作(U2)、用户详情抽屉(U5)、角色默认权限(R2)
3. **体验未打磨**(7项)：S2~S7+i18n/样式/响应式/骨架屏/toast/确认弹窗

**建议**：12项遗留收敛为 `PMO-06 Phase 1 收尾`，工期3天，不阻塞Phase 2启动。

---

## 七、可复用资产

- ✅ AuthController（登录/JWT/BCrypt/RefreshToken）— 增强而非重写
- ✅ UserController（9 endpoints）
- ✅ RoleController（7 endpoints）
- ✅ PermissionController（5 endpoints）
- ✅ SysConfigController（配置key已定义）
- ✅ AuditController（审计日志查询）
- ✅ SecurityConfigController（安全profile CRUD）— Phase 1拆分
- ✅ SecurityProfileController（用户/角色安全profile）— 新增
- ✅ UserManagement.tsx（1393行）— 待拆分增强
- ✅ SecurityAudit.tsx（259行）— 增强非重写
- ✅ LanguageContext.tsx（2690行）— 拆分为locales
- ✅ locales/zh-CN.json + en.json
- ✅ 集成验证脚本 tests/phase1-verify.sh
- ✅ 安全接入规则文档 docs/1-sysman/03-安全接入规则.md
- ✅ 前端已有Tailwind主题系统（useTheme()）
