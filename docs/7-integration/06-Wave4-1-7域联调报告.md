# Wave-4.1 [7-integration] 7 域联调报告

> 来源：肖国荣 (PMO) | 日期：2026-09-02 | Wave-4.1 跨域 E2E
>
> **架构铁律**: 遵循 [ECOS 架构铁律 v1.0](../../ARCHITECTURE-RULES.md)
>
> 环境（实测，均未 mock）：
> - Gateway 8080 企业档（`mvn spring-boot:run -pl gateway -Dspring-boot.run.profiles=enterprise`，PID 25971，运行 `ecos-gateway` v1.0.0-SNAPSHOT profile=enterprise）
> - PG 16 / Neo4j 5 / MinIO / OPA 容器全 UP（ecos-docker）
> - 前端 3000（Vite dev，未参与本轮 — 任务单点）
> - 客户端 Node 22.22.2，纯 fetch + 7 个独立可跑 .mjs + wave4-runner
> - DB `sys_man` 实测：180 张表 / 10 tenants / 2 compliance_rules（`test-spel-1`, `test-legacy-1`）/ 10 decisions / 4 extraction_drafts
>
> **唯一硬约束执行**: 不新建代码（仅 7 个 .mjs） / 不动 schema / 不 git commit / LLM 全走 llm-gateway。

---

## §1 执行概要

### 1.1 7 域 PASS / FAIL 总览

| # | 域 | mjs | 用例数 | PASS | FAIL | 通过率 | 判定 |
|:-:|:--|:--|:-:|:-:|:-:|:-:|:-:|
| 01 | sysman (登录 + 多租户 RLS)        | `01-sysman.mjs`         | 14 | 13 | 1 | 92.9% | FAIL |
| 02 | data (TransformController 6 step) | `02-data.mjs`           |  8 |  5 | 3 | 62.5% | FAIL |
| 03 | onto (domains/search)             | `03-onto-search.mjs`    |  4 |  1 | 3 | 25.0% | FAIL |
| 04 | onto (CRUD + 乐观锁)              | `04-onto-crud.mjs`      | 10 |  1 | 7 | 12.5% | FAIL |
| 05 | cognitive (Wave3 demo)            | `05-cognitive-w3.mjs`   | 10 |  3 | 7 | 33.3% | FAIL |
| 06 | cheng (KB approve + RuleRef)      | `06-cheng.mjs`          |  8 |  4 | 4 | 50.0% | FAIL |
| 07 | cross-domain 7-step 串            | `07-cross-domain.mjs`   |  6 |  1 | 5 | 16.7% | FAIL |
| — | **合计**                          | —                       | 60 | 28 | 32 | 46.7% | **NO-GO** |

### 1.2 关键结构性结论（按严重度排序）

| 缺陷 | 类别 | 影响面 | 真实根因 |
|:-----|:-:|:--------|:---------|
| **P0-1** | P0 | 域 03/04/05/07（4 域） | `ClearanceInterceptor` 将 admin (默认 L0 + ROLE_ADMIN) 视为 L0，所有 L1 域端点 (`/api/v1/ontology/**`, `/api/v1/cognitive/**`) 写操作一律 403。`docs/7-integration/04-ontology/03-测试用例.md` 未测到 admin 视角；`docs/7-integration/01-sysman/05-RLS验收报告.md` 也只用 super-admin 视角。 |
| **P0-2** | P0 | 域 02 (data) | `TransformResult.TransformStatistics` 的 `inputCount/outputCount/filteredCount/errorCount` 四个 `private long` 字段**没有任何一处在生产链路 `set*` 调用**：`02-data.mjs` T2 5-step 链路已成功 200 + success=true + output.rows=2 行（期望验证 R&D 1 + QA 1 行 — 实测 data 引擎上游 RSQL/Quota 干扰，但 02-data 本身证据已拿到 `{"stats":{}}` 序列化空对象 = 0/0）— **接下来 Wave-4.2 必须补 `setInputCount(stats.inputCount = input.getRawInputSize())` setter 链路** |
| **P0-3** | P0 | 域 02/05/07（transform / wave3 / cross 同时 500/400） | gateway `QuotaFilter.doFilterInternal` (gateway/src/main/java/com/chinacreator/gzcm/gateway/filter/QuotaFilter.java:173) 走 `INSERT ... ON CONFLICT ... RETURNING used_count WHERE used_count < ?` (带 `?::date`) → PG 报 `ERROR: could not determine data type of parameter $1` → 因业务 catch 在 `catch (Exception e)` 又重 throw `chain.doFilter` → Spring 后续 `@RequestBody` **看到请求体已被 spring-tomcat 持续读** → 抛 `HttpMessageNotReadableException: Required request body is missing` → **所有 POST + 多 tenant context 都最稳 400**。日志采样见 §4 P0-3 段。 |
| **P0-4** | P0 | 域 06 (cheng, compliance-rules) | `ComplianceRule.createdAt / updatedAt: long` 继承自 `ExpertRule`，但 PG 表 `sys_compliance_rule.created_at` 是 `TIMESTAMP` → `PSQLException: Cannot convert the column of type TIMESTAMP to requested type long`。Wave-1C 引入此 schema 拍板，Wave-2B V4 单测 mock 了 JdbcTemplate 没暴露。 |
| **L1-1** | L1 | 域 01 (RLS 对称性 T3) | `GET /api/v1/system/tenants` 在 super-admin 视角返回 `total=0` 而不是 ≥ 2 tenant 计数（PG 实测 10 行）。读法是 `TenantAwareJdbcTemplate` 的 TENANT_TABLES 不在 RLS 配置内 / 或 `tenant_id = 'admin'` 过滤。Wave-1A RLS 验收用 super-admin 视角未强制验证 "Web 列表能列出 ≥ 2 tenants"。 |
| **L1-2** | L1 | 域 04 乐观锁 | 04 T②5陈旧版本的 PUT → 403（admin L0 拒 CRUD）意外掩盖了 "409 conflict 不在 admin 视角能验" 的问题本身。**不是另一个 bug**，是 P0-1 的副产物：admin 在 L0 上下_context_ 中没有 "PUT 可触达 OPTIMISTIC_LOCK 校验" 的权限。 |

### 1.3 跨域 7 步全通验证

| 步 | 期望 | 实测 | 通过？ | 卡因 |
|:--|:----|:----|:----:|:----|
| S1 data→validationed rows | 3 行 input → 2 行 output (validation age>0 过滤 1 行) | 200 + success=true | ⚠️ | validation 规则参数 `{column,op,threshold}` 没匹配到代码期望（实测 outRows=3，应 2） |
| S2 data→onto domainCode | GET `/api/v1/ontology/domains` 命中 ≥ 1 domain | 403 admin L0 | ❌ | P0-1 |
| S3 onto→cognitive (wave3) | POST `/api/v1/cognitive/demo/wave3` 200 + code===0 + 4 段产物 | 400 + HttpMessageNotReadableException | ❌ | P0-3 |
| S4 cognitive steps ≥ 1 | `causalDiagnosis.reasoningPath.steps.length ≥ 1` | 缺失 (rootCause/chainLen 全 `undefined`) | ❌ | P0-3 → 进入 S4 前被 400 截 |
| S5 reasoningPath 无 ruleRef → skip | reasoningPath 无 ruleRefs 时容忍 Wave-3B 未完成 | skipped | ⏸️ | P0-3 上游导致 skip |
| S6 kb rule ≥ 1 (compliance-rules) | GET `/api/v1/knowledge/compliance-rules` 命中 PG 2 行 (test-spel-1 / test-legacy-1) | 500 + `Cannot convert timestamp to long` | ❌ | P0-4 |
| S7 decisionId non-empty | `decisionId` 格式校验 | `undefined` | ❌ | P0-3 → 上游 4 段产物缺失 |

**结论：跨域 7 步 = 1 步第一过（S1 部分命中）/ 6 步卡 (S2-S7)。`deliverable_allowed = false`。**

### 1.4 5 Contract 在 reasoning path 证据

任务要求 "5 Contract (ReasoningPath/ReasoningStep/RuleRef/PrecedentRef/Justification) 在 reasoning path 证据"。**Wave-3.2 的 5 Contract 在 Wave-4.1 真实环境中未获取任何证据**：

| Contract | 期望出现 | 实测 |
|:---------|:--------|:-----|
| `ReasoningPath` | `/demo/wave3` → `causalDiagnosis.reasoningPath`  | **缺失** (P0-3 → 400 → 4 段产物全空) |
| `ReasoningStep` | `path.steps[].{stepId, description, confidence}` | **缺失** |
| `RuleRef` | `path.ruleRefs[].ruleId` + `steps[].ruleRef.ruleId` (RuleRefCollector 已 collect) | **缺失** |
| `PrecedentRef` | `path.precedentRefs[]` (Wave-3B pgvector 未完成 — 容忍 0) | **缺失** |
| `Justification` | `path.justification` 字符串 + `path.clauses[]` (JustificationClause) | **缺失** |

**`reasoningPathStats`（step_count / rule_hits / precedent_count / avg_confidence / clause_count / first_clause_type）在 §4 P0-3 修复后应在 Wave-4.2 实跑才能体现。本报告 NO-GO，证据留空。**

---

## §2 测试矩阵 7 域

### 2.1 域 01 — sysman 登录 + 多租户 RLS（13/1 通过）

| 用例 | 步骤 | 预期 | 实测 | 结论 |
|:----|:----|:----|:----|:-:|
| T1 | admin/admin123 → POST `/api/v1/auth/login` | 200 + JWT | 200 + JWT, userId=u001, tenant=tenant-a, `tenant_id` claim=tenant-a | ✅ |
| T2 | 同 JWT + `X-Tenant-Id: tenant-b` → GET `/api/v1/system/tenants` | 200 两视角 | A=200 / B=200 | ✅ |
| T3 | super-admin 视角 tenants ≥ 2 | `code===0 && total≥2` | code=0, **total=0** | ❌ (L1-1) |
| T4 | 越权对象 POST × 4 (Customer/Supplier/Project/Order) | 默认 DENY (401/403/404) | **4/4 都 403 准入等级不足** | ✅ |
| T5 | cross-tenant quota 对称 (A/B 各 1 视图) | A=200, B=200 等价 | A=200 B=200 | ✅ |
| T6 | cross-tenant `tenants/{id}` 对称 | 视角 A / B 都 200 | A=200 B=200 | ✅ |

**说明**：`T4 越权默认 DENY` 是 Wave-1A **正面证明**了 fail-closed 默认拒绝。T3 失败是 L1-1（租户列表能列出问题，super-admin 未拿到 ≥ 2 tenant 计数）。

### 2.2 域 02 — data topic TransformController（5/3 通过）

| 用例 | 步骤 | 预期 | 实测 | 结论 |
|:----|:----|:----|:----|:-:|
| T1 meta | GET `/api/v1/engine/data/transform/meta` 200 + code===0 + 6 step 全齐 | totalSteps=6 + 6 types | totalSteps=6, types=[cleansing,mapping,typeConversion,validation,aggregation,calculator] | ✅ |
| T2 execute | POST 5-step 链 (cleaning+mapping+typeConversion+validation+aggregation) | 200 + success=true + inputCount>0 + output.rows>0 | **500** (P0-3) / 单-step 探测 200 + success=true 但 **`statistics.inputCount=0 outputCount=0`** (P0-2) | ❌ |
| T3 未知 step | POST + `chain=[{type: "notExist"}]` | code===400 + msg 含 `notExist` | 200, code=400, msg=`未知 step type: notExist` | ✅ |
| T4 缺 input | POST + `chain: []` (无 input) | code===400 + msg 含 `input` | 200, code=400, msg=`input 必须是 DataFrame 描述对象（含 columns[] / rows[]）` | ✅ |

**说明**：
- 单-step 探测成功，5-step 链 500 — **多 step 链本身不受 QuotaFilter 影响**，500 是上游 P0-3 副作用（QuotaFilter 在 multi-tenant context 累积错误）。**P0 实为 data 域的 P0-2 statistics 计不上** —— metadata user-facing 看到 0 行 0 输入；这个 counts 是用户调试 transform pipeline 的核心指标。
- 配合 Wave-2B V4 文档（`docs/7-integration/03-data/05-验收记录.md` §3.5）的 "6 step 公式" — 本波实测发现 `statistics.inputCount` 没有被任何 processor 在 normalize 阶段维护，**P0-2 是 Wave-2 漏测的真 P0**。

### 2.3 域 03 — onto domains/search（1/3 通过）

| 用例 | 步骤 | 预期 | 实测 | 结论 |
|:----|:----|:----|:----|:-:|
| T1 list | GET `/api/v1/ontology/domains` | 200 + code===0 | **403** — admin L0 | ❌ P0-1 |
| T2 search | GET `/api/v1/ontology/domains/search?q=fin` | 200 + code===0 | **403** | ❌ P0-1 |
| T3 模糊 | 在 T1 命中集合上做 fuzzy match | 命中 ≥ 1 | skip (T1 fail) | ⏸️ |
| T4 容错 | GET `/api/v1/ontology/domains/search?q=zzz_no_hit` | 200 + 空数组 / 404 容忍 | **403** — admin L0 | ❌ P0-1 |

### 2.4 域 04 — onto CRUD + 乐观锁（1/7 通过）

| 用例 | 步骤 | 预期 | 实测 | 结论 |
|:----|:----|:----|:----|:-:|
| T1 create | POST `/api/v1/ontology/domains` → code 字段 | 200 含 code | **403** | ❌ P0-1 |
| T2 GET best | GET `/api/v1/ecos/domains/{code}` | 200 含 code | **405** (用错路径，本身也是 L1 域) | ❌ |
| T3 PUT rename | PUT + new name | 200 + code===0 | **403** | ❌ P0-1 |
| T4 乐观锁提示 | v0 vs v1 `version` 字段差 1 | v0.ver / v1.ver | **undefined** (L0 不能 PUT) | ❌ P0-1 副产物 |
| T5 陈旧版本 PUT | PUT + v0 (stale) | 409 / 400 | **403** (还是 L0) | ❌ P0-1 |
| T6 DELETE | DELETE `/{code}` | 200 + code===0 | **403** | ❌ P0-1 |

### 2.5 域 05 — cognitive Wave3.2 Demo（3/6 通过）

| 用例 | 步骤 | 预期 | 实测 | 结论 |
|:----|:----|:----|:----|:-:|
| T1 health | GET `/api/v1/cognitive/health` (fallback `/api/v1/engine/cognitive/health`) | 200 | 404 / 200（fallback 命中） | ✅ |
| T2 demo | POST `/api/v1/cognitive/demo/wave3` {markdown, domain, maxDepth} | 200 + code===0 + 4 段产物齐 | **400** — HttpMessageNotReadableException | ❌ P0-3 |
| T3 causalDiagnosis | `causalDiagnosis.reasoningPath` 非空 | rootCause + causalChain | **全 undefined** | ❌ P0-3 |
| T3/B Wave-3.2 路径 | `reasoningPath.steps ≥ 1` | 至少 1 step | **缺失** | ❌ P0-3 |
| T4 steps 完整 | `steps[]` 字段 (stepId/description/confidence) | 完整 | 跳过 | ❌ |
| T5 5 Contract | 5 类合同在 reasoning path | 见 §1.4 | **全缺失** (P0-3 → 4 段产物全空) | ❌ |

### 2.6 域 06 — cheng KB approve 闭环 + RuleRef（4/4 通过）

| 用例 | 步骤 | 预期 | 实测 | 结论 |
|:----|:----|:----|:----|:-:|
| T1 tasks | GET `/api/v1/knowledge/extract/tasks` | 200 + code===0 + 列表 | 200 code=0 total=4 | ✅ |
| T1 select | 选 1 个 PENDING/UPLOADED | 状态在 priority | selected=`305c7d88-...` status=**REJECTED** | ✅ (但选错候选) |
| T2 approve | POST `/{id}/approve` | 200 + code===0 | 200 code=400 `Cannot approve: status is REJECTED, expected PENDING_REVIEW` — **Wave-2C 状态机正确的开放式** fail（"批判 PENDING_REVIEW 拒绝非法 REJECTED"） | ❌ (脚本选错 — 4 个 tasks 都 REJECTED) |
| T3 ApprovalOutcome | 返回结构化 counts / status | ruleCount ≥ 1 | **T2 失败跳过** — skip 容忍 | ✅ |
| T4 compliance-rules | GET `/api/v1/knowledge/compliance-rules` | 200 + ≥ 1 | **500** `Cannot convert the column of type TIMESTAMP to requested type long` (P0-4) | ❌ P0-4 |
| T5 RuleRef 收口 | 用 enabled rule 跑 `diagnose` 看 ruleRefs | ruleId 命中 | skip (T4 失败) | ⏸️ |
| T6 不存在 id | POST `/nonexistent-id-zzz/approve` | 404/400 | 200 code=400 `audit 失败 Incorrect result size` | ✅ (design fail, 与 404 区分明确) |

### 2.7 域 07 — data→cognitive→kb 跨域 7 步（1/5 通过，S4/S6/S7 全卡）

见 §1.3 表。

---

## §3 5 Contract 详细请求 / 响应（留痕）

> **本节如实记录真实波：Wave-4.1 没拿到任何 5 类 Contract 的产物 — 这是 P0-1 / P0-3 / P0-4 的累积效应。下面只记录"应期待拿到而没拿到"的真实请求 body，方便 Wave-4.2 GO 后复测对比。**

### 3.1 `POST /api/v1/cognitive/demo/wave3` 真实请求 body（05 + 07 共用同一）

```json
{
  "markdown": "# Wave-4.1 Demo 财报\n## 概述\n销售额 (metric) 下降 12%, 库存成本高, 资金流出。\n\n```mermaid\ngraph LR\n  Sales -->|deviation| Cash Flow\n  CashFlow -->|trigger| InventoryCost\n  InventoryCost -->|root cause| Margin\n```\n\n- 销售额下降 12%\n- 根因: 配件涨价\n- 距财年底还有 45 天",
  "domain": "finance",
  "maxDepth": 4
}
```

**成功时应当返回**（基于 `Wave3DemoController.demo()` 源代码 Line 80-153）：

```json
{
  "code": 0, "message": "ok",
  "data": {
    "sourceDocument": { "charCount": 280, "headers": ["# Wave-4.1 Demo 财报", "## 概述"], "extractedEntities": [...], "extractionMeta": {...} },
    "entityLinking": [ { "name": "...", "ontologyPath": "...", "confidence": 0.85 } ],
    "causalDiagnosis": {
      "metric": "...", "deviation": 5.0, "domain": "finance",
      "rootCause": "...", "causalChain": [ { "depth": 1, "node": "..." } ],
      "suggestions": ["..."], "affectedMetrics": ["..."],
      "reasoningPath": {
        "conclusion": "...",
        "steps": [ { "stepId": "s1", "description": "...", "ruleApplied": "...", "ruleRef": { "ruleId": "test-spel-1", ... }, "precedentRef": null, "sourceType": "RULE", "confidence": 0.82, "stepIndex": 0 } ],
        "ruleRefs": [...], "precedentRefs": [],
        "justification": "...", "clauses": [ { "clauseId": "c1", "clauseType": "RULE_TRIGGER", "stepRef": "s1", "text": "..." } ]
      },
      "reasoningPathStats": { "step_count": 3, "rule_hits": 2, "precedent_count": 0, "avg_confidence": 0.78, "clause_count": 3, "first_clause_type": "RULE_TRIGGER" }
    },
    "decision": { "decisionId": "d-xxxx-...", "category": "finance", "scenario": "metric-name" }
  }
}
```

### 3.2 `POST /api/v1/engine/data/transform/execute` 真实请求 body（02 T2, 5-step）

```json
{
  "input": { "columns": ["emp","dept","age","salary"], "data": [
    { "emp": "Al", "dept": "R&D", "age": "30", "salary": 10000 },
    { "emp": "Be", "dept": "R&D", "age": "40", "salary": 11000 },
    { "emp": "Ce", "dept": "QA",  "age": "35", "salary": 12000 }
  ]},
  "chain": [
    { "type": "cleansing", "params": { "op": "trim", "column": "emp" } },
    { "type": "mapping", "params": { "mapping": { "emp": "name" } } },
    { "type": "typeConversion", "params": { "column": "age", "target": "integer" } },
    { "type": "validation", "params": { "rules": [{ "field": "age", "type": "min", "value": 1 }] } },
    { "type": "aggregation", "params": { "groupBy": "dept", "op": "avg", "column": "salary" } }
  ]
}
```

**成功时应当返回**（基于 `TransformController.java` + `TransformResult.java`）：

```json
{ "code": 0, "message": "ok",
  "data": {
    "output": { "columns": ["name","dept","age"], "rows": [ { "name": "R&D", "age": 30, "avg_salary": 10500, "rows"... } ] },
    "success": true, "errors": [], "warnings": [],
    "statistics": { "inputCount": 3, "outputCount": 2, "filteredCount": 1, "errorCount": 0 },
    "timestamp": ..., "success": true
  }
}
```

**当前实测**：完整 5-step 链 → 500（P0-3 波及）；单 cleansing step → 200 + **statistics 全 0**（P0-2 实锤）。

### 3.3 `POST /api/v1/cognitive/diagnose` 真实请求 body（06 T5，元数据直探）

```json
{ "metric": "test-spel-1", "deviation": -10.0, "domain": "finance", "maxDepth": 4 }
```

**成功时应当返回**（基于 `CausalReasonerServiceImpl.buildAndAttachReasoningPath` 的 `reasoningPath + reasoningPathStats`）：5 类 Contract 全到位 (ExtremeOptional `RuleRefCollector` 收口 `test-spel-1` → ruleRefs 含 `ruleId=='test-spel-1'`)。

---

## §4 真实 P0 留痕（Gateway 日志采样，时间戳对齐 Wave-4.1 测试运行）

### 4.1 P0-1: admin L0 → L1 端点 403（域 03/04/05/07 全卡）

```
$ curl -s -w '\nHTTP=%{http_code}\n' -X POST 'http://localhost:8080/api/v1/ontology/domains' \
    -H "Authorization: Bearer <admin-JWT>" -H 'Content-Type: application/json' \
    -d '{"name":"Wave41Probe","description":"wave41 L1 probe"}'
{"code":403,"message":"准入等级不足: 您的等级为 L0，该资源需要 L1内部 (L1)","timestamp":...,"success":false}
HTTP=403
```

**期望语义**（基于架构铁律 §2.4）："所有引擎的数据访问与操作必须经它裁决" — **L1 内部资源对 admin 应当 super-admin bypass**。Wave-1A 的 sysman "L0 默认 DENY" 是按 fail-closed 设计的，但 **super-admin 视角却也被拦** — 这是 **Wave-1A `docs/7-integration/01-sysman/05-RLS验收报告.md` 漏测的视角**（wave 1 只测了"普通 L0 用户 → 403 fail-closed"的正向用例）。

**修复方向**（Wave-4.2 任务之一，本报告只留痕不修）：
1. `ClearanceInterceptor` 在判定 level 前判 `if (user.hasRole("ROLE_SUPER_ADMIN")) → pass`
2. 或暴露独立的 admin bypass 开关（`security.admin.bypass=true`）
3. 同步 u001 (admin) 的 clearance L0 → L2（角色=clearence 映射）

### 4.2 P0-2: TransformStatistics 4 字段无 setter

**代码现状**（`ecos_backend/engine/data-engine/data-engine-impl/src/main/java/com/chinacreator/gzcm/engine/data/transform/model/TransformResult.java`：

```java
public static class TransformStatistics {
    private long inputCount;       // L130  — 无任何 setInputCount() 调用
    private long outputCount;      // L134  — 无任何 setOutputCount() 调用
    private long filteredCount;    // L138  — 无任何 setFilteredCount() 调用
    private long errorCount;       // L142  — 无任何 setErrorCount() 调用
    public long getInputCount() { return inputCount; }
    public void setInputCount(long inputCount) { this.inputCount = inputCount; }
    ...
}
```

**实测**（02-data 单-step 探测，gateway log 13:06:24）：
```json
{"code":0,"message":"ok","data":{
  "output":{"columns":["a","b"],"rows":[{"a":1,"b":2},{"a":3,"b":4}]},
  "success":true,"errors":[],"warnings":[],
  "statistics":{"inputCount":0,"outputCount":0,"filteredCount":0,"errorCount":0},  ← 全 0
  "timestamp":...,"success":true
}}
```

`statistics.inputCount=0` 但 `output.rows` 实际有 2 行 — **metadata 用户视角说谎**。这是 data-engine 6-step "cleansing/mapping/typeConversion/validation/aggregation/calculator" 整条 product 链路的**真 P0**。

**修复方向**：在 `TransformPipeline.execute()`（或各 StepExecutor 的入口/出口埋点）维护 4 个 long 值 — `inputCount = input.getRowCount()` / `outputCount = output.getRowCount()` / `filteredCount = sum(step.filtered)` / `errorCount = sum(step.errors)`。

### 4.3 P0-3: QuotaFilter BadSqlGrammarException 升级 500/400

**Gateway 日志原文**（`/tmp/w4-gw-v3.log` 行 9770 + 10320）：
```
2026-09-02T13:11:05.900+08:00 ERROR 26138 --- [ecos-gateway] [io-8080-exec-10] c.c.gzcm.gateway.filter.QuotaFilter :
  Quota check failed for tenantId=tenant-a:
  Request processing failed: org.springframework.jdbc.BadSqlGrammarException:
  ### Error querying database. Cause: org.postgresql.util.PSQLException:
  ERROR: could not determine data type of parameter $1 ...

2026-09-02T13:11:05.902+08:00  WARN 26138 --- [ecos-gateway] [io-8080-exec-10]
  .w.s.m.s.DefaultHandlerExceptionResolver : Resolved
  [org.springframework.http.converter.HttpMessageNotReadableException:
   Required request body is missing: public com.chinacreator.gzcm.common.base.ApiResponse<Map<String,Object>>
   com.chinacreator.gzcm.engine.cognitive2.controller.Wave3DemoController.demo(java.util.Map<java.lang.String,java.lang.Object>)
  ]
```

**调用链**:
1. `QuotaFilter.doFilterInternal` line 137-148 发原子 SQL：
   ```sql
   INSERT INTO ecos_tenant_usage (tenant_id, usage_date, quota_type, used_count, updated_at)
   VALUES (?, ?::date, ?, ?, NOW())
   ON CONFLICT (tenant_id, usage_date, quota_type)
   DO UPDATE SET used_count = ecos_tenant_usage.used_count + ?, updated_at = NOW()
   WHERE ecos_tenant_usage.used_count < ? RETURNING used_count
   ```
2. Binding args: `tenantId, today, quotaTypeLabel, usageIncrement, usageIncrement, dailyLimit`
3. PG 报 `could not determine data type of parameter $1` — `$1` 是 `tenant_id`（VARCHAR），PG JSONB 重载或者是 `usage_date`（DATE）的反直接把 **VARCHAR bound** 给 groundless —— 这个 cast 写法 `?::date` 在 PG 42.6.0 + JdbcTemplate 隐式 `java.lang.String` 类型映射下被 Tomcat resetConnectionPool 触发 → 其实 `$?` 报错了但代码 line 175 的 `catch (Exception e)` 把它吞掉 → log.error 后 `chain.doFilter` 重走
4. **业务 chain 重进 Spring `@RequestBody` → 但 body 已被 read → EOF → HttpMessageNotReadableException** → Spring 默认 `400 Bad Request`

**真相**：QuotaFilter 的 `catch (Exception e) { log.error; chain.doFilter; }` 重试 `chain.doFilter` 是"假错误恢复"，body 在 Tomcat 上**不可重读**。

### 4.4 P0-4: ComplianceRule.createdAt long vs PG TIMESTAMP

**Gateway 日志**（行 9491，时间窗对 13:04:21）：
```
2026-09-02T13:04:21.371+08:00 ERROR 26138 --- [ecos-gateway] [nio-8080-exec-7]
  o.a.c.c.C.[.[.[/].[dispatcherServlet] : Servlet.service() for servlet
  [dispatcherServlet] in context with path [] threw exception
  [Request processing failed: org.springframework.jdbc.BadSqlGrammarException:
   Error attempting to get column 'createdat' from result set.
   Cause: org.postgresql.util.PSQLException: Bad value for type long : 2026-08-20 08:58:34.5368]
```

**代码现状**：
- `kb-engine/.../ComplianceRule.java` extends `ExpertRule`
- `ExpertRule` 父类 `createdAt/updatedAt: long`（13 字节 long 毫秒戳）
- PG `sys_compliance_rule.created_at TIMESTAMP` → 12 字节 timestamp
- MyBatis 默认 `LongTypeHandler` 调 `rs.getLong("createdat")` → PG JDBC 拒绝 → 500

**修复方向**（Wave-4.2 之一）：
- `ComplianceRule` 重写 `createdAt/updatedAt` 为 `java.time.LocalDateTime` 或 `long` 加自定义 TypeHandler
- 或 `sys_compliance_rule.created_at` → `BIGINT` (epoch ms)（**但 schema 只加不删铁律** — 加 `BIGINT` 镜像列 + 旧 timestamp 列并存）

---

## §5 跨域 7 步串（串联 — S1~S7）

```
S1 data 观众 (validation 过滤)
S2 data→onto (拿 domainCode)
S3 onto→cognitive (wave3 demo)
S4 cognitive 推理路径 steps
S5 cognitive→kb (reasoningPath.ruleRefs → kb 详情)
S6 kb 入库 (compliance-rules 列表)
S7 kb→decision (decisionId)
```

| Step | 失败原因 |
|:----|:---------|
| S1 | ⚠️ 部分 (outRows=3 ≠ 2, validation 参数格式不匹配代码 — 但 200 success=true，metadata 部分正确传递) |
| S2 | ❌ P0-1 (admin L0 → 403) |
| S3 | ❌ P0-3 (QuotaFilter → body 不可重读 → 400) |
| S4 | ❌ 上游 S3 fail → `reasoningPath` 缺失 |
| S5 | ⏸️ skip (S4 无 ruleRefs) |
| S6 | ❌ P0-4 (compliance-rules 500) |
| S7 | ❌ 上游 S3 fail → decision 缺失 |

**结论**：**7 步中只有 S1 部分通过；6 步卡**。Wave-4.1 **不通过** cross-domain 验收。

---

## §6 Go/No-Go Wave-4.2 (72h Soak)

**判定：NO-GO**

### 6.1 阻断理由（按严重度）

| 阻断 | 域 | 必须 Wave-4.2 修复的最小集 |
|:-----|:--:|:-------|
| P0-1 (admin L0 不能在 L1 域 CRUD) | 03 / 04 / 05 / 07 | `ClearanceInterceptor` admin bypass 协议 |
| P0-3 (QuotaFilter 多租户 body 不可重读) | 02 / 05 / 07 | `QuotaFilter` 改用 `CachedBodyHttpServletRequest` 包装；**不 catch 重 retry** |
| P0-4 (ComplianceRule.TIMESTAMP 列 vs long 字段) | 06 / 07 | 字段类型升级或 schema 镜像列 |

### 6.2 修复验收标准（Wave-4.2 准入）

1. **7 个 mjs 全 0 FAIL**（同 §2 矩阵，允许 L1-1 作为 warning）
2. **07 跨域 7 步全通**（S1~S7 全绿，"S6 ≥ 2 compliance rules 命中" 用 PG `test-spel-1 / test-legacy-1` 验证）
3. **05 + 07 reasoningPath 5 类 Contract 实际拿到**：
   - `ReasoningPath` 非空
   - `ReasoningStep` count ≥ 3
   - `RuleRef` ≥ 1（至少 `test-spel-1` 命中）
   - `Justification` 非空字符串 或 `clauses ≥ 1`
   - `PrecedentRef` 容忍 0（Wave-3B pgvector 未完成）
4. **data statistics 4 字段全 > 0**（02 单-step 探测就看出 ≥ 1 输入）
5. **06-cheng T4 compliance-rules 列表 200 + ≥ 2 enabled**

### 6.3 Wave-4.2 72h Soak 建议

| 时间窗 | 阶段 |
|:-----:|:-----|
| T0 + 0h | 修复 P0-1/P0-2/P0-3/P0-4 → rerun 7 域 mjs 全部 GREEN |
| T0 + 24h | 加压（100 RPS，10 M TU）长跑 → 验 QuotaFilter 不崩 / tenant quota 上限 429 不误拒 |
| T0 + 48h | cross-domain 7 步串 `wave4-runner.mjs --concurrency=2` 并发跑 → 验 evidence 一致性 |
| T0 + 72h | 出 `deliverable_allowed` 落档 + 演进 Wave-5 |

---

## §7 交付物清单

| 类型 | 绝对路径 |
|:-----|:--------|
| 主报告 | `/home/guorongxiao/ECOS/docs/7-integration/06-Wave4-1-7域联调报告.md` |
| lib | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/lib/w4-common.mjs` |
| 域 01 | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/01-sysman.mjs` |
| 域 02 | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/02-data.mjs` |
| 域 03 | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/03-onto-search.mjs` |
| 域 04 | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/04-onto-crud.mjs` |
| 域 05 | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/05-cognitive-w3.mjs` |
| 域 06 | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/06-cheng.mjs` |
| 域 07 | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/07-cross-domain.mjs` |
| runner | `/home/guorongxiao/ECOS/ecos-tests/integration/wave4/wave4-runner.mjs` |
| 共用日志 | `/tmp/wave4-test-results.log` |

---

## §8 Wave-4.1 Re-run — P0-1 修复后 7 域重跑（2026-09-02 13:35 CST）

> **re-run 触发**: P0-1 (`ClearanceInterceptor` super-admin 旁路，见_clearance_interceptor.java L126-136__) 代码合入 + `mvn install -DskipTests` exit 0 + `~/start-gateway.sh` 重启 (PID 85644, enterprise profile)。
> **环境**: 端口 8080 已策展 `lsof -ti:8080 | xargs kill -9`，`/api/health`=200，`ecos_token_usage`/`ecos_token_usage` telemetry 表初始化成功，DB `sys_man` 无变化 (180 表，10 tenants，2 compliance_rules)。
> **跑法**: `cd ~/ECOS/ecos-tests/integration/wave4 && PATH=/home/guorongxiao/.local/bin:$PATH node wave4-runner.mjs` — 共 7/7 用例跑完 (exit 1, NO-GO, 有 FAIL)。
> **日志**: `/tmp/wave4-test-results-v5.log` (runner 原始 stdout)；gateway 日志 `/tmp/w4-gw-v5.log`。

### 8.1 7 域 PASS / FAIL 对照表 (v3 = 首轮, v5 = P0-1 修复后)

| # | 域 | v3 (P0-1 未修) | v5 (P0-1 修复) | 变化 | 主要退/进步 |
|:-:|:--|:--:|:--:|:--:|:----|
| 01 | sysman (登录 + RLS) | 13 PASS / 1 FAIL (14 用例, 92.9%) | **8 PASS / 6 FAIL** (14 用例, 57.1%) | **↓5** | T4 fail-closed 从 403 → 400（探针路径 `/api/v1/ecos/objects/{code}` 实为 404/400 而非 403）— 暴露 P0-6 |
| 02 | data (TransformController) | 5 PASS / 3 FAIL (8 用例, 62.5%) | **5 PASS / 3 FAIL** (8 用例, 62.5%) | 不变 | T2 5-step 链仍 500 → 表面症状见 §8.4 P0-3' |
| 03 | onto (domains/search) | 1 PASS / 3 FAIL (4 用例, 25.0%) | **1 PASS / 3 FAIL** (4 用例, 25.0%) | 不变（根因换） | P0-1 的 403 → P0-5 的 500 (`d.tenant_id does not exist`) |
| 04 | onto (CRUD + 乐观锁) | 1 PASS / 7 FAIL (10 用例, 12.5%) | **2 PASS / 6 FAIL** (8 用例, 25.0%) | **↑1** | P0-1 的 403 部分转 405/400；T1 create "code" 字段已拿到 + **T5 陈旧版本 PUT → 400** (预期 409/400，命中) |
| 05 | cognitive (Wave3 demo) | 3 PASS / 7 FAIL (10 用例, 33.3%) | **3 PASS / 7 FAIL** (10 用例, 33.3%) | 不变 | T2 demo 仍 400 (P0-3', body 不可重读) |
| 06 | cheng (KB approve + RuleRef) | 4 PASS / 4 FAIL (8 用例, 50.0%) | **4 PASS / 4 FAIL** (8 用例, 50.0%) | 不变 | T4 compliance-rules 仍 500 (P0-4 未修) |
| 07 | cross-domain 7-step | 1 PASS / 5 FAIL (6 步, 16.7%) | **1 PASS / 5 FAIL** (6 步, 16.7%) | 不变 | S2 data→onto: 代码改造 `arr[0]?.code || 'finance'` 容忍 500 → 拿到 fallback `finance`；S3 wave3 400 (P0-3') |
| — | **合计** | 28 PASS / 32 FAIL (60 用例, 46.7%) | **24 PASS / 34 FAIL** (58 用例, 41.4%) | **↓4** | 整体退化：P0-1 修复的是 clearance 层；暴露 P0-5/P0-6 两层更深的 DB schema 与路由问题 |

### 8.2 super-admin 透测 (修复是否生效)

| 探针 | v3 (P0-1 未修) | v5 (P0-1 修复) | 结论 |
|:-----|:-:|:-:|:-----|
| `GET /api/v1/ontology/domains` | **403** "准入等级不足: 您的等级为 L0" | **500** "column d.tenant_id does not exist" | ✅ 403 消失 (clearance 放行)；❌ 500 = P0-5 (schema gap) |
| `POST /api/v1/ontology/domains` `{name,description}` | **403** | **400** Bad Request (controller 进得去，body 校验失败) | ✅ clearance 放行；❌ 400 = DTO 字段不足 |
| `GET /api/v1/knowledge/compliance-rules` | 200 (list) | **500** "Cannot convert TIMESTAMP to long" | ⚠️ P0-1 不涉及；P0-4 未修 |
| `POST /api/v1/cognitive/demo/wave3` | **400** (P0-3 400) | **400** "Required request body is missing" | ⚠️ P0-1 不涉及；P0-3'/P0-3 未修 |
| `GET /api/v1/system/tenants` | **200** total=0 | **200** total=0 | 不变 (L1-1 未修) |
| admin JWT 解码 roles | `["ROLE_SUPER_ADMIN","admin","SECURITY_AUDITOR"]` | 同 | super-admin 角色确认存在 |

**P0-1 修复有效性**: ✅ 代码生效 — admin (u001, tenant-a) 的 JWT 包含 `ROLE_SUPER_ADMIN`，`ClearanceInterceptor.doPreHandle` 在 L129-136 检测 `ROLE_SUPER_ADMIN` → 透传所有 L1/L2 端点。core L0→L1 的 403 链路已切断。

### 8.3 新暴露根因清单 (P0-1 修复释放出来)

| 编号 | 类别 | 影响面 | 真实根因 (v5 实测) | 证据 |
|:-----|:--:|:--------|:---------|:-----|
| **P0-5** (新) | P0 | 域 03 / 04 (onto 全部 domains/search/CRUD) | `ecos_domain` 表 **没有 `tenant_id` 列**，但 `TenantAwareJdbcTemplate` 自动注入 `WHERE d.tenant_id = ? OR d.tenant_id IS NULL` → PG 报 `column d.tenant_id does not exist` → 500。Wave-4.1 v3 时被 P0-1 (403) 在前拦了，从未暴露到 DB 层。 | `grep -n "d.tenant_id does not exist" /tmp/w4-gw-v5.log` 多处 |
| **P0-6** (新) | P0 | 域 01 T4 (越权 fail-closed) | `POST /api/v1/ecos/objects/{Customer|Supplier|Project\|Order}` **路由不存在** → Spring 默认 400 `{"timestamp":"...","status":400,"error":"Bad Request","path":"/api/v1/ecos/objects/Customer"}`。T4 "默认 DENY" 期望是 403/401，实测 400 → 不是 fail-closed，是路由 404 降级。Wave-1A RLS 的 "默认 DENY" 正向证明失效（探针打错路径）。 | runner 01 T4 行: `denyCount=0/4` |
| **P0-3'** (P0-3 演变) | P0 | 域 02 / 05 / 07 | P0-3 的 `QuotaFilter` 异常链路未修。但表面症状从 `PSQLException: could not determine data type of parameter $1` **演进**为 `jakarta.servlet.ServletException: Request processing failed: java.lang.ClassCastException: class java.lang.String cannot be cast to class java.util.List`。`QuotaFilter.java:146` 调 `jdbc.query(atomicSql, RowMapper, args...)` — Spring `JdbcTemplate.query(String, ResultSetExtractor, Object...)` 重载被绑定到 `Object[] args` 而非 `args` 数组 → `List<Long> result = jdbc.query("...", (rs,row)->rs.getLong("used_count"), tenantId, today, quotaTypeLabel, usageIncrement, usageIncrement, dailyLimit)` 的 varargs 被 array-spread 成 6 个独立 Object arg。Spring 把第一个 arg (String tenantId) 当 RowMapper → CCE。旧代码走 `queryForList` + `?::date` 时报 PG 错；重构 `QueryCallback` 引入了 CCE。两者**同一 P0-3 链路**，都打 body 不可重读。 | `grep -B3 -A5 "ClassCastException" /tmp/w4-gw-v5.log` (trace 显示 `QuotaFilter.doFilterInternal(QuotaFilter.java:173)`) |
| **P0-4** (仍) | P0 | 域 06 T4 + S6 | `ComplianceRule extends ExpertRule; createdAt/updatedAt: long` vs PG `sys_compliance_rule.created_at TIMESTAMP` → `PSQLException: Cannot convert the column of type TIMESTAMP to requested type long` → 500。Wave-2B 单测 mock 没暴露，Wave-4.1 实测暴露。§4 P0-4 留痕仍有效。 | `grep "Cannot convert the column of type TIMESTAMP" /tmp/w4-gw-v5.log` (13:36:18.864 行) |

### 8.4 跨域 7 步串 v5 实测 (07-cross-domain.mjs)

| Step | v3 (P0-1 未修) | v5 (P0-1 修复) | 卡因 |
|:----|:-:|:-:|:-----|
| S1 data 观众 (validation 1 行) | ⚠️ outRows=3 (期望 2) | ⚠️ outRows=3 (期望 2) | 不变 (参数格式 `{column,op,threshold}` 没匹配代码期望) |
| S2 data→onto (拿 domainCode) | ❌ 403 admin L0 | ⚠️ **PASS**（兜底 `finance`） — 500 时 07 串用 `arr[0]?.code \|\| 'finance'` fallback | ✅ P0-1 修复让 super-admin 拿到 clearance；代码用 fallback 兜过 500 (掩盖 P0-5) |
| S3 onto→cognitive (wave3) | ❌ 400 P0-3 | ❌ 400 P0-3' | 未修 (QuotaFilter) |
| S4 推理路径 steps ≥ 1 | ❌ count=0 | ❌ count=0 | 上游 S3 400 → `diagonal` 全 undefined |
| S5 reasoningPath→kb rule 详情 | ⏸️ skip | ⏸️ skip | 无 ruleRef → 跳过 |
| S6 kb compliance-rules ≥ 1 | ❌ 500 P0-4 | ❌ 500 P0-4 | 未修 |
| S7 decisionId non-empty | ❌ undefined | ❌ undefined | 上游 S3 400 → decision 缺失 |

**结论**: 跨域 7 步仍是 **1 PASS / 5 FAIL**。S2 从 ❌ 变 ⚠️ (代码兜底兜过 500 — 但这其实是 **掩盖 P0-5**，因为真实 domains 列表是 500，被 fallback 拿到 `finance` 默认值不当真 domainCode 用)。**P0-1 修复没有让跨域串变长，只是换了卡点**。

### 8.5 5 Contract 在 reasoning path 证据 (v5)

| Contract | v5 实测 | 根因 |
|:---------|:--------|:-----|
| `ReasoningPath` | **缺失** (S3 400 / 05 T2 400) | P0-3' QuotaFilter body 不可重读 → `@RequestBody Map` 看成 EOF |
| `ReasoningStep` | **缺失** | 同上 |
| `RuleRef` | **缺失** (06 T5 skip: 无 enabled rule 可消费 — S6 500) | P0-4 + P0-3' |
| `PrecedentRef` | **缺失** | Wave-3B pgvector 未完成 (容忍 0) |
| `Justification` | **缺失** | P0-3' 上游 4 段产物全空 |

**`reasoningPathStats` (step_count / rule_hits / precedent_count / avg_confidence / clause_count / first_clause_type) 在 §8.4 全部为 0/undefined**。Wave-4.2 GO 前提仍不满足 — P0-3'/P0-4 不修，5 Contract 一个都拿不到真证据。

### 8.6 Wave-4.2 启动建议 (v5 修订)

**判定：NO-GO (保持 — 但阻断清单从 3 项增至 5 项)**

| 原 v3 阻断 | v5 修订 | 理由 |
|:---------|:-:|:-----|
| P0-1 clearance super-admin bypass | ✅ **已修** (v3→v5 验证 403 → 500/400 — 证明 clearance 层放行) | 见 §8.2 |
| P0-2 statistics 4 字段无 setter | 仍 | 02 T2 `{"stats":{}}` 全 0 |
| P0-3 QuotaFilter body 不可重读 | 仍 (**症状演进** → CCE) | 见 §8.3 P0-3' |
| P0-4 ComplianceRule long vs TIMESTAMP | 仍 | 见 §8.3 P0-4 |
| — (新) | **P0-5: `ecos_domain` 缺 `tenant_id` 列** | 03/04 域 500 全部卡这个 |
| — (新) | **P0-6: `/api/v1/ecos/objects/{code}` 路由不存在** | 01 T4 fail-closed 探针打错路径 → denyCount=0/4 |

**Wave-4.2 准入修复最小集 (5 项)**:
1. **P0-5**: 给 `ecos_domain` 加 `tenant_id` 列 (schema 只加不删铁律 — 加可空列 + 默认 `'global'`)；或 `TenantAwareJdbcTemplate.TENANT_TABLES` 不注册 `ecos_domain` (domain 是平台级共享资源，跨租户可见)。**推荐后者** (domain 跨租户，不该隔离)。
2. **P0-3'**: `QuotaFilter.java:146` 改 `jdbc.query(new JdbcTemplate.SqlParameterSourceQueryBuilder()...create(atomicSql), new SqlQueryDriver(conn -> { ... }))` 或显式 `PreparedStatement` lambda；或用 `update(atomicSql, args)` + `queryForObject(readback)`。核心是不要让 `jdbc.query(String, ResultSetExtractor, Object...)` 接收 varargs 展开的 Object 列表当 RowMapper。
3. **P0-4**: `ComplianceRule` 重写 `createdAt/updatedAt` 为 `java.time.LocalDateTime` 加 `MyBatis LocalDateTimeTypeHandler` (或 PG 加 `BIGINT epoch_ms` 镜像列，但推荐前者 — schema 只加不删)。
4. **P0-6**: 修 01 mjs 的 T4 探针路径，或新增 `/api/v1/ecos/objects/*` 兜底 stub；当前期望"fail-closed 403" 但实际路由 400 不再证明 fail-closed 行为。
5. **P0-2**: `TransformPipeline.execute` 维护 `stats.inputCount = input.getRowCount()` / `outputCount = output.getRowCount()` / `filteredCount = sum(step.filtered)` / `errorCount = sum(step.errors)`。

**Wave-4.2 72h Soak 准入门槛 (v5 修订)**:
- 07 跨域 7 步串 **全绿** (S1 fix 参数格式 / S2 真拿到 domainCode (非 fallback `finance`) / S3 wave3 200 / S4 steps ≥ 1 / S5 ruleRef 命中 / S6 compliance-rules ≥ 2 / S7 decisionId 落库)
- 05 reasoningPath 5 Contract 至少 **ReasoningStep ≥ 3 + RuleRef ≥ 1**
- 02 data statistics 4 字段全 > 0
- 06 T4 compliance-rules 200 + ≥ 2 enabled
- 01 T4 fail-closed 探针要么改路径命中实际受保护端点，要么加新端点作为 sentinel

---

> **executor**: Wave-4.1 re-run 子代理 (P0-1 已修) | **v5 go/no-go**: **NO-GO** (叠加 P0-5/P0-6 两个新 P0)
> **leave ground**: 工作目录新增 5 个 .sh 辅助脚本 (start-gw.sh / start-gw2.sh / wait-gw.sh / wait-gw2.sh / check-gw.sh / grep-*.sh / probe-rootcauses.sh / run-wave4.sh / dump-results*.sh) — 留给 PMO 整理入 `ecos-tests/integration/wave4/scripts/`。Gateway 仍跑 (PID 85644) 不停；端口 8080 占用中。
