# PMO-3C: data-engine P2 方案文档（第一轮实现收尾）

> **架构铁律**: 必须遵循 [ECOS架构铁律](../../ARCHITECTURE-RULES.md)
> 来源: 肖国荣 | 日期: 2026-08-24
> 铁律: ①本轮只出方案文档，不写实现代码 ②方案必须遵守架构规则（runtime 收敛 / security 裁决 / profile 裁剪）

## §背景

P0+P1 实现完成后，本轮收尾产出 4 份方案文档，为下一轮实现打底。

## §Task

| Task | 产出文件 | 内容 | 工期 |
|------|---------|------|:--:|
| T9 | `docs/7-integration/03-data/P2-01-Pipeline-Schema规范.md` | Pipeline YAML Schema 规范：节点类型枚举（SOURCE_JDBC/CSV/REST/CDC/TRANSFORM_SQL/OUTPUT_OBJECT）+ config schema + version 语义 + dependsOn 依赖。参考 Kettle step 模型 + Foundry Pipeline Builder | 1d |
| T10 | `docs/7-integration/03-data/P2-02-CDC接入设计.md` | FlinkCdcConnector 设计（flagship 版）：注册进 runtime-access ConnectorFactory 动态类型，订阅 binlog 实时入湖，批流双轨边界。javaprojects 已有 flink-cdc 资产可参考 | 1d |
| T11 | `docs/7-integration/03-data/P2-03-安全对接清单.md` | CLS 列级/脱敏/审计 的完整对接点清单（RLS 已在 PMO-3B T8 接）：QueryController 返回前 CLS + 脱敏（email/phone/idCard），写操作异步审计。按架构规则 2.4 七条逐项列对接点 + 调用格式 | 0.5d |
| T12 | `docs/7-integration/03-data/P2-04-调度统一方案.md` | B9 调度统一方案：runtime-task 是半成品（`parseCronToDelay` cron 占位返回 60000 + `DefaultTaskExecutor` 空实现），方案=先修 runtime 层（换 Quartz CronExpression + 补 Executor 注册机制）→ 再把 DQ/Schema/Pipeline 统一走 runtime-task（删 new Thread + 各自 @Scheduled） | 1d |

## §交付

- [ ] 4 份方案文档产出，内容遵守架构规则
- [ ] T12 明确标注 runtime-task 半成品现状 + 跨模块改动边界
- [ ] 本轮（第一轮实现）闭环：P0 修复 + P1 补齐 + P2 方案，测试指令（PMO-3D）在实现验收后再出
