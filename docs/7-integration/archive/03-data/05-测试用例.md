# Phase 3 测试用例：data-engine 数据引擎联调（ETL 架构版）

> 配套 `01-需求文档.md` + `04-测试方案.md` | 2026-08-24
> 优先级：P0 = 联调门禁，P1 = 能力覆盖

---

## 一、数据接入（需求文档二、三：4 类 + 及时性）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-DS-01 | 登录 | GET /api/v1/datanet/datasource | 200 非 404，返回列表 | P0 |
| TC-DS-02 | DS-01 | POST 创建 PG 数据源（连 sys_man 库，connection_config 含 host/port/db/user/pwd） | code=0，td_datasource 落库 | P0 |
| TC-DS-03 | DS-02 | POST /api/v1/datanet/datasource/{id}/test | success=true（JDBC 连通） | P0 |
| TC-DS-04 | DS-02 | POST 创建 CSV 数据源（datasource_type=FILE）→ test | success=true（CSV 文件解析） | P1 |
| TC-DS-05 | DS-02 | POST 创建 REST 数据源（datasource_type=SERVICE）→ test | success=true（REST 连通） | P1 |
| TC-DS-06 | — | 核实 FlinkCdcConnector 设计（P2-02 文档） | 设计文档存在，标注 flagship 实跑 | P1 |
| TC-SYNC-01 | DS-02 | 触发全量抽取（JDBC 分页） | 数据入湖，行数正确 | P1 |
| TC-SYNC-02 | DS-02 | 触发增量抽取（updated_at > last_sync） | 只同步变更行 | P1 |

## 二、元数据采集 + 目录 + 分类（需求文档 P0-1/P1-1）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-META-01 | DS-02 | POST /api/v1/datanet/metadata/collect/{datasourceId} | resourcesCollected>0 | P0 |
| TC-META-02 | META-01 | GET /api/v1/datanet/metadata/resources/{datasourceId} | 返回表/字段列表（非空） | P0 |
| TC-META-03 | META-02 | GET /api/v1/datanet/metadata/preview/{resourceId}?limit=10 | 返回数据预览 rows | P1 |
| TC-CAT-01 | META-01 | GET /api/v1/datanet/catalog/search?q=关键词 | 返回目录搜索结果 | P1 |
| TC-CAT-02 | 登录 | POST /api/v1/datanet/category 创建分类 | code=0，td_data_category 落库 | P0 |
| TC-CAT-03 | CAT-02 | GET /api/v1/datanet/category/tree | 返回分类树（含子节点） | P0 |

## 三、数据血缘（需求文档 P0-1 B4）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-LINEAGE-01 | META-01 | GET /api/v1/engine/data/lineage?datasourceId=X&tableName=Y | 返回 nodes+edges（非 404，非空） | P0 |
| TC-LINEAGE-02 | — | GET /api/v1/engine/data/lineage/nodes | 返回节点列表 | P1 |
| TC-LINEAGE-03 | — | GET /api/v1/engine/data/lineage/edges | 返回边列表 | P1 |
| TC-LINEAGE-04 | Pipeline 有 SYNC 任务 | POST /api/v1/engine/data/lineage/build | JSqlParser 解析出真实 nodes/edges（非空） | P0 |

## 四、数据质量（需求文档 P0-2/P1-2）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-DQ-01 | 登录 | POST /api/v1/engine/data/quality/rules 造 NOT_NULL 规则 | code=0，ecos_quality_rule 落库 | P0 |
| TC-DQ-02 | DQ-01 | GET /api/v1/engine/data/quality/rules | 返回刚建规则 | P0 |
| TC-DQ-03 | DQ-01 + 有数据表 | POST evaluate（datasourceId+tableName） | 返回评估结果，ecos_quality_evaluation 落库 | P0 |
| TC-DQ-04 | DQ-01 | 造 UNIQUE 规则 + 造重复数据 → evaluate | 返回 failed（非硬编码 true） | P0 |
| TC-DQ-05 | DQ-01 | 造 CUSTOM 规则（expression）→ evaluate | 按表达式评估（==/!=/contains） | P1 |
| TC-DQ-06 | 2 条 enabled 规则 | 手动触发 DQ 巡检（executeDailyScan） | 巡检「2 条」（非 N² 次），evaluation 落 2 条 | P0 |
| TC-DQ-07 | DQ-03 | GET evaluation history | 返回巡检历史 | P1 |

## 五、Schema 变更检测（需求文档 P0）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-SCHEMA-01 | — | GET /api/v1/engine/data/schema/changes | 返回变更列表（可能空） | P1 |
| TC-SCHEMA-02 | — | 手动触发 detect() 一次 | schema_snapshots 有快照，schema_changes 记录新表 | P1 |
| TC-SCHEMA-03 | SCHEMA-02 | POST /api/v1/engine/data/schema/changes/{id}/acknowledge | acknowledged 置 true | P1 |

## 六、Pipeline（需求文档 P0-3/P1-3/git 版本管理）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-PIPE-01 | 登录 | POST /api/v1/engine/data/pipeline/tasks 建 2 节点任务 | code=0，ecos_pipeline_task 落库 | P0 |
| TC-PIPE-02 | PIPE-01 | POST /api/v1/engine/data/pipeline/tasks/{id}/run | 立即返回 runId（异步，非阻塞） | P0 |
| TC-PIPE-03 | PIPE-02 | GET /api/v1/engine/data/pipeline/runs/{runId} | 状态 QUEUED→RUNNING→COMPLETED 流转 | P0 |
| TC-PIPE-04 | PIPE-03 | GET /api/v1/engine/data/pipeline/runs/{runId}/steps | 步骤返回真实 rows（非 SELECT 1 占位） | P0 |
| TC-PIPE-05 | PIPE-03 | GET /api/v1/engine/data/pipeline/tasks/{id}/runs | 返回执行历史 | P1 |
| TC-PIPE-06 | PIPE-01 | POST /api/v1/engine/data/pipeline/tasks/{id}/git/commit | Pipeline YAML 提交到 git（pipelines/{id}/pipeline.yaml） | P1 |
| TC-PIPE-07 | PIPE-06 | GET /api/v1/engine/data/pipeline/git/branches | 返回分支列表 | P1 |
| TC-PIPE-08 | PIPE-06 | POST /api/v1/engine/data/pipeline/git/load | 从 git 加载 Pipeline 定义 | P1 |
| TC-PIPE-09 | — | GET /api/v1/pipeline/definitions | 返回定义列表（前端在用，保留） | P1 |

## 七、查询 + UDF + Copilot + Settings（需求文档 P1/6.8）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-QUERY-01 | DS-02 | POST /api/v1/engine/data/query/execute（sql+datasource_id） | 返回查询结果（大表 LIMIT） | P0 |
| TC-QUERY-02 | QUERY-01 | GET /api/v1/engine/data/query/history | 返回历史 | P1 |
| TC-QUERY-03 | QUERY-01 | POST /api/v1/engine/data/query/template 存模板 | 模板保存成功 | P1 |
| TC-UDF-01 | 登录 | POST /api/v1/engine/data/udf/register | 注册成功 | P1 |
| TC-UDF-02 | UDF-01 | POST /api/v1/engine/data/udf/convert | 转换成功 | P1 |
| TC-COPILOT-01 | 登录 | POST /api/v1/engine/data/copilot/chat | 返回响应 | P1 |
| TC-SETTINGS-01 | — | GET /api/v1/engine/data/settings | 返回引擎配置（L2） | P1 |
| TC-SETTINGS-02 | — | PUT /api/v1/engine/data/settings 改配置 | 配置更新，可查询 | P1 |

## 八、安全接入（需求文档 P0-4/6.4，架构规则 2.4 强制）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-RLS-01 | 两个不同 tenant 用户 | 分别 POST /api/v1/engine/data/query/execute 带 table_name=td_user 查同表 | 返回行数不同（RLS 注入 tenant_id 过滤） | P0 |
| TC-RLS-02 | RLS-01 | 停 security-engine boot（18081）→ 再查（带 table_name） | 返回 403（默认 DENY，非裸查） | P0 |
| TC-RLS-03 | RLS-01 | 不带 table_name 的查询 | 正常执行（RLS 仅对指定表生效） | P1 |

## 九、回归测试（全绿门禁）

| 编号 | 步骤 | 预期 |
|------|------|------|
| TC-REG-01 | env -i mvn install -Pstandard | BUILD SUCCESS |
| TC-REG-02 | mvn test -pl common/common-api | 铁律 5 全绿 |
| TC-REG-03 | mvn test 六引擎 ArchitectureTest + gateway ApiPathUniquenessTest | 全绿 |

---

## 统计

- P0：18 用例（门禁）
- P1：26 用例（能力覆盖）
- 回归：3 项

**版本差异说明**：本轮 standard 版验证（JDBC/CSV/REST + 批处理 + 血缘内存解析）。CDC 实时（TC-DS-06）、Neo4j 血缘落图、Doris OLAP 为 enterprise/flagship 差异项，本轮仅确认设计文档 + 不报错。
