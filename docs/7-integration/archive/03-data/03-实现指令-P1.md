# PMO-3B: data-engine P1 能力补齐 + 安全接入（第一轮实现）

> **架构铁律**: 必须遵循 [ECOS架构铁律](../../ARCHITECTURE-RULES.md)
> 来源: 肖国荣 | 日期: 2026-08-24
> 铁律: ①引擎数据访问必过 security-engine（架构规则 2.4，T8 强制）②基础设施访问走 runtime-access（架构规则 2.5）③不跨 Phase

## §背景

P0 四个 bug 修复后，补齐 4 项能力缺口（B5/B6/B7/B8）+ 1 项安全接入（T8，架构规则 2.4 强制：data-engine 数据访问必过 security-engine 裁决）。

## §禁止清单（继承架构规则 5.1 + 本指令特有）

1. 不跨 Phase 预创建文件
2. 引擎内自建权限/脱敏/审计逻辑（架构规则 2.4 禁止重复实现，一律走 security-engine REST）
3. 基础设施访问绕过 runtime-access（架构规则 2.5）
4. 新增 Bean 不加 `ecos` 前缀（架构规则 1.3）
5. 动 kb-engine / cognitive-engine

## §Task

| Task | 文件 | 操作 | curl 验收 | 工期 |
|------|------|------|----------|:--:|
| T4 | `.../service/StubCategoryService.java` + 迁移 SQL | **B6**：① 迁移 SQL 建 `td_data_category` 表（id/parent_id/name/type/sort/created_at，Schema 只加不删）；② 重写为真实 JdbcTemplate 实现（create/update/getById/getTree/getChildren/remove/getCategoryStats），删 stub no-op | `curl POST http://localhost:18082/api/v1/datanet/category` 创建成功；GET 返回树 | 1d |
| T5 | `.../service/QualityServiceImpl.java` | **B7**：`evaluateValue` 的 `case "UNIQUE"` 用 SQL `GROUP BY target HAVING COUNT(*)>1` 检测重复；`case "CUSTOM"` 支持 parameters 表达式评估 | 造 UNIQUE 规则 + 重复数据 → evaluate 返回 failed | 0.5d |
| T6 | `.../pipeline/PipelineExecutionService.java` | **B8**：executeSourceJdbc 通过 `connectorFactory.getConnector(...)` 拿外部数据源执行（不硬编码系统 JdbcTemplate，走 runtime-access）；executeTransformSql 返回真实影响行数（`jdbc.update(sql)` 非 `jdbc.update("SELECT 1")`） | Pipeline run 后步骤详情返回真实 rows | 0.5d |
| T7 | 无代码改动 | **B5**：grep 前端 + 后端，确认 `/api/v1/pipeline`（def/exec 表）是否有消费。产出结论：死代码（标记归档）or 保留 | 结论写进修复记录 | 0.5d |
| T8 | `.../controller/QueryController.java` | **安全接入（架构规则 2.4 强制）**：数据查询前调 security-engine RLS 注入行级过滤条件（`POST /api/security/rls/apply`），查询结果不裸返全列。调用格式参照 `docs/archive/7-integration/02-security/05-安全能力操作手册.md` | 不同租户/角色用户查同表 → 返回行数不同（RLS 生效），敏感列被过滤 | 1d |

> T8 说明：本指令先接 RLS（行级过滤，最核心的数据权限）。CLS 列级/脱敏/审计 的完整对接点清单在 PMO-3C（P2）里出。

## §执行

P0 验收通过后，T4→T5→T6→T7→T8 顺序执行（T4/T5/T6 互不冲突可并行，T7 纯梳理，T8 依赖 security-engine 已联调）。

## §交付

- [ ] B5/B6/B7/B8 完成 + 安全接入 RLS 完成，每项独立 commit（`I3-fix:` 前缀）+ curl 证据
- [ ] 安全接入走 security-engine REST，无引擎内自建安全逻辑
- [ ] 回归全绿
