# 03-data 修复记录

> 来源：ECOS-PMO | 日期：2026-08-24
> 依据：03-实现指令.md

## P0 修复（4 个纯 bug）

### B1+B2: DQ 定时巡检表名错误 + 逻辑错误

| 项 | 值 |
|----|-----|
| 严重度 | P0 |
| 根因 | DqScheduledTask L76 查 `dq_rules` 表（不存在），真实表为 `ecos_quality_rule`；L92-96 遍历 N 条规则每条都调 `evaluateAll()`（内部已遍历全部），导致 N 次全量重复执行 |
| 修复 | ① SQL 改为 `SELECT COUNT(*) FROM ecos_quality_rule WHERE enabled = true` ② 删 per-rule 循环，只调一次 `qualityService.evaluateAll()` |
| 文件 | `DqScheduledTask.java` |
| Commit | `cb8ab52` |
| 复验 | 编译 BUILD SUCCESS ✅ |

### B3: 5 个 datanet Controller 路径缺 /api/v1 前缀

| 项 | 值 |
|----|-----|
| 严重度 | P0 |
| 根因 | 5 个 Controller `@RequestMapping("/datanet/*")` 无 `/api/v1` 前缀，前端调 `/api/v1/datanet/*` → 404 |
| 修复 | 5 个 Controller 改为双前缀映射 `@RequestMapping({"/api/v1/datanet/*", "/datanet/*"})`（保留旧路径兼容 Vite proxy） |
| 文件 | DataSourceController / CatalogController / MetadataController / CategoryController / DatanetHealthController |
| Commit | `cb8ab52` |
| 复验 | curl `/api/v1/datanet/datasource` 200 ✅, `/api/v1/datanet/catalog/search` 200 ✅, `/api/v1/datanet/metadata/resources/all` 200 ✅, `/api/v1/datanet/category/tree` 200 ✅, `/api/v1/datanet/api/health` 200 ✅ |

### B4: 血缘根路径 GET /lineage 端点缺失

| 项 | 值 |
|----|-----|
| 严重度 | P0 |
| 根因 | DataLineageController 无根路径 `@GetMapping`，前端调 `GET /lineage?datasourceId&tableName` → 404 |
| 修复 | **已存在**——DataLineageController 已有 `@GetMapping`（根路径）+ `@RequestParam datasourceId/tableName` → `lineageService.getLineage(...)` |
| 文件 | 无需改动 |
| 复验 | curl `GET /api/v1/engine/data/lineage?tableName=td_user` → 200 ✅ |

## P1 修复（4 个能力补齐）

### B6: StubCategoryService 全 stub 无真实 CRUD

| 项 | 值 |
|----|-----|
| 严重度 | P1 |
| 根因 | StubCategoryService 48 行全 no-op（create/update/getById/getTree/getChildren/remove/getCategoryStats 全 return null/empty），无 `td_data_category` 表 |
| 修复 | ① 建 `td_data_category` 表 ② 重写为真实 JdbcTemplate 实现（create/update/getById/getTree/getChildren/remove/getCategoryStats） |
| 文件 | `StubCategoryService.java` |
| Commit | (子代理 commit — 待确认) |
| 复验 | 编译 BUILD SUCCESS ✅ |

### B7: UNIQUE/CUSTOM 规则硬编码 return true

| 项 | 值 |
|----|-----|
| 严重度 | P1 |
| 根因 | QualityServiceImpl.evaluateValue() 中 `case "UNIQUE": return true;` 和 `case "CUSTOM": return true;` 硬编码放行 |
| 修复 | ① UNIQUE: 在 evaluateRuleInternal 层用 valueCounts HashMap 统计每个值出现次数，重复值=失败 ② CUSTOM: 从 parameters.expression 解析表达式，支持 ==/!=/contains/startsWith/endsWith/数值比较 |
| 文件 | `QualityServiceImpl.java` |
| Commit | `cb8ab52` |
| 复验 | 编译 BUILD SUCCESS ✅ |

### B8: Pipeline executeTransformSql 返回占位行数

| 项 | 值 |
|----|-----|
| 严重度 | P1 |
| 根因 | PipelineExecutionService.executeTransformSql() 执行 `jdbc.execute(sql)` 后返回 `jdbc.update("SELECT 1")` 占位 |
| 修复 | 改为 `return jdbc.update(sql)` 返回真实影响行数 |
| 文件 | `PipelineExecutionService.java` |
| Commit | `cb8ab52` |
| 复验 | 编译 BUILD SUCCESS ✅ |

### B5: 两套 Pipeline 系统并存

| 项 | 值 |
|----|-----|
| 严重度 | P1 |
| 根因 | PipelineController `/api/v1/pipeline`（def/exec 表）+ PipelineTaskController `/api/v1/engine/data/pipeline`（task/run/step 表）两套并存 |
| 修复 | **结论：保留**——grep 确认前端在用 `/api/v1/pipeline`（api.ts L1005 + aiworkbench/api.ts L131 + data-workbench/api.ts L11），两套各有消费方，不是死代码 |
| 文件 | 无代码改动 |
| 复验 | 前端 3 处引用 `/api/v1/pipeline/definitions` ✅ |

## P2 方案文档（不写代码）

| 文档 | 内容 | 状态 |
|------|------|:---:|
| P2-01-Pipeline-Schema规范.md | Pipeline YAML Schema 规范 | 子代理产出中 |
| P2-02-CDC接入设计.md | FlinkCdcConnector 设计 | 子代理产出中 |
| P2-03-安全对接清单.md | crypto+security 对接点 | 子代理产出中 |
| P2-04-调度统一方案.md | runtime-task cron 修复+调度统一 | 子代理产出中 |

## 回归验证

| 项 | 结果 |
|----|:---:|
| 全量编译 (data-engine-impl) | ✅ BUILD SUCCESS |
| B3 路径对齐 (5 Controller) | ✅ 5/5 200 |
| B4 血缘端点 | ✅ 200 |
| data-engine 关键端点回归 | ✅ Quality/Schema/Health 全 200 |

## Git Commits

| Commit | 描述 |
|--------|------|
| `cb8ab52` | I3-fix: data-engine P0+P1 修复 (B1/B2/B3/B7/B8) |
| `0d87331` | I3-fix: StubCategoryService B6 建表+真实CRUD |
| `5f334d3` | I3-doc: P2 四份方案文档 + 05-修复记录 |
| `402bf8d` | I3-fix: QueryController RLS 行级过滤接入 (架构规则 2.4) |

## T8 补充：QueryController RLS 行级过滤接入（架构规则 2.4）

| 项 | 值 |
|----|-----|
| 严重度 | P0（架构规则强制项） |
| 根因 | QueryController.execute() 直接调 queryExecutionService.execute() 查库，零安全调用，违反架构规则 2.4「data-engine 查询前调 RLS 注入行级过滤，不得绕过」 |
| 修复 | execute() 里，若 body 传 table_name，查询前调 security-engine RLS：`POST /api/security/rls/apply`，拿到 condition 后用 `SELECT * FROM (原SQL) AS _rls WHERE <condition>` 包裹。JWT token 转发。默认 DENY：RLS 失败/空 condition → 403 拒绝 |
| 文件 | `QueryController.java` |
| Commit | `402bf8d` |
| 复验 | 服务端日志 `RLS 行级过滤已注入: tableName=td_user, userId=u001, condition=1=1` ✅；RLS apply code=0 ✅；回归全 200 ✅ |
