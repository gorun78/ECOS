# 05 — Wave-2B ge (D→I) 收口验收记录 (C REQ)

> **版本**: v1.0 | **日期**: 2026-09-02 | **域**: 3-data / ge (格, D→I)
> **回填需知 / Scope**: Wave-2B tag `v1.0-ge` 验收门；数据治理 Phase 1-2 已交付，本次收口把 6 步 D→I 转换 Pipeline 暴露为对外 API
> **关联需求**: [01-需求.md](./01-需求.md) (C REQ 前排依据)
> **关联收口记录**: [3-data/02-1-Wave2B-ge-D到I-收口.md](../../3-data/02-1-Wave2B-ge-D到I-收口.md)
> **QA Sub-Agent**: 全栈开发工程师
> **测试阶段**: 单元测试 (JUnit 5) + 集成 smoke (.mjs, 需 Gateway)
> **测试基线**: 本地编译产物 (`target/classes`) 已验证；Gateway 8080 集成 smoke 在 WSL 启动后执行
> **判定**: ✅ **GO** (单元测试 5/5 PASS，编译 install exit=0，铁律零违反)

---

## §1 执行概要

| 项 | 值 |
|:--|:--|
| 测试套件 | `TransformControllerTest` (本次新增, 5 case) |
| 运行环境 | WSL JDK 17.0.19+10, Maven 3.9.11 (env -i 绕 UNC 双写 bug) |
| 单测数量 / 通过 | **5 / 5** (`Tests run: 5, Failures: 0, Errors: 0, Skipped: 0`, 3.318s) |
| 集成 smoke | `TransformSmoke.mjs` (Node 24+, fetch) — .mjs 脚本已落，运行需 `~/start-gateway.sh` enterprise profile + PG |
| **P0 风险** | **0** |
| **P1 风险** | **0** |
| **判定** | ✅ **GO** (符合架构铁律 §5.1 #10 + §2.1 + §3.3) |

> **判定依据**: 6 步 D→I 转换 Pipeline (cleansing/mapping/typeConversion/validation/aggregation/calculator) 经 `TransformController` 暴露 HTTP 端点；铁律 §2.1 跨引擎只调 API (本次新端点不跨引擎)；§3.3 不新增 DB 表；§5.1 #10 不新建 Maven 模块 / 不提新 Docker 容器。三滤波器零改动 (路径已在 `/api/v1/engine/**` permitAll 下)。

---

## §2 范围与前置

### 2.1 本次 (C REQ) 范围

| 端点 | Method | 行为 |
|:--|:--:|:--|
| `/api/v1/engine/data/transform/meta` | GET | 返回 6 类步骤清单 + totalSteps |
| `/api/v1/engine/data/transform/execute` | POST | 执行 DataFrame 转换链路，返回 output/rows/statistics/errors/warnings |

### 2.2 不在本次范围 (5 端点中其余 4)

| 端点 | 域 | 责任 |
|:--|:--:|:--|
| `cognition/domain/search` | cognitive-engine | PMO-32/33 已交付 |
| `cognition/domains/{id}/infer` | cognitive-engine | PMO-33 已交付 |
| `cognition/causality/parse` | cognitive-engine | PMO-20 已交付 |
| Hologres → MinIO OSS (G1) | data-engine + runtime-access | PMO-R3 已收敛到 runtime-access |

---

## §3 单元测试 (5/5 PASS)

### 3.1 用例清单

| # | 用例 | 类.method | 入参 | 断言 | 结果 |
|:--:|:--|:--|:--|:--|:--:|
| UT-1 | meta 端点 6 步清单完整 | `metaReturnsSixSteps` | 无 | `code==0`；`totalSteps==6`；`availableSteps.size==6`；6 类 type 都在；每 step 3 字段齐 | ✅ |
| UT-2 | execute null body | `executeNullBodyReturnsBadRequest` | body=null | `code==400` | ✅ |
| UT-3 | execute input 非 Map | `executeInvalidInputTypeReturnsBadRequest` | input=String | `code==400`，message 含 `input` | ✅ |
| UT-4 | execute 未知 step type | `executeUnknownStepTypeReturnsBadRequest` | chain=[{type:"notExist"}] | `code==400`，message 含 `notExist` | ✅ |
| UT-5 | execute 合法 cleansing+trim | `executeValidCleansingChainSucceeds` | input=[[name:"  张三  ",age:30]] + cleansing trim | `code==0`，`success==true`，output.rows[0].name.trim()=='张三' | ✅ |

### 3.2 执行命令 (V3 子模块验证)

```bash
# 绕 Hermes UNC 双写 bug (架构铁律 §6)
wsl -d Ubuntu -- bash /home/guorongxiao/ECOS/ecos_backend/ecos-gen-scratch/wave2b-ge-verify2.sh
# 该脚本内部:
#   env -i HOME=/home/guorongxiao PATH=/usr/bin:.../apache-maven-3.9.11/bin \
#     JAVA_HOME=/home/guorongxiao/.local/jdk/jdk-17.0.19+10 \
#     bash _wave2b-inner.sh
# _wave2b-inner.sh 步骤:
#   V3a: mvn install -pl data-engine-impl -am -DskipTests -Dmaven.test.skip=true -q
#   V3b: 检查 .m2 JAR 含 TransformController
#   V3c: mvn test -pl data-engine-impl -Dtest=TransformControllerTest -q
```

### 3.3 surefire 报告

```
Target: engine/data-engine/data-engine-impl/target/surefire-reports/com.chinacreator.gzcm.engine.data.transform.controller.TransformControllerTest.txt
───────────────────────────────────────────────────────────────────────────────────────
Test set: com.chinacreator.gzcm.engine.data.transform.controller.TransformControllerTest
───────────────────────────────────────────────────────────────────────────────────────
Tests run: 5, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 3.318 s
```

**尾巴摘要**:
```
[install] exit=0 Took122s
[target/classes] TransformController.class ✅
[target/classes] TransformServiceImpl.class ✅
[test] exit=0
```

---

## §4 集成 smoke (.mjs) — 待执行

### 4.1 脚本

`docs/7-integration/03-data/TransformSmoke.mjs` (170 行, Node 24+, fetch, 无 Playwright)

### 4.2 执行前提

| 项 | 现状 |
|:--|:--:|
| Gateway 8080 起来 (enterprise profile, PG 可连) | ⏳ 需 `bash ~/start-gateway.sh` |
| Node 24+ + fetch | ✅ 已验证 (05-RlsCrossTenantTest.mjs 同范) |
| admin/admin123 凭据可用 | ✅ PD 已 seed |

### 4.3 断言项 (对应 §3 的 5 单测 镜像)

| # | 断言 cmdlib | 期望 |
|:--:|:--|:--|
| IT-1 | `GET /api/v1/engine/data/transform/meta` | status=200, code=0, availableSteps.length==6 |
| IT-2 | `POST /transform/execute` (cleansing+mapping) | status=200, code=0, success=true, rows[0].name=="张三" |
| IT-3 | `POST /transform/execute` (unknown type) | status=200, code=400, message 含 notExist |

### 4.4 执行命令 (Gateway 启动后)

```bash
cd /home/guorongxiao/ECOS/docs/7-integration/03-data
node TransformSmoke.mjs
# 退出码: 0 = GO, 1 = NO-GO
```

---

## §5 铁律符合性

| 铁律 | 要求 | 本次行为 | 结论 |
|:--|:--|:--|:--:|
| §0.3 不新建 Maven 模块 (基线 13) | 不新增 pom module | 无新 pom | ✅ |
| §0.4 不提 Docker 容器 | 不加 compose image | 无 compose 变更 | ✅ |
| §1.2 三滤波器逐项验证 | SecurityConfig / ClearanceInterceptor / VersionPrefixRewriteFilter | 全部零改 (路径在 `/api/v1/engine/**` permitAll + 豁免 已覆盖 line 72 / line 107) | ✅ |
| §2.1 跨引擎只调 API | 不直 import Impl | `TransformController` 仅 import 本模块 `engine.data.transform.*` | ✅ |
| §2.4 安全走 security-engine | ge 端点不写数据, 无 RLS/CLS/脱敏/ABAC 敏感数据流 | 4 个引擎数据访问都安全 (ge 的 /transform 是无状态转换，不落库) | ✅ |
| §3.3 跨引擎不操作对方表 | 不 JdbcTemplate 操作对方表 | 无 JdbcTemplate | ✅ |
| §5.1 #3 controller 不先 JdbcTemplate | Controller 不直接 JdbcTemplate | `@Autowired ITransformService`; Controller 零 JdbcTemplate 直接调用 | ✅ |
| §5.1 #4 controller 不 implements 已有 Service Bean | Adapter 不 implements 已有 Service 接口 | TransformController 是独立 `@RestController`，不 implements 任何 Service | ✅ |
| §5.4 验证四步法 | V1+V2+V3+V4 | V1 ✅ V2 ✅ V3 ✅ (install+test exit=0 5/5) V4 ⏳ (待 Gateway 起) | ✅ (V4 脚已落) |

---

## §6 风险留痕

| 项 | 等级 | 缓解 |
|:--|:--:|:--|
| CalculatorStep 用 Nashorn (Java 17 无内置) | 🟡 P3 | 本次 T4 暴露的是 API 入口，未在 smoke 中触发 calculator expression (step 4) 中的 CalculatorStep；若未来实装脚本引擎需另开 PMO (fallback 到 SpEL 是认知层统一选项) |
| .mjs 集成 smoke 未执行 | 🟢 P3 | 脚本已落 05 §4，需 `~/start-gateway.sh` 跑后补 (下一个 Wave 或 全链路联调 PMO-27 时可一并执行) |

---

## §7 工件版本 (SOURCE_PATCH)

| 工件 | 路径 | 行数 |
|:--|:--|:--:|
| TransformController.java | `ecos_backend/engine/data-engine/data-engine-impl/src/main/java/com/chinacreator/gzcm/engine/data/transform/controller/TransformController.java` | 217 |
| TransformServiceImpl.java | `.../transform/service/impl/TransformServiceImpl.java` (line 11 加 @Service) | +9 |
| TransformControllerTest.java | `.../src/test/.../transform/controller/TransformControllerTest.java` | 192 |
| TransformSmoke.mjs | `docs/7-integration/03-data/TransformSmoke.mjs` | 171 |
| 01-需求.md (本运作前排) | `docs/7-integration/03-data/01-需求.md` | ~160 |
| 02-1-Wave2B-ge-D到I-收口.md | `docs/3-data/02-1-Wave2B-ge-D到I-收口.md` | ~200 |
| 05-验收记录.md (本文档) | `docs/7-integration/03-data/05-验收记录.md` | 本文 |

**BUILD_ARTIFACT**: `~/.m2/repository/com/chinacreator/gzcm/data-engine-impl/1.0.0-SNAPSHOT/data-engine-impl-1.0.0-SNAPSHOT.jar` (mvn install V3 产出，含 `TransformController.class`)

**推断 commit subject**（按 Git 提交规范 `type(模块): 动宾结构`）:
```
feat(数据·ge): 暴露 D→I 转换 Pipeline 端点 /api/v1/engine/data/transform
```

---

<!-- ECOS_WAVE2B_3DATA_05 / 03-data / 05-验收记录 / 2026-09-02 / v1.0 -->

---
---

# §3 W2C cheng (K→C) review — 新增

> **版本**: v1.0 | **日期**: 2026-09-02 | **域**: 3-data / cheng (诚意, K→C)
> **关联 PMO**: PMO-22 (kb-engine) + PMO-24 (知识工作台) + PMO-26 (知识抽取)
> **关联收口**: [03-1-Wave2C-cheng-K到C-收口.md](../../3-data/03-1-Wave2C-cheng-K到C-收口.md)
> **判定**: ✅ **GO**

## §3.1 执行概要

| 项 | 值 |
|:--|:--|
| 测试套件 | `ReasoningPathBuilderWave2CTest` (5) + `KnowledgeExtractionServiceWave2CTest` (5) |
| 单测数量 / 通过 | **10 / 10** |
| 编译 | `mvn install -P enterprise -DskipTests -Djacoco.skip=true -o` → BUILD SUCCESS |
| **P0 风险** | **0** |
| **判定** | ✅ **GO** (铁律零违反) |

## §3.2 新增/修改文件 (SOURCE_PATCH)

| 文件 | 模块 | 操作 |
|:--|:--|:--|
| `RuleRef.java` | cognitive-engine-api | 新增 — 04 文档第 4 类 Contract |
| `ReasoningStep.java` | cognitive-engine-api | 增量 — +ruleRef/+sourceType/+stepIndex |
| `ReasoningPath.java` | cognitive-engine-api | 增量 — +ruleRefs 列表 |
| `ReasoningPathBuilder.java` | cognitive-engine-impl | 重写 — buildStepsFromCausal/buildPathFromCausal (04 §4.1) |
| `MinerUHttpParser.java` | kb-engine-impl | 新增 — MinerU HTTP 通道 (05 §三) |
| `DocumentParserService.java` | kb-engine-impl | 增量 — +MinerU 路由 (>5MB) |
| `KnowledgeExtractionService.java` | kb-engine-impl | 重写 — approve 闭环+规则去重+实体 Neo4j 写入 |
| `ExtractionController.java` | kb-engine-impl | 增量 — reject 支持 reason |
| `pb-engine-impl/pom.xml` | kb-engine-impl | 增量 — +mockito-core/spring-test |

## §3.3 铁律符合性抽检

| 铁律 | 结论 |
|:--|:--:|
| §0.3 不新建 Maven 模块 | ✅ 基线 13 保持 |
| §2.1 跨引擎只调 API，不直 import Impl | ✅ cognitive 只 import kb-engine-api 模型 |
| §3.3 cognitive 不新增 DB 表 | ✅ 推理结果纯内存，零新增表 |
| §3.3 kb 不操作 cognitive 表 | ✅ 只操作 extraction_drafts + sys_compliance_rule |
| §5.1 #6 用 `mvn install` 非 compile | ✅ |
| §5.1 #10 不提 Docker 容器 | ✅ MinerU 是外部 infra |
| 05 文档：kb 不直接调 LLM | ✅ 走 ai-engine Agent Loop API |
| 05 文档：cognitive 不操作 kb 表 | ✅ 实体链接只在 kb-engine-impl 内 |
