# PMO-3D: data-engine 测试执行（第一轮实现验证）

> **架构铁律**: 必须遵循 [ECOS架构铁律](../../ARCHITECTURE-RULES.md)
> 来源: 肖国荣 | 日期: 2026-08-24
> 铁律: ①T8 代码在工作区未 commit，先提交再测 ②curl 200 ≠ 功能正确（DQ 看落库 / Pipeline 看状态流转 / RLS 看多租户差异）③前后端一起测

## §背景

第一轮实现已完成（P0/P1/P2 + T8 RLS），本指令按 `04-测试方案.md` + `05-测试用例.md` 执行验证。用例编号见测试用例文档，逐条执行并回填 `04-测试报告.md`。

**关键**：T8 的 QueryController 改动在工作区未 commit（`git status` 显示 `M`），先 commit 再测。

## §测试环境

| 项 | 值 |
|----|----|
| Gateway | http://localhost:8080（admin/admin123） |
| data-engine boot | 18082 / security-engine boot 18081 |
| PG | localhost:5432 库 sys_man 用户 postgres |
| 前端 | http://localhost:3000（HashRouter `/#/`） |

token：登录后用 python 提取 accessToken 写 `/tmp/auth_header.txt`（避免 shell 转义）。

## §Task

### T1 — commit T8（前置）

工作区 `QueryController.java` 已实现 RLS（RestTemplate + 默认 DENY），未提交。

```bash
git add ecos_backend/engine/data-engine/data-engine-impl/src/main/java/com/chinacreator/gzcm/engine/data/controller/QueryController.java
git commit -m "I3-fix: QueryController 接入 RLS 行级过滤（架构规则2.4，默认DENY）"
```

**验收**：`git log -1` 出现该 commit。

### T2 — 数据接入 + 元数据 + 目录 + 分类（用例 TC-DS / TC-SYNC / TC-META / TC-CAT）

- TC-DS-01~03：JDBC 数据源接入 + 测试连接（P0）
- TC-DS-04~05：CSV / REST 数据源接入（P1）
- TC-DS-06：CDC 设计文档核实（P1，标注 flagship 实跑）
- TC-SYNC-01~02：全量 / 增量抽取（P1）
- TC-META-01~02：元数据采集 + resources 列表（P0）
- TC-META-03：数据预览（P1）
- TC-CAT-01：目录搜索（P1）
- TC-CAT-02~03：分类创建 + 树（P0，td_data_category 落库）

**验收**：数据源/元数据/分类 前后端 + DB 三者一致（td_datasource / td_data_category 落库）。

### T3 — 血缘 + DQ + Schema（用例 TC-LINEAGE / TC-DQ / TC-SCHEMA）

- TC-LINEAGE-01：血缘根路径（P0，nodes+edges 非空）
- TC-LINEAGE-02~04：nodes/edges/build（P0 build 要 JSqlParser 解析非空）
- TC-DQ-01~03：规则 CRUD + evaluate（P0，ecos_quality_evaluation 落库）
- TC-DQ-04：UNIQUE 重复检测（P0，返回 failed 非 true）
- TC-DQ-05：CUSTOM 表达式（P1）
- TC-DQ-06：DQ 巡检一次性全量（P0，巡检 2 条非 N² 次）
- TC-DQ-07：evaluation history（P1）
- TC-SCHEMA-01~03：Schema 检测 + acknowledge（P1）

**验收**：DQ 巡检看 `ecos_quality_evaluation` 真实落库记录数；血缘 build 看 JSqlParser 非空 nodes。

### T4 — Pipeline + git + 查询 + UDF/Copilot/Settings + RLS（用例 TC-PIPE / TC-QUERY / TC-UDF / TC-COPILOT / TC-SETTINGS / TC-RLS）

- TC-PIPE-01~05：Pipeline 建任务 + run 异步 + 状态流转 + steps 真实行数 + runs 历史（P0）
- TC-PIPE-06~08：git commit / branches / load（P1，版本管理）
- TC-PIPE-09：/api/v1/pipeline/definitions 保留（P1）
- TC-QUERY-01~03：SQL 查询 + history + template（P0/P1）
- TC-UDF-01~02：UDF 注册/转换（P1）
- TC-COPILOT-01：Copilot chat（P1）
- TC-SETTINGS-01~02：引擎配置 GET/PUT（P1，可配置性 L2）
- **TC-RLS-01~03（架构规则 2.4 强制）**：
  - RLS-01：两个不同 tenant 用户查同表 → 行数不同（P0）
  - RLS-02：停 security-engine（18081）→ 查询返回 403（默认 DENY，P0）
  - RLS-03：不带 table_name 查询正常（P1）

**验收**：Pipeline 状态 QUEUED→RUNNING→COMPLETED 流转；RLS 两个 tenant 行数不同 + 停 18081 后 403（真停，不模拟）。

### T5 — 前端 dogfood + 回归

- 前端走查：登录 → 数据工作台 → 数据源 → 元数据 → 目录 → 血缘 → DQ → Pipeline → SQL 查询，每步查控制台错误
- TC-REG-01：`env -i ... mvn install -Pstandard` BUILD SUCCESS
- TC-REG-02：`mvn test -pl common/common-api` 铁律 5 全绿
- TC-REG-03：`mvn test 六引擎 ArchitectureTest + gateway ApiPathUniquenessTest` 全绿

## §执行顺序

```
T1（commit）→ T2（接入/元数据/分类）→ T3（血缘/DQ/Schema）→ T4（Pipeline/查询/RLS）→ T5（dogfood/回归）
```

T2/T3/T4 依赖 Gateway 启动（`bash /home/guorongxiao/start-gateway.sh`），TC-RLS-02 需临时停 18081。

## §禁止清单

1. 用「端点 200」冒充功能正确
2. RLS 默认 DENY 不真停 security-engine（模拟失败不算数）
3. 只测后端不测前端
4. 测试数据不清理
5. 砍用例——P0 18 条 + P1 26 条逐条执行，跳过要标注原因

## §交付

- [ ] T8 commit 落地
- [ ] `04-测试报告.md`：按用例编号逐条 PASS/FAIL + curl 证据，P0 全过
- [ ] `05-修复记录.md` 补 T8 根因 + 修复 + commit
- [ ] 回归全绿
- [ ] 前端 dogfood 控制台 0 错误
