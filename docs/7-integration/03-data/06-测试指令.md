# PMO-3D: data-engine 测试执行（第一轮实现验证）

> **架构铁律**: 必须遵循 [ECOS架构铁律](../../ARCHITECTURE-RULES.md)
> 来源: 肖国荣 | 日期: 2026-08-24
> 铁律: ①T8 代码已在工作区未 commit，先提交再测 ②curl 200 ≠ 功能正确，要看到真实落库/状态流转/多租户差异 ③前后端一起测

## §背景

第一轮实现已完成：P0（B1/B2/B3/B4）+ P1（B5/B6/B7/B8）+ T8（RLS 安全接入）+ P2（4 份方案）。本指令执行验证，覆盖 `04-测试方案.md` + `05-测试用例.md` 的 P0/P1 用例。

**关键**：T8 的 QueryController 改动在工作区未 commit（`git status` 显示 `M`），先 commit 再测。

## §测试环境

| 项 | 值 |
|----|----|
| Gateway | http://localhost:8080（admin/admin123） |
| data-engine boot | 18082（localhost 免 auth） |
| security-engine boot | 18081 |
| PG | localhost:5432，库 sys_man，用户 postgres |
| 前端 | http://localhost:3000（HashRouter `/#/`） |

token 获取：`curl -s -X POST http://localhost:8080/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'` → 提取 accessToken 写 header 文件。

## §Task

### T1 — commit T8（前置）

工作区 `QueryController.java` 已实现 RLS（RestTemplate 调 `/api/security/rls/apply` + 默认 DENY），未提交。执行 `git add` + `git commit -m "I3-fix: QueryController 接入 RLS 行级过滤（架构规则2.4，默认DENY）"`。

**验收**：`git log -1` 出现该 commit。

### T2 — 后端 P0 验证（curl）

| 验证项 | 命令 | 预期 |
|--------|------|------|
| B3 路径 | `curl -s http://localhost:18082/api/v1/datanet/datasource` | 200 非 404 |
| B3 路径 | `curl -s http://localhost:18082/api/v1/datanet/metadata/resources/all` | 200 非 404 |
| B4 血缘 | `curl -s "http://localhost:18082/api/v1/engine/data/lineage?tableName=td_user"` | 返回 nodes+edges（非 404） |
| B1+B2 DQ | 造 2 条 enabled 规则 → 触发 `executeDailyScan()` → 查 `ecos_quality_evaluation` | 巡检日志「2 条」+ evaluation 落库 2 条（非 N² 次） |

**验收**：4 项全 PASS，B1+B2 要看 `ecos_quality_evaluation` 表实际落库记录数。

### T3 — 后端 P1 验证（curl）

| 验证项 | 命令 | 预期 |
|--------|------|------|
| B6 分类 | `curl -X POST http://localhost:18082/api/v1/datanet/category -H "Content-Type: application/json" -d '{"name":"测试分类","parent_id":null,"type":"BUSINESS"}'` | code=0 创建成功；GET 树可见 |
| B7 UNIQUE | 造 UNIQUE 规则 + 造重复数据 → evaluate | 返回 failed（非 true 放行） |
| B8 Pipeline | 建 2 节点 Pipeline → run → 查步骤详情 | 步骤返回真实 rows（非 SELECT 1 占位） |
| B5 双系统 | `curl -s http://localhost:18082/api/v1/pipeline/definitions` | 返回定义列表（前端在用，保留） |

**验收**：4 项全 PASS，B7 要看到重复数据触发 failed，B8 要看到真实影响行数。

### T4 — T8 RLS 验证（curl，架构规则 2.4）

| 验证项 | 命令 | 预期 |
|--------|------|------|
| 多租户隔离 | 两个不同 tenant 用户，`POST /api/v1/engine/data/query/execute` 带 `table_name: td_user` 查同表 | 返回行数不同（RLS 注入 tenant_id 过滤生效） |
| 默认 DENY | 停 security-engine boot（18081）→ 再查（带 table_name） | 返回 403（非裸查） |

**验收**：① 多租户行数不同 ② RLS 不可用 → 403，两证据都要 curl 截图/输出。

### T5 — 前端 dogfood + 回归

| 项 | 方法 | 预期 |
|----|------|------|
| 前端走查 | dogfood（browser_exec）：登录 → 数据工作台 → 数据源 → 元数据 → 血缘 → DQ → Pipeline | 控制台 0 错误，前后端数据一致 |
| 编译回归 | `env -i ... mvn install -Pstandard` | BUILD SUCCESS |
| 铁律5 | `mvn test -pl common/common-api` | 全绿 |
| 六引擎 | `mvn test 六引擎 ArchitectureTest + gateway ApiPathUniquenessTest` | 全绿 |

## §执行顺序

```
T1（commit T8）→ T2（P0 curl）→ T3（P1 curl）→ T4（RLS curl）→ T5（dogfood + 回归）
```

T2/T3/T4 依赖 Gateway 启动（`bash /home/guorongxiao/start-gateway.sh`），T4 的默认 DENY 需临时停 18081。

## §禁止清单

1. 用「端点 200」冒充功能正确——DQ 看落库、Pipeline 看状态流转、RLS 看多租户差异
2. T4 默认 DENY 测试不真停 security-engine（模拟失败不算数）
3. 只测后端不测前端（架构规则：前后端一起）
4. 测试数据不留清理（测完删测试数据源/规则/Pipeline）

## §交付

- [ ] T8 commit 落地
- [ ] `04-测试报告.md`：P0 12 用例 + P1 17 用例 + T8 RLS 2 用例，全 PASS/FAIL + curl 证据
- [ ] `05-修复记录.md` 补 T8 的根因+修复+commit
- [ ] 回归全绿
- [ ] 前端 dogfood 0 控制台错误
