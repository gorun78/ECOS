# PMO-3G: data-workbench 前端完善 + 架构归位 测试执行

> **架构铁律**: 必须遵循 [ECOS架构铁律](../../ARCHITECTURE-RULES.md)
> 来源: 肖国荣 | 日期: 2026-08-25
> 铁律: ①curl 200 ≠ 功能正确（建数据源看 td_datasource 落库 / 建任务看 PipelineTask 落库 / 前端刷新 +1）②前后端一起测 ③发现即修，每 bug 一 commit（`I3-fix:` 前缀）④前端禁硬编码中文+颜色（架构规则 4.1/4.3）

## §背景

PMO-3E（架构归位）+ PMO-3F（前端完善）已提交。肖总验证发现：**前端"读"真实了，"写"还是假的**——DataWorkbenchLayout 的 createConnection/createSync/createHealth/testConnection/triggerSync/runCheck 仍是纯前端本地 push 假对象，硬编码假 ID（`postgres_prod_db`/`crew_schedules_sftp`/`sync_sap_costs`/`ds_flights_clean`），api.ts 无创建数据源/同步任务的 POST 函数。这违背 PMO-3F 铁律①"删除假功能/mock 冒充真实"。

本指令：先补齐写入真实化（T0 前置），再对 3E 后端 + 3F 前端做回归验证。

## §已知缺口（验证结论，务必在 T0 修掉）

1. **写入路径 mock（P0 核心）**：`src/pages/DataWorkbenchLayout.tsx`
   - L79-81 硬编码 `selConnId='postgres_prod_db'` / `selTaskId='sync_flights_schedule'` / `selCheckId='check_flights_freshness'`
   - L140-147 `createConnection` 本地 `setConnections([...])` push 假对象，不调后端
   - L149-163 `triggerSync` 本地 `setTimeout` 模拟状态流转
   - L165-170 `createSync` / L172-177 `createHealth` 同样本地 push
   - `api.ts` 缺 `createDataSource` / `createSyncTask` / `createHealthCheck` POST 函数
2. **PipelineBuilderTab 5 处硬编码中文**：`tabs/PipelineBuilderTab.tsx`（已更新/已创建/保存失败/执行成功/已触发/执行失败）
3. **PipelineExecutionMonitor 主文件 403 行**（>300 目标，架构规则 4.6）
4. **全仓 tsc 299 错误**：全部在 `components/aiworkbench/`（knowledge/guardrails/agent-studio），历史遗留（最近提交 2026-08-07），**非本轮引入**，记录缺口不阻塞。

## §测试环境

| 项 | 值 |
|----|----|
| Gateway | http://localhost:8080（admin/admin123） |
| 前端 | http://localhost:3000（HashRouter `/#/data-workbench`） |
| PG | localhost:5432 库 sys_man 用户 postgres |

token（先登录再测，避免 shell 转义）：
```bash
TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | python3 -c "import sys,json; d=json.load(sys.stdin).get('data',{}); print(d.get('accessToken') or d.get('token') or '')")
echo "Authorization: Bearer $TOKEN" > /tmp/auth_header.txt
```

## §Task

### T0 — 写入路径真实化（P0 前置修复，已知 mock 缺口）

**文件**：`src/pages/DataWorkbenchLayout.tsx` + `src/pages/data-workbench/api.ts`

**操作**：
1. `api.ts` 新增：`createDataSource(payload)`、`testDataSource(id)`、`createSyncTask(payload)`、`createHealthCheck(payload)`（POST 到 data-engine 真实端点，字段与 `mapDsToConn`/`mapSyncTask` 对齐）。
2. `DataWorkbenchLayout.tsx` 的 `createConnection`/`createSync`/`createHealth`/`testConnection`/`triggerSync`/`runCheck` 改为调上述 api 函数，删除本地 push + `setTimeout` 模拟。
3. 删除 L79-81 硬编码假 ID，初始 `selConnId`/`selTaskId`/`selCheckId` 设为空字符串（未选中态）。
4. `PipelineBuilderTab.tsx` 5 处中文 → `t()`，zh/en 补 key。
5. `PipelineExecutionMonitor.tsx` 继续拆（主文件 ≤300 行，逻辑不变）。

**验收**：
- 前端建数据源 → `curl http://localhost:8080/datanet/datasource` 列表 +1（td_datasource 落库）
- `grep -rn "postgres_prod_db\|crew_schedules_sftp\|sync_sap_costs\|ds_flights_clean" src/pages` 无残留
- `PipelineBuilderTab.tsx` 0 硬编码中文
- `wc -l PipelineExecutionMonitor.tsx` ≤ 300

### T1 — 后端架构归位 curl 验证（PMO-3E）

```bash
# DqController 迁回 data-engine，buszhi 无残留
curl -s -H @/tmp/auth_header.txt http://localhost:8080/api/v1/ecos/dq/rules | python3 -m json.tool
grep -rln "class DqController" /home/guorongxiao/ECOS/ecos_backend  # 只应在 data-engine

# integration 真实化（connections 来自 td_datasource，非 doris/postgresql 假数据）
curl -s -H @/tmp/auth_header.txt http://localhost:8080/api/integration/metadata | python3 -m json.tool

# lineage 真实化
curl -s -H @/tmp/auth_header.txt http://localhost:8080/api/lineage/impact | python3 -m json.tool

# CeosCompatController 已拆删（仅注释残留）
grep -rn "CeosCompatController" --include='*.java' /home/guorongxiao/ECOS/ecos_backend | grep -v "替换 CeosCompatController\|原 CeosCompatController"
```

**验收**：三个端点 200 且返回真实表数据；`CeosCompatController` 无类文件残留（仅注释）。

### T2 — 前端 8 Tab dogfood 走查（PMO-3F）

用 browser_exec 走完整用户流：登录 → `/#/data-workbench` → 逐 Tab 点开，每步查控制台错误 + 截图取证。

| 步骤 | 预期 |
|------|------|
| 侧边栏 | 9 个真实 Tab（guide/connections/syncs/pipelines/health/lineage/pipeline-builder/sql-query/engine-config），**无** code-repositories/code-workbooks/contour |
| connections | 显示 td_datasource 真实数据（空库=空列表+引导建数据源，非 doris/postgresql 假数据） |
| pipeline-builder | 点开是真实 PipelineFlowEditor（可拖拽画布） |
| 中英文切换 | 8 Tab 无硬编码中文，不出现原始 key（`dw.txt.419d9f` 等） |
| 主题切换 | 4 主题（slate-light/deep-space/cyber-terminal/royal-purple）正常 |

**验收**：控制台 0 错误；截图留存。

### T3 — 前后端数据一致性（建 → 落库 → 刷新 +1）

1. 前端建 1 个数据源 → `psql -c "SELECT count(*) FROM td_datasource"` +1 → 前端刷新列表 +1
2. 前端建 1 个同步任务 → PipelineTask 表 +1 → 前端刷新 +1
3. 前端"测试连接" → 后端 testConnection 端点真实调用（非本地 setTimeout 假日志）

**验收**：前端显示 / 后端返回 / DB 实际 三者一致。任一不一致 = bug。

### T4 — 编译 + 回归 + 缺口清单

```bash
cd /home/guorongxiao/ECOS/ecos_frontend
npm run lint 2>&1 | grep -c "data-workbench"   # 必须 0
env -i HOME=/home/guorongxiao PATH=/usr/bin:/usr/local/bin:/home/guorongxiao/.local/bin:/home/guorongxiao/.local/apache-maven-3.9.11/bin JAVA_HOME=/home/guorongxiao/.local/jdk/jdk-17.0.19+10 bash -c 'cd /home/guorongxiao/ECOS/ecos_backend && mvn test -pl common/common-api'
```

**验收**：data-workbench 0 TS 错误；common-api 铁律 5 全绿。`components/aiworkbench/` 的 299 历史错误记入功能缺口清单（Phase 7 ai-engine 联调处理），本轮不修、不阻塞。

## §执行顺序

```
T0（写入真实化，P0）→ T1（后端 curl）→ T2（前端 dogfood）→ T3（一致性）→ T4（编译回归）
```

T0 是前置，不修完 T3 的"建数据源落库"必 FAIL。T1/T2/T3 依赖 Gateway + 前端启动。

## §禁止清单

1. 用「端点 200」冒充功能正确
2. 保留本地 mock / `setTimeout` 模拟状态（写路径必须真实 POST）
3. 只测后端不测前端（或反之）
4. 测试数据不清理
5. 硬编码中文/颜色（修复时一并清）
6. 跨 Phase 预创建文件

## §交付

- [ ] T0 写入真实化 commit（`I3-fix:` 前缀）+ curl 证据
- [ ] `04-测试报告.md` 回填：T1-T4 逐条 PASS/FAIL + curl/截图证据，P0 全过
- [ ] `05-修复记录.md` 补本轮 bug 根因 + 修复 + commit hash + 复验
- [ ] data-workbench 0 TS 错误 + common-api 全绿
- [ ] 缺口清单（aiworkbench 299 + 本轮新发现）更新到总规划「疑似 stub 清单」
