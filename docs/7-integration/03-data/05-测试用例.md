# Phase 3 测试用例：data-engine 数据引擎联调

> 配套 `01-需求文档.md` + `02-测试方案.md` | 2026-08-24
> 优先级：P0 = 联调门禁，P1 = 能力覆盖

---

## 一、数据源接入（B3 路径验证）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-DS-01 | 登录 | GET /api/v1/datanet/datasource | 非 404，返回数据源列表（或空数组） | P0 |
| TC-DS-02 | 登录 | POST /api/v1/datanet/datasource 造 PG 数据源（连 sys_man 库） | code=0，td_datasource 表落库 | P0 |
| TC-DS-03 | DS-02 | POST /api/v1/datanet/datasource/{id}/test | 返回 success=true | P0 |
| TC-DS-04 | DS-02 | DELETE /api/v1/datanet/datasource/{id} | 删除成功，列表不再见 | P1 |

## 二、元数据采集 + 目录（B3 路径验证）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-META-01 | DS-02 | POST /api/v1/datanet/metadata/collect/{datasourceId} | 返回 resourcesCollected>0 | P0 |
| TC-META-02 | META-01 | GET /api/v1/datanet/metadata/resources/{datasourceId} | 返回表/字段列表（非空） | P0 |
| TC-META-03 | META-02 | GET /api/v1/datanet/metadata/preview/{resourceId}?limit=10 | 返回数据预览 rows | P1 |
| TC-CAT-01 | META-01 | GET /api/v1/datanet/catalog/search | 返回目录搜索结果 | P1 |

## 三、数据血缘（B4 端点缺失验证）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-LINEAGE-01 | META-01 | GET /api/v1/engine/data/lineage?datasourceId=X&tableName=Y | 非 404（B4 修复后），返回 nodes+edges | P0 |
| TC-LINEAGE-02 | — | GET /api/v1/engine/data/lineage/nodes | 返回血缘节点列表 | P1 |
| TC-LINEAGE-03 | — | GET /api/v1/engine/data/lineage/edges | 返回血缘边列表 | P1 |
| TC-LINEAGE-04 | Pipeline 有 SYNC 任务 | POST /api/v1/engine/data/lineage/build | 从 yaml SQL 解析出真实 nodes/edges（非空） | P0 |

## 四、数据质量（B1+B2 修复验证）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-DQ-01 | 登录 | POST /api/v1/engine/data/quality/rules 造 NOT_NULL 规则 | code=0，ecos_quality_rule 落库 | P0 |
| TC-DQ-02 | DQ-01 | GET /api/v1/engine/data/quality/rules | 返回刚建的规则 | P0 |
| TC-DQ-03 | DQ-01 + 有数据表 | POST evaluate（带 datasourceId+tableName） | 返回评估结果，ecos_quality_evaluation 落库 | P0 |
| TC-DQ-04 | DQ-01 | 手动触发 DQ 定时巡检（executeDailyScan） | 日志巡检 N 条（非 N² 次），evaluation 落库（B1/B2 修复后） | P0 |
| TC-DQ-05 | DQ-01 | 造 UNIQUE 规则 + 重复数据 → evaluate | 返回 failed（B7 修复后） | P1 |
| TC-DQ-06 | DQ-03 | GET evaluation history | 返回巡检历史 | P1 |

## 五、Schema 变更检测

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-SCHEMA-01 | — | GET /api/v1/engine/data/schema/changes | 返回变更列表（可能空） | P1 |
| TC-SCHEMA-02 | — | 手动触发 detect() 一次 | schema_snapshots 表有快照，schema_changes 记录新表 | P1 |
| TC-SCHEMA-03 | SCHEMA-02 | POST /api/v1/engine/data/schema/changes/{id}/acknowledge | acknowledged 置 true | P1 |

## 六、Pipeline（B5 双系统 + 异步执行）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-PIPE-01 | 登录 | POST /api/v1/engine/data/pipeline/tasks 建 2 节点任务 | code=0，ecos_pipeline_task 落库 | P0 |
| TC-PIPE-02 | PIPE-01 | POST /api/v1/engine/data/pipeline/tasks/{id}/run | 立即返回 runId，非阻塞 | P0 |
| TC-PIPE-03 | PIPE-02 | GET /api/v1/engine/data/pipeline/runs/{runId} | 状态 QUEUED→RUNNING→COMPLETED 流转 | P0 |
| TC-PIPE-04 | PIPE-03 | GET /api/v1/engine/data/pipeline/tasks/{id}/runs | 返回执行历史 | P1 |
| TC-PIPE-05 | PIPE-03 | GET /api/v1/engine/data/pipeline/runs/{runId}/steps | 返回步骤详情 | P1 |
| TC-PIPE-06 | — | 确认 /api/v1/pipeline（旧 def/exec 表）是否有前端消费 | 有→保留，无→标记死代码 | P1 |

## 七、查询 + UDF + Copilot + Settings

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-QUERY-01 | DS-02 | POST /api/v1/engine/data/query/execute | 返回查询结果（大表加 LIMIT） | P0 |
| TC-QUERY-02 | QUERY-01 | GET /api/v1/engine/data/query/history | 返回历史 | P1 |
| TC-UDF-01 | 登录 | POST /api/v1/engine/data/udf/register | 注册成功 | P1 |
| TC-UDF-02 | UDF-01 | POST /api/v1/engine/data/udf/convert | 转换成功 | P1 |
| TC-LAYER-01 | — | GET /api/v1/engine/data/layers/{layer} | 返回分层信息 | P1 |
| TC-COPILOT-01 | 登录 | POST /api/v1/engine/data/copilot/chat | 返回响应 | P1 |
| TC-SETTINGS-01 | — | GET /api/v1/engine/data/settings | 返回引擎配置 | P1 |

## 八、分类管理（B6 处置）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:--:|
| TC-CAT-02 | — | 核实 td_data_category 表是否存在 | 存在→补实现；不存在→记缺口 | P1 |

---

## 九、回归测试（全绿门禁）

| 编号 | 步骤 | 预期 |
|------|------|------|
| TC-REG-01 | mvn install -Pstandard | BUILD SUCCESS |
| TC-REG-02 | mvn test common-api ArchitectureTest | 铁律 5 全绿 |
| TC-REG-03 | mvn test 六引擎 ArchitectureTest + gateway ApiPathUniquenessTest | 全绿 |

---

## 统计

- P0：12 用例（门禁）
- P1：17 用例（能力覆盖）
- 回归：3 项
