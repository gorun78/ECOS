# ECOS 前端界面操作实现情况检查报告

> 检查人：肖国荣（AI-Native 工厂，PM 前端方向）
> 日期：2026-09-05
> 检查范围：`ecos_frontend/src/**`（30+ 路由 / 100+ 页面 / 12 个 API 文件 / 2,848 行 `api.ts`）
> 口径依据：[.trae/rules/前端开发规范.md](../../.trae/rules/前端开发规范.md) + [AGENTS.md](../../AGENTS.md) Frontend Conventions + [docs/ARCHITECTURE-RULES.md](../ARCHITECTURE-RULES.md) 4.x 节

## 0. 总览

| 维度 | 完整度 | 评价 |
|----|:----:|----|
| 路由版图 | 9/10 | 50 个路由均已注册，Suspense/懒加载到位，RequireAuth 守卫统一 |
| 401 回退 | 8/10 | `api.ts` 单点 `handleAuthExpired` 跳 `#/login`，全项目一致 |
| **403 回退** | 4/10 | **3 个页面将 403 当 401 跳登录（语义错误）**，余下无统一处理 |
| Loading 骨架 | 6/10 | 公共组件到位，约 50% 数据列表页缺失 |
| EmptyState | 4/10 | 公共组件实际使用率 < 15%，大量"空容器/空白画布" |
| **网络断开 retry** | **1/10** | **全局完全缺失**；Login 是唯一识别 "Failed to fetch" 的地方 |
| 删除 ConfirmDialog | 5/10 | 公共组件到位，但 **14 处 `window.confirm`** |
| 删除 alert/console.log | 3/10 | `TaskPanel` 5 处 alert + 6 处 console.log toast 残缺 |
| 表单 required | 4/10 | 数值字段有 min/max，文本/必填项大量缺失，无 HTML 原生属性 |
| 错误 toast 通畅 | 4/10 | `ToastProvider` 未在 main.tsx 挂载，30+ 页各写一份重复 Toast |
| 主题 useTheme | 5/10 | 约 30 个 user-facing 页面双重违规（既缺 useTheme 又缺 useLanguage） |
| **i18n** | 4/10 | **23,235 处硬编码中文** / 100+ 文件 |
| lucide-react 图标 | 9/10 | 仅 6 处真违规（4 文件 6 SVG），12 处业务图豁免 |
| **API 路径一致** | 4/10 | **26 个 P0 接口指向不存在的后端端点** |
| 统一 request 封装 | 5/10 | 6 个文件自建 `fetch` helper 走统一封装外 |
| 文件 ≤ 800 行 | 4/10 | `AgentStudioView.tsx` 1,915 行 / `useAgentStudio.ts` 1,488 行 / `OntologyWorkbenchLayout.tsx` 1,000+ |

**致命问题**（任一命中即阻断）：
1. 26 个 API 调用指向不存在的后端端点（用户操作 404）
2. `api.ts` 大量 `return { data: [], total: 0 }` mock 降级把"后端宕机"伪装成"空数据"，用户永远看不到故障
3. `main.tsx` 未挂载 `ToastProvider`，30+ 页各自手写 `<Toast>`——统一基础设施是死代码
4. `pages/knowledge/components/ExtractionReviewPanel.tsx:171` `dangerouslySetInnerHTML` 未 sanitize — **XSS 漏洞**
5. `pages/aiworkbench/index.tsx:44-47` 4 个 fetch 全部静默 `.catch(()=>{})`
6. `EngineMonitor.tsx` / `MonitoringCenter.tsx` / `GuardrailsView.tsx` 把 **403 当 401** 跳登录语义错误

---

## 1. 路由版图（main.tsx）

主路由采用 `HashRouter` + `RequireAuth` + lazy 加载，50 个路由点全部命中真实组件（无空路由）。结构分组：

| 分组 | 路由 | 组件 | 状态 |
|---|---|---|---|
| 总览 | `/`、`mission_control`、`monitor` | CognitiveOperatingSystem, MonitoringCenter | 真实实现 |
| 业务 | `biz_dashboard` 业务仪表盘 | BizDashboard | ⚠️ `fetchBizDashboard` P0 不存在端点 |
| 业务 | `project_tracker` / `contract_manager` / `ops_dashboard` / `ops_apps` / `marketplace` | ProjectTracker/ContractManager/OperationsDashboard/OperationalApps/MarketplaceBrowser | 真实实现，缺 EmptyState 与 Loading 骨架 |
| 数据 | `objects` / `data-workbench` | ObjectExplorer / DataWorkbenchLayout | 后者有 5 个 Tab 子组件均实现 |
| I 信息层 | `ontology_workbench` / `business-workbench` | 双工作台 | 真实实现，但 Ontology 子组件大量硬编码中文（P0）|
| Legacy 重定向 | `domains` / `domain_designer` / `ontology` / `ontology_designer` / `glossary` / `agent_studio` | Navigate 到 `ontology_workbench` 或 `ai-workbench` | ✅ |
| K 知识 | `workflow_designer` / `project_workbench` / `world_model` / `knowledge_view` | 4 页 | 真实实现 |
| W 智能 | `ai-workbench` / `agent_mesh` / `agent-builder/:agentId?` / `agent-test/:agentId` / `case_library` / `alerts` | 6 组件 | 真实实现 |
| 安全 | `security-center` / `guardrails` | SecurityCenter / GuardrailsView | 真实实现 |
| 系统 | `iam` / `kanban` / `telemetry` / `tokens` / `tenants` | 5 页 | 真实实现 |
| 引擎 | `engine-security` / `engine-data` / `engine-ontology` / `engine-cognitive` / `engine-knowledge` / `engine-ai` / `engine-tasks` | 固定模板 + CognitiveEngineView 自定义 | 真实实现 |
| 兜底 | `*` | WorldModelViewer | 合理 |

**发现**：90%+ 路由都命中真实组件。其中 `agent-builder` 与 `agent-test` 两个路径挂载子路由支持员工（`?:`），不存在 stub。

---

## 2. API 路径 vs 后端现状（统一 request 封装合规）

### 2.1 P0 — 前端 26 个调用指向后端不存在端点

| # | 文件:行号 | 函数 | 真实调用 URL | 后端实际情况 |
|:-:|----------|------|------------|-------------|
| 1 | `src/api.ts:2798` | `forceLogoutUser` | `POST /api/v1/system/users/{id}/force-logout` | sysman 无此端点 |
| 2 | `src/api.ts:2803` | `resetPasswordGenerate` | `POST /api/v1/system/users/{id}/reset-password` | sysman 仅有 `PUT /{id}/password` |
| 3 | `src/api.ts:2808` | `batchCreateUsers` | `POST /api/v1/system/users/batch` | sysman 无 `/batch` |
| 4 | `src/api.ts:2340` | `fetchBizDashboard` | `GET /api/v1/ecos/biz/dashboard` | 全仓无 `/biz/dashboard` 任何 Controller |
| 5 | `src/api.ts:2356` | `callDiagnosticAgent` | `POST /api/v1/agent/call` | `DiagnosticAgentController` 无 `/call`，仅 `/chat`、`/tools/execute` 等 |
| 6 | `src/api.ts:1636` | `globalSearch` | `GET /api/v1/portal/search?q=&type=` | 后端无 `Portal*Controller`（filter 已留 rewrite，无实现） |
| 7 | `src/api.ts:1330` | `paretoOptimize` | `POST /api/v1/pareto/optimize` | 无 `/api/v1/pareto` 任何端点 |
| 8 | `src/api.ts:1338` | `fetchParetoProblems` | `GET /api/v1/pareto/problems` | 同上 |
| 9 | `src/api.ts:1346` | `fetchParetoResult` | `GET /api/v1/pareto/result/{problemId}` | 同上 |
| 10 | `src/api.ts:1352` | `paretoFromScenario` | `POST /api/v1/pareto/from-scenario` | 同上 |
| 11 | `src/api.ts:2611` | `resetSysConfig` | `PUT /api/v1/sysconfig/{key}/reset` | 后端 `SysConfigController` 无 `/{key}/reset`，且无 `/v1/sysconfig/` 前缀 |
| 12 | `src/services/agentConfig.ts:199` | `fetchKnowledgeBases` | `GET /api/v1/knowledge-bases` | kb-engine 用 `/api/v1/knowledge/...` |
| 13 | `src/services/cognitiveEngineApi.ts:115` | `fetchBlueprint` | `GET /api/v1/cognitive/blueprint` | cognitive-engine 无 `/blueprint` |
| 14 | `src/services/cognitiveEngineApi.ts:133` | `createPlan` | `POST /api/v1/cognitive/plan` | 无此端点 |
| 15 | `src/services/cognitiveEngineApi.ts:140` | `fetchPlan` | `GET /api/v1/cognitive/plan/{id}` | 同上 |
| 16 | `src/services/cognitiveEngineApi.ts:126` | `runOptimize` | `POST /api/v1/cognitive/optimize` | 同上 |
| 17 | `src/services/ontologyApi.ts:855` | `previewEntities` | `POST /api/v1/ecos/domains/{domainCode}/auto-discover/preview` | 后端仅有 `POST /domains/{domainCode}/auto-discover`（**无 /preview**） |
| 18 | `src/services/ontologyApi.ts:889` | `fetchOntologySources` | `GET /api/v1/ecos/ontology/sources` | 后端无此端点 |
| 19 | `src/services/ontologyApi.ts:302` | `fetchWorkflowDefinitions` | `GET /api/v1/engine/ontology/workflow/definitions?pageSize=` | 后端 `OntologyEngineStatusController` 仅 5 端点（health/config/status/start/stop） |
| 20 | `src/services/ontologyApi.ts:768` | `fetchVersions` | `GET /api/v1/ontology/versions` | 后端是 `/api/v1/ecos/versions` 与 `/api/v1/ecos/ontologies/{id}/versions` |
| 21 | `src/services/ontologyApi.ts:813` | `fetchVersionDiff` | `GET /api/v1/ontology/versions/diff?v1=&v2=` | 同上非此路径 |
| 22 | `src/pages/aiworkbench/api.ts:377` | `fetchAgentMetrics` | `GET /api/v1/agent-metrics/{agentId}` | 后端是 `/api/v1/aip/agent-metrics/{agentId}`（`agent` 与 `aip` 不一致） |
| 23 | `src/pages/aiworkbench/api.ts:398` | `fetchAgentErrors` | `GET /api/v1/agent-metrics/{agentId}/errors` | 同上 |
| 24 | `src/pages/sql-query-console/api.ts:177` | `fetchTemplates` | `GET /api/v1/engine/data/query/template` | 后端 `QueryController` 是 `/templates`（**复数**） |
| 25 | `src/pages/sql-query-console/api.ts:192` | `deleteTemplate` | `DELETE /api/v1/engine/data/query/template/{id}` | 后端 `DELETE /templates/{id}`（**复数**） |
| 26 | `src/pages/data-workbench/api.ts:604` | `saveMetadataStrategy` | `PUT /api/v1/datanet/metadata/strategy/{id}` | 后端 `MetadataController` 无 `/strategy/{id}` 子路径 |

### 2.2 未走统一 request 封装的裸 fetch 文件（6 个）— P1

- `src/services/glossary.ts` — 自建 `request<T>` 函数
- `src/services/dict.ts` — 自建 `request<T>` 函数（且 `code !== 0` 逻辑反了）
- `src/services/glossaryClient.ts` — 直接 fetch
- `src/services/agentConfig.ts` — 自建 `authHeaders()` 与相对路径 `/v1/agents`
- `src/pages/data-workbench/api.ts` — 自建 `get/post/put/del` 4 个 helper 裸 fetch
- `src/pages/sql-query-console/api.ts` — 自建 `get/post/del` 裸 fetch

### 2.3 异常处理缺失（P2 约 70 项）

`src/api.ts` 中 70+ 个写操作 POST/PUT/DELETE **没有明确 try/catch 兜底**；`services/ontologyApi.ts` 全部 60+ 函数无 try/catch；`services/data-workbench/api.ts` 全部函数无 try/catch；写操作 `createObject/updateObject/...`（`src/api.ts:811-999`）+ `publishMarketAsset`（`1993-2054`）+ `keep fetchAgentMetrics` 等。

### 2.4 分页参数未校验（P2）

- `fetchAuditLogs` `page` 无下限（传 0 会发 `page=0`）
- `fetchLineageImpact` `depth` 无上限（可 OOM）
- `fetchSchemaTree` 无 size 上限
- 排序参数 `sort`/`orderBy` 几乎无白名单校验（XSS/SQL 风险）

---

## 3. 规范违规（主题 / i18n / 图标 / 文件长度）

### 3.1 硬编码中文（最重灾区，总计 23,235 处）

按文件量级 Top 10：

| 文件 | 数量 | 严重度 |
|---|:-:|:-:|
| `src/components/pipeline/InteractiveStepGuide.tsx` | 2,005 | P0 |
| `src/data/ontologyMockData.ts` | 1,675 | P2（mock） |
| `src/pages/project-workbench/data.ts` | 1,634 | P2（mock） |
| `src/pages/aiworkbench/mockData.ts` | 1,038 | P2（mock） |
| `src/pages/aiworkbench/useAgentStudio.ts` | 1,183 | P0 |
| `src/stores/useWorkbenchStore.ts` | 991 | P1 |
| `src/services/dataCatalogClient.ts` | 291 | P1 |
| `src/services/ontologyApi.ts` | 492 | P1 |
| `src/pages/aiworkbench/AgentStudioView.tsx` | 1,913 | P0 |
| `src/components/aiworkbench/knowledge/ClosedLoopTab.tsx` | 698 | P0 |

**硬性违规 user-facing 关键页**：
- `src/pages/Login.tsx`：7 处中文未走 `t()`（用户名/密码/ApmsToken/登录中/登录失败/请检查网络/请输入）— **登录前公共服务色，双违规**
- `src/pages/AIPKnowledgeView.tsx`：**整个 RAG 页未使用 useLanguage，56 处中文全硬编码**
- `src/pages/SystemConfigManager.tsx:347-380`：`isZh ? '参数消耗审计' : '...'` **反 i18n 反模式**
- `src/components/TaskPanel.tsx`：500+ 行 任务操作文案全硬编码
- `src/components/common/DataTable.tsx:165,189`：`N 条记录` / `第 N/M 页` **硬编码中文**（公共组件违规！）

### 3.2 硬编码 Tailwind 颜色（≥ 190 处）

按文件 Top 10：

| 文件 | 命中数 | 严重度 |
|---|:-:|:-:|
| `src/pages/Login.tsx` | 20+ | P0 |
| `src/pages/AIPKnowledgeView.tsx` | 7+ | P0（无任何 useTheme） |
| `src/pages/aiworkbench/{ModelCatalogView, ChatbotStudioView, useAgentStudio}.tsx` | 各 ≥10 | P0 |
| `src/pages/OntologyWorkbenchLayout.tsx` | 大量 | P0 |
| `src/components/ontology-workbench/**` 全集 | 大量 | P0 |
| `src/components/aiworkbench/{agent-studio, knowledge, chatbot, guardrails}/**` 全集 | 大量 | P0 |
| `src/pages/project-workbench/tabs/*` 4 个 Tab | 大量 | P0 |
| `src/components/Sidebar.tsx` | 6 | P1 |
| `src/components/Topbar.tsx` | 9 | P1 |
| `src/App.tsx:239,282` | 4 | P1 |
| `src/components/aiworkbench/log/*.tsx` 6 节点 | 各 2-3 | P1（节点固定 `bg-white`） |

**典型模式**：`bg-white` + `border-slate-200/100` + `text-slate-800/600/700` 的默認调色板在 ~60+ user-facing 文件中存在；这些应改用 `styles.cardBg / styles.cardBorder / styles.cardText`。

### 3.3 自定义 SVG（16 处，4 真违规 + 12 业务图豁免）

| 真违规 | 行号 | 应替换 |
|---|:-:|---|
| `src/pages/OntologyWorkbenchLayout.tsx` | 346 | `lucide-react Download` |
| `src/pages/Login.tsx` | 96,179,214 | `CheckCircle2` / `Loader2` / `ChevronDown` |
| `src/pages/SystemConfigManager.tsx` | 351 | `X` |
| `src/pages/data-workbench/tabs/HealthTab.tsx` | 70 | `Plus`（同文件其它处已用 LucideIcon，矛盾！） |

业务图豁免 12 处（力导向 / Gauge / 折线 / 环形图）— 合理。

### 3.4 文件 > 800 行（前端铁律 4.6）

| 文件 | 行数 | 严重度 |
|---|:-:|:-:|
| `src/pages/aiworkbench/AgentStudioView.tsx` | **1,915** | P0 |
| `src/pages/aiworkbench/useAgentStudio.ts` | **1,488** | P0 |
| `src/pages/OntologyWorkbenchLayout.tsx` | ~1,000+ | P0 |
| `src/components/pipeline/InteractiveStepGuide.tsx` | ~2,085 | P0 |
| `src/components/aiworkbench/knowledge/ClosedLoopTab.tsx` | 698 | P1 |
| `src/pages/aiworkbench/ModelCatalogView.tsx` | 678 | P1 |

> 该 Top 5 应触发 PMO-C1（前端大文件拆分）工作流，参考 [PMO-C1](../6-techdebt/PMO-C1-前端大文件拆分.md)。

### 3.5 图标混用与 useTheme/useLanguage 缺失

- **25 个文件双重缺失**（既缺 useTheme 又缺 useLanguage）：全部 P0，包括 Login / AIPKnowledgeView / SystemConfigManager / TaskPanel / ontology-workbench 子组件 / aiworkbench 子组件 / project-workbench 4 Tab
- **30+ 文件单缺 useLanguage**（结构色已切 styles. 但文案硬编码）

### 3.6 alert() / console.log toast 残缺（实操 P0）

| 文件 | 行号 | 问题 | 应替换 |
|---|:-:|---|---|
| `src/components/TaskPanel.tsx` | 305,307,326,332 | `alert("操作失败: ...")` × 4 | `useToast` |
| `src/components/TaskPanel.tsx` | 316 | `window.confirm("确定要批量... N 个任务")` | `ConfirmDialog` |
| `src/main.tsx` | 86 | `showToast={(type,msg) => console.log(...)}` | 挂 ToastProvider |
| `src/components/Sidebar.tsx` | 316 | 同上 console.log toast 残缺 | 同上 |
| `src/pages/AIPKnowledgeView.tsx` | 25 | 同上 | 同上 |
| `src/pages/WorkshopView.tsx` | 27 | 同上 | 同上 |
| `src/pages/ScenarioManagementView.tsx` | 28 | 同上 | 同上 |
| `src/pages/DataWorkbenchLayout.tsx` | 49 | 同上 | 同上 |

### 3.7 同一节点 condition && list.map（React 中 v-if/v-for 替代反模式）

可疑清单（需二次精扫）：`CommandPalette.tsx` 命令列表 / copilot 多条 / `MarketplaceCardGrid.tsx` / `UserList.tsx` / `sql-query-console/HistoryPanel.tsx` / `knowledge/tabs/**` / `worldmodel/tabs/**` / `project-workbench/tabs/**` / `aiworkbench/**` 多 Tab。

### 3.8 **XSS 漏洞**（P0 必改）

`src/pages/knowledge/components/ExtractionReviewPanel.tsx:171`
```tsx
dangerouslySetInnerHTML={{ __html: highlightedSource }}
```
用户可控源文本直接拼入 innerHTML；应使用 `DOMPurify.sanitize()`。

---

## 4. 空状态 / 加载态 / 错误恢复 / 表单校验 / 网络重试

### 4.1 无错误处理的页面（P0）

#### `src/pages/aiworkbench/index.tsx:44-47`
```ts
fetchAIPAgentsFromMesh().then(setAgents).catch(() => {});
fetchGuardrailPolicies().then(setGuardrails).catch(() => {});
fetchPipelineDefinitions().then(setPipelines).catch(() => {});
fetchAgentModels().then(setModels).catch(() => {});
```
**全部静默吞错**，无 loading、无 error、无 empty、无 retry。后端 5xx 时用户看到的是**空 tabs + 空白子视图**，完全无停止点。

#### `src/pages/AgentMesh.tsx:66-68`
```ts
catch (e) { /* Silently ignore polling errors */ }
```
`agents`/`missions` 主列表无 loading 骨架；`missions.length===0` **无法区分**"加载失败"与"真没数据"。

#### `src/main.tsx`
**未挂载 `ToastProvider`**——`common/Toast.tsx` 的 `ToastProvider` 是死代码，30+ 页各自手写 `<Toast>`。

### 4.2 空状态缺失的 Top 高风险数据列表（P1）

`AgentMesh / aiworkbench/index / TaskCenter / WorkflowDesigner / GlossaryManager / KanbanBoard / BizDashboard / KnowledgeGraphHome / KnowledgeGraphPage / OperationalApps / SecurityAudit / AbacPolicyManager / ContractManager / ProjectTracker / DataLineage / Marketplace / TenantManager / AIPKnowledgeView / CryptoAuditPanel / GuardrailsView / CaseLibraryView / GitPanel / security-center/tabs/*`（共 ~20 个）— 均为空容器/空白画布，无 `<EmptyState>`。

### 4.3 **API 把"请求失败"伪装成"空列表"**（致命反模式 P0）

`src/api.ts` 行 144-188、294-322、1254-1255、728-770、2318-2325：
```ts
.catch(e => { console.warn('...'); return { data: [], total: 0 }; })
```
配合 `fetchDatasets` 直接 `return MOCK_DATA_ASSETS`（行 150）— **真实数据 vs 兜底数据用户无法区分**，不知后端是否宕机。

### 4.4 401 / 403 权限回退

| 路径 | 行号 | 问题 | 严重度 |
|---|:-:|---|:-:|
| `src/api.ts:34-51` `handleAuthExpired` | — | 只处理 401；403 当普通 HTTP 错误 throw 出去 | P1 |
| `src/pages/EngineMonitor.tsx:71-73` | — | 同时把 **401 和 403** 都 `removeItem('token')` 并跳 `#/login` | P1 |
| `src/pages/MonitoringCenter.tsx:51-53` | — | 同上 | P1 |
| `src/pages/GuardrailsView.tsx:114-116` | — | 同上 | P1 |
| `src/components/RequireAuth.tsx` | — | 只判 `localStorage.token`，不判 401 | P1 |

### 4.5 网络断开 retry UI（**全局完全缺失 P0**）

- 仅 `src/pages/Login.tsx:66` 识别 `message === "Failed to fetch"`
- 无 `navigator.onLine` 监听 / 无顶栏网络断开横幅 / 无 `<NetworkErrorRetry>` 组件
- `src/api.ts:42-97` 三个 fetch 封装不识别 `TypeError`（fetch reject），直接 throw
- `App.tsx:117` `setInterval(poll, 10000)` `taskStats` 轮询，API 失败时 `taskStats` 停在旧值，无"已断开"UI

### 4.6 危险操作 delete 确认（14 处 window.confirm + 6 处 ConfirmDialog 合规）

合规：`UserManagement.tsx` / `user-management/UserList.tsx` / `aiworkbench/ontology/ActionTypeTab.tsx`

window.confirm 违规（P1）：
- `src/pages/aiworkbench/LogicView.tsx:418`（硬编码中文）
- `src/pages/aiworkbench/AgentStudioView.tsx:241`（注销 AIP 智能体）
- `src/pages/aiworkbench/ChatbotStudioView.tsx:121`（已 i18n，但仍是 window.confirm）
- `src/pages/OntologyWorkbenchLayout.tsx:253`（删除+回滚）
- `src/components/ontology/Sidebar.tsx:141`（删除域）
- `src/pages/ontology/OverviewView.tsx:46`（删除域）
- `src/components/TaskPanel.tsx:317`（**批量** + **硬编码中文**）
- `src/pages/datasource/DataSourceList.tsx:93`（删数据源）
- `src/aiworkbench/agent-studio/AgentManager.tsx:123`（**回滚 = 危险操作**）
- `src/pages/PolicyEngine.tsx:107`（未保存变更确认）

### 4.7 表单校验（required/maxLength）

合规：`Login.tsx` 手动校验；`CreateAgentModal`/`VectorIndexTab`/`ConfigPanel`/`ModelCatalogView`/`CausalGraphView`/`LogicView`/`ScenarioSandbox`/`AgentBuilder`/`TenantManager`/`DetectTab`/`DataEngineConfigPanelForm` 数值字段有 min/max。

缺失：
- `pages/worldmodel/CrudDialog.tsx` — 名称/值无 `required`/`maxLength`
- `pages/AbacPolicyManager.tsx`（600+ 行）— 策略表达式无 `required`
- `pages/GlossaryManager.tsx` — `DOMAINS` **中文硬编码** + 术语 `required` 缺失
- `pages/DictManager.tsx` — `dictCode` 无格式校验（大写/下划线/不重复）
- `pages/sql-query-console/index.tsx:70-71` — `saveName/setSaveDesc` 无 `required`，空名可保存
- `pages/datasource/DataSourceWizard.tsx` — JDBC 无 URL/格式校验
- 大部分 Modal 内保存按钮无 `disabled` 防脏数据提交

---

## 5. 综合修复优先级

### 5.1 P0（阻断，必须在下一轮修复；任一项不打折叠 P0 全部置为"不可验收"）

| # | 类别 | 项 | 触达文件 |
|:-:|:-:|:-:|:-:|
| 1 | API | **补 26 个不存在的后端端点** + 前端路径不对齐 | 见 §2.1 表格 |
| 2 | 边界 | `api.ts` 去 mock 降级反模式（不能把 5xx 映射成 `data: []`） | `api.ts:144,294,728,1254,2318` |
| 3 | 边界 | `pages/aiworkbench/index.tsx:43-48` 4 个吞错 fetch | 1 文件 |
| 4 | 边界 | `AgentMesh.tsx:66-90` 列表静默吞错 | 1 文件 |
| 5 | 基础设施 | `main.tsx` 挂 `ToastProvider`，统一 30+ 页 Toast | 1 + 30 文件 |
| 6 | 基础设施 | `main.tsx:86` showToast→console.log（含 `AIPKnowledgeView:25`、`WorkshopView:27`、`ScenarioManagementView:28`、`DataWorkbenchLayout:49`） | 5 文件 |
| 7 | 安全 | `ExtractionReviewPanel.tsx:171` `dangerouslySetInnerHTML` 加 DOMPurify | 1 文件 |
| 8 | 边界 | `EngineMonitor:71` / `MonitoringCenter:51` / `GuardrailsView:114` 把 403 当 401 | 3 文件 |
| 9 | 规范 | `Login.tsx` 整页：20+ 硬编码色 + 7 处中文 + 4 个自定义 SVG | 1 文件 |
| 10 | 规范 | `AIPKnowledgeView.tsx` 全页（双缺失 + 中文硬编码） | 1 文件 |
| 11 | 规范 | `SystemConfigManager.tsx:349,377` `isZh ? '参数消耗审计' :` 反 i18n 反模式 | 1 文件 |
| 12 | 规范 | `pages/knowledge/components/ExtractionReviewPanel.tsx`:171 已列项 7 | 同项 7 |
| 13 | 规范 | `TaskPanel.tsx` 5 处 alert() + 1 处 window.confirm 全替换 | 1 文件 |
| 14 | 网络 | `api.ts:42-97` 三个 fetch 封装加 `NetworkError` 识别；顶栏加 onLine 横幅；页面级 `<NetworkErrorRetry>` 组件 | `api.ts` + 顶栏 + 公共组件 |
| 15 | 大文件 | `AgentStudioView.tsx` 1,915 行 / `useAgentStudio.ts` 1,488 行 / `OntologyWorkbenchLayout.tsx` ~1,000+ / `InteractiveStepGuide.tsx` ~2,085 — 拆分（参考 PMO-C1） | 4 文件 |

### 5.2 P1（一周内修，用户体验/一致性）

- 硬编码 Tailwind 色在 `project-workbench/aiworkbench/datasource/OntologyWorkbench` 全链路替换 styles. token（~30 文件）
- 30+ user-facing 页签补 `useTheme` + `useLanguage`
- 14 处 `window.confirm` 全部换 `common/ConfirmDialog`（删除/回滚/批量优先）
- `DataTable.tsx:165,189`/`App.tsx:239` 硬编色与中文
- `LogicView/AgentStudioView/ChatbotStudioView/OntologyWorkbenchLayout/ontology/Sidebar/OverviewView/TaskPanel/DataSourceList/AgentManager/PolicyEngine` — window.confirm → ConfirmDialog
- 约 25 个 `pages/*` 数据列表补 EmptyState
- 约 20 个 `pages/*` 数据加载补 Loading 骨架
- 20+ 处 `catch` 里仅 `console.error`，无 UI 反馈 → 改用 `useToast` + 3 秒内自动消失
- `ToastProvider` 挂载后删除 30+ 页重复 `<Toast>` 实现
- 关键表单（`AbacPolicyManager`/`sql-query-console index`/`ActionTypeTab`/`CrudDialog`/`DataSourceWizard`/`GlossaryManager`/`DictManager`）补 `required`/`maxLength`/`type="email"`
- `pages/aiworkbench/index.tsx:44-48` → 改为有 loading/error/empty 三态 + 重试按钮
- `GlossaryManager.tsx:DOMAINS` 中文硬编码替换

### 5.3 P2（批量脚本后续收敛）

- 数据样张 1,000+ 行中文（`ontologyMockData/project-workbench/data/aiworkbench/mockData/types/mockData`）下沉到 mock/seedData，加 `// 测试数据：i18n 不适用` 注释豁免
- 副本/文档中文注释
- api.ts `ANY` 类型收敛与 JSDoc 补全
- 分页/排序白名单

---

## 6. 跨智能体协调请求（PM 介入）

| # | 协调对象 | 请求事项 |
|:-:|------|------|
| 1 | 后端 | 确认并补齐 §2.1 中 26 个 P0 不存在端点。其中：**Pareto /eq**`/api/v1/pareto/*`（4 个）、**PolicyEngine 状态**(`/v1/policy-engine/status`)、**Twin 命令**(`/twins/{id}/command`)、**Portal 搜索**(`/portal/search`)、**Biz Dashboard**（`/ecos/biz/dashboard`）、**Cognitive /plan /blueprint /optimize**（6 个）、**Ontology/preview /workflow/definitions /sources /versions**（5 个）、**Agent metrics api/v1/agent-metrics/{id}**（2 个）。① 后端补 Controller；② 或前端改正确路径。 |
| 2 | 后端 | 与前端核对 **sql-query-console POST /api/v1/engine/data/query/templates** 与 `/templates`（单复数）是否有最近一次重构改过 `@RequestMapping`；**data-workbench/datanet/metadata/strategy/{id}** PUT 后端是否支持；**datanet/datasource** 是否支持 `@RequestMapping` 双路径斜杠前缀写法；**cognitive/world-model 的 /v1/agent-metrics** vs `/v1/aip/agent-metrics` |
| 3 | 后端 | `SysConfigController` 是否补 `PUT /v1/sysconfig/{key}/reset`（前端 resetSysConfig 当前 404） |
| 4 | 后端 | `/api/v1/portal/search` — 是补 Portal*Controller 还是撤掉前端 globalSearch 功能 |
| 5 | 前端 | P0 项 1-15 的修复全部由 fullstack-implementer 按 PMO 指令拆分落地；需先经 reviewer-code-review 验收 |
| 6 | QA | 修复后需跑 `ecos-tests/data-workbench-smoke.mjs` + 4 主题（slate-light/deep-space/cyber-terminal/royal-purple）切换一遍 + zh/en 中英文切换一遍 |
| 7 | 流程 | 门控：所有 P0 关闭才允许交付到下一波次 |

---

## 7. 验收自检清单（修复后验证）

- [ ] 浏览器 devtools Network 抓包确认 26 个 P0 函数全部 2xx（无 404/405）
- [ ] `npm run lint` 无新增 error
- [ ] `npm test` 所有用例过
- [ ] 切 4 主题一遍：深色主题下登录页 / AIPKnowledge / TaskPanel 重点看
- [ ] 切中英文一遍，重点看 Login / 任务中心 / DataTable
- [ ] 模拟断网（Chrome DevTools Offline）30 秒再恢复：所有列表页无白屏
- [ ] 危险操作（删数据源/回滚/批量删任务/删本体）均弹 ConfirmDialog 非 window.confirm
- [ ] 表单空值必填项提交被拦截
- [ ] 删除一个 graph 节点后刷新页面无 500
- [ ] 后端宕机（`lsof -ti:8080 | xargs kill -9`）时页面显示"服务暂不可用 + 重试"而非空白

---

> 本报告由 /goal 模式检查流程产出（资深前端工程师视角），问题数达到可触发 PMO-C1 前端大文件拆分。所有 P0 项需 PM 确认由 fullstack-implementer 主线分发，PIE-特种嘱咐项 + 后端补全项分两条线并行。
