# Phase 2: AI工作台 — 差距分析

> 基线: ECOS当前代码 (2026-08-05) | 目标: 产品化方案 Phase 2 | ai-engine P0-P2已完成

---

## 〇、当前状态

### AI工作台前端

| 页面 | 行数 | 现状 |
|------|:--:|------|
| `aiworkbench/index.tsx` | 223 | 6-Tab布局骨架（仪表盘/逻辑/AI Agent/Chatbot/模型/护栏）+ Knowledge Tab |
| `aiworkbench/DashboardView.tsx` | — | AIP指标仪表盘 |
| `aiworkbench/LogicView.tsx` | — | 逻辑编排面板 |
| `aiworkbench/AgentStudioView.tsx` | 1301 | Agent管理+工具分配（mock数据） |
| `aiworkbench/ChatbotStudioView.tsx` | 1612 | Agent列表+对话区+函数选择（mock数据） |
| `aiworkbench/ModelCatalogView.tsx` | — | 模型目录 |
| `aiworkbench/GuardrailsView.tsx` | — | 安全护栏审计 |
| `aiworkbench/KnowledgeView.tsx` | — | 知识工作台（→Phase 3） |

**问题**：整个AI工作台用的是硬编码mock数据。5个View都是demo骨架，未对接真实后端API。

### 后端AI引擎

| 能力 | 文件 | 状态 |
|------|------|:--:|
| Agent Loop | AgentLoopService.java | ✅ 多轮工具调用(≤5轮)、SSE流式 |
| 工具注册 | ToolRegistry.java | ✅ 7+内置工具(query_db/invoke_rest/delegate/read_file/write_file/search_files/patch) |
| Tool执行 | ToolExecutorService.java | ✅ SQL/REST/BUILTIN三模式+30s超时 |
| Agent模板 | AgentTemplateService.java | ✅ 6内置Agent(data/ontology/cognitive/security/kb/ai) |
| 会话管理 | AgentSessionService.java | ✅ PG持久化+30min过期 |
| 子Agent委托 | AgentDelegationService.java | ✅ 单层delegate |
| 三级配置 | AgentLoopConfig.java | ✅ L1(yml)→L2(builtin模板)→L3(实例) |
| 评估/对比 | AgentStudioController.java | ✅ test/compare/orchestrate/pipeline |
| Agent CRUD | AIPAgentController.java | ✅ CRUD+templates+instantiate |
| SSE流式 | AgentLoopController.java | ✅ /chat + /sessions + /sessions/{id}/chat |

**结论**：后端能力完备，前端是瓶颈——5个View全是mock壳，需要对接真实API做交互。

---

## 一、前端差距（5个面板）

### 1.1 ChatbotStudio（对话面板）— 🔴 核心

| # | 需求 | 现状 | 差距 |
|---|------|------|------|
| C1 | 三栏布局 | 有骨架 | 左侧Agent列表mock、中间对话mock、右侧配置面板硬编码 |
| C2 | Agent选择 | 有下拉 | 未调 `/api/v1/aip/agents` |
| C3 | 对话区（Markdown+表格+来源标注） | 有对话区 | 未调 `/api/v1/agent-loop/chat`，无SSE流式打字效果 |
| C4 | System Prompt实时编辑 | 无 | 需新增textarea→调Agent更新API |
| C5 | 工具白名单可视配置 | 部分 | 有functionIds选择但硬编码 |
| C6 | 参数面板（温度/模型/最大轮次） | 无 | 需新增 |
| C7 | 对话历史/多线程切换 | 无 | 后端 `/api/v1/agent-loop/sessions` 已有，前端未对接 |
| C8 | 空状态引导 | 无 | 需"选择一个Agent开始对话"引导 |

### 1.2 AgentStudio（Agent管理）— 🟡 重要

| # | 需求 | 现状 | 差距 |
|---|------|------|------|
| A1 | Agent列表（CRUD） | 有 | 对接 `/api/v1/aip/agents` 替换mock |
| A2 | Agent市场（6内置模板卡片） | 无 | 后端 `/api/v1/aip/agent-templates` 已有，前端无卡片化展示 |
| A3 | 一键实例化 | 无 | 后端 `/api/v1/aip/agents/instantiate` 已有 |
| A4 | 测试控制台 | AgentTestConsole.tsx存在 | 需对接后端test API |
| A5 | Agent状态管理 | mock | 需对接真实status字段 |

### 1.3 LogicView（逻辑编排）— 🟡 重要

| # | 需求 | 现状 | 差距 |
|---|------|------|------|
| L1 | 可视化Pipeline画布 | 有LogicView | 列表式非拖拽画布 |
| L2 | 拖拽Agent节点→连线 | 无 | 需引入React Flow或自绘 |
| L3 | 一键执行 | 无 | 后端 `/api/v1/aip/studio/pipelines/{id}/execute` 已有 |
| L4 | 展开节点输出 | 无 | 需调executions API |

### 1.4 评估面板（DashboardView改造）— 🟡 重要

| # | 需求 | 现状 | 差距 |
|---|------|------|------|
| E1 | 选择Agent+问题集→评估 | 无 | 后端compare API已有 |
| E2 | 实时进度条 | 无 | — |
| E3 | 雷达图（5分类得分） | 无 | 需引入图表库 |
| E4 | 业务场景问题集 | 无 | 后端需补充（财务/供应链/制造） |

### 1.5 多Agent协同 — 🟡 重要

| # | 需求 | 现状 | 差距 |
|---|------|------|------|
| M1 | 输入需求→自动拆解子任务 | 无前端 | 后端 `/api/v1/aip/studio/orchestrate` 已有 |
| M2 | 子Agent耗时和输出展示 | 无 | — |
| M3 | 汇总报告生成 | 无 | — |

---

## 二、后端差距（轻量补齐）

| # | 能力 | 现状 | 措施 |
|---|------|------|------|
| B1 | Agent调用链追踪 | ToolRegistry有日志，未结构化返回给前端 | 在chat response中附加 `trace: [{tool, args, result, elapsed}]` |
| B2 | SSE心跳 | 无 | 每5s发 `data: [HEARTBEAT]` |
| B3 | 多Agent并发控制 | 有Semaphore但硬编码上限3 | 改为可配置 |
| B4 | 友好错误文案 | Agent Loop异常返回堆栈 | 映射为友好文案（"查询超时，请缩小数据范围"） |
| B5 | 评估问题集 | 无业务场景 | 新增财务分析/供应链/制造执行3个json问题集 |
| B6 | 工具描述业务化 | "执行SQL查询" | →"查询企业经营数据" |

---

## 三、对话体验打磨

| # | 需求 | 现状 |
|---|------|------|
| D1 | 多线程切换 | 无thread下拉框 |
| D2 | SSE流式打字效果 | 无（目前是整段返回） |
| D3 | 错误自动重试提示 | 无 |
| D4 | 空状态引导 | 无 |
| D5 | 对话导出 | 无 |

---

## 四、严重度汇总

| 严重度 | 数量 | 关键项 |
|:--:|:--:|------|
| 🔴 阻塞 | 3 | ChatbotStudio三栏布局对接真实API、Agent市场（模板卡片+实例化）、SSE流式对话 |
| 🟡 重要 | 8 | 逻辑编排画布、评估面板、多Agent协同前端、调用链追踪、问题集、Agent测试控制台、工具业务化、友好错误文案 |
| 🟢 体验 | 5 | 心跳、对话导出、空状态引导、打字效果、多线程切换 |

---

## 五、可复用资产

- ✅ AgentLoopController — `/chat` + `/sessions` + SSE
- ✅ AIPAgentController — CRUD + templates + instantiate + test
- ✅ AgentStudioController — test/compare/orchestrate/pipeline
- ✅ AgentTemplateService — 6内置Agent模板
- ✅ ToolRegistry — 7+工具
- ✅ AgentSessionService — PG会话
- ✅ AgentDelegationService — 子Agent委托
- ✅ aiworkbench/ — 5个View骨架（改造非重写）
- ✅ aiworkbench/api.ts — API层已有fetch函数
- ✅ types/aiworkbench — 类型定义完整
