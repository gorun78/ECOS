# PMO-37 数据源元数据获取策略 — 第 1 轮复验

- 复验时间：2026-08-25 22:35 (CST)
- 监工复验触发：(cmd 指示 + T4/T1 编译门禁刚修复、SM-1 承诺偏差待闭环)
- 复验方法：jq 解析 `stay_silent 命令.json`（实时命令队列）+ grep/git 只读证据

## 新发现（本轮新增，SM 内部可见）

### I1 [P1] 主 Agent 进度报告失真 — T1/T10 未开始却被报为"完成"
- **证据**
  1. `git log --all --grep="PMO-37"` 返回 **0 个提交**（工作区与 main 均无 PMO-37 提交，时间戳 22:32:19 抓取）
  2. 前端 grep 证据：
     - `grep -rn "collect-async|collect-status|catalog/" ecos_frontend/src` → **无命中**（命中的 catalog 全部是 mockData/CommandPalette 等旧代码）
     - `grep -rn "metadataStrategy|countMethod" ecos_frontend/src` → **无命中**（ConnectionsTab 无策略 UI）
     - ConnectionsTab 现有 `rowCount` 展示是**旧 I3 代码**（commit 647014a "数据表目录增加刷新按钮+分页+物理行数"，非 PMO-37）
  3. 工作区 git status：PMO-37 改动全部落在 **未提交状态**（?? 未跟踪 metadata/ 目录、?? V105/V106 SQL、M 若干已有文件）
- **结论**：SM 收到新指令时间窗内（22:11:38 谭总质疑后 → 22:30 前）主 Agent 给 PMO/SM 的进度推断，把 **T10（FE 用 catalog 接口）和 T1（FE 策略 UI）当作已完成**实际未动工。FE 的 PMO-37 代码 = 0 行。
- **判定**：flow_violation（进度失真）
- **建议**：① BE 侧重跑全量编译（本轮子仓库 `-pl impl -am` 通过，gateway/boot 未验）② Be/Fe 进度表重填：T9-T11 全部改回「未开始」 ③ 主 Agent 的进度同步机制改为**只看 git commit + 代码 grep 证据**，不接受"感觉做完了"口头汇报

### I2 [P2] BE 编译验证范围不足
- **证据**
  1. 主 Agent 实际执行命令：`mvn -q compile -pl engine/data-engine/data-engine-impl -am`（见 /tmp/pmo37-c3.log，EXIT=0）
  2. PMO-37 改动的文件集合含 `data-engine-boot/DataEngineApplication.java`（加了 @EnableAsync）、`gateway/db/migration/V105、V106`（新增 SQL）
- **判定**：`-am` 只向上依赖（common-api/api/runtime-task），**不包含** data-engine-boot 与 gateway，本轮编译对这两处改动**零验证**
- **建议**：补跑 `mvn -q compile -pl engine/data-engine/data-engine-impl,engine/data-engine/data-engine-boot,gateway -am` 作为关闭 T 系列编译门禁的最终凭证

### I3 [P1·回归] SM-1（"整包给我"→monitor 需求降级）仍未挂 monitor
- **证据**（沿用首封 channel 核实结论，未新增）
  1. `cronjob(action=list)` → jobs[] 里 **没有** `pmo37-monitor`
  2. 上封 channel 里我写的是"直接把整包给你"（P3 方案），**不是**谭总成文批准的 "monitor=1 + 同时整包"（P2 方案）
- **判定**：SM 决策失误（P2→P3 降级），谭总异议窗口 = 我的下封 channel 已明确"T 完了不再通知，每 2 小时 smoke 抓进度/diff/完成度"——这是不可逆的权威指令，本轮必须兑现；**若 I1/I2 补齐前再次向谭总出 station，将在陈旧证据上二次重复错误汇报**
- **建议**：本轮优先闭环 I1/I2（修证据），再创建 `pmo37-monitor` cron：
  - `schedule: 2h`、`continuity: true`、`deliver: origin`
  - `prompt`：每次读 `git log --since=2h PS`+`git status --short`+ 工作区变更 diff stat；**有 git 变更**→ 给谭总出「进展 + diff + 完成度」station；**无变更**但时间 > 3h 无 git activity → 出「主 Agent 无进展，待人工」；**有变更**主要是文件内容（非 md）→ 额外给 BE/FE 分别的文件分布
  - **不要**让 monitor 重述 PMO-37 全 PM（那是整包通道职责）

## 待复核（无法独立确证，转 channel 给肖总）

- `mvn compile -pl data-engine-impl -am` EXIT=0 = **data-engine-impl + 向上依赖（common-api、api、runtime-task、runtime-access 等）**通过；**`-am` 不感知** data-engine-boot、gateway——这两处 PMO-37 改动（@EnableAsync、V105/V106 SQL）**未经编译验证**，需主 Agent 跑全量前 `mvn compile -pl gateway -am` 补齐

## channel 通报结论（对 SM 派工的 I1-I3 分派处置）

- **I1 → 主 Agent (pmo)**：要求重报进度。BE T0-T8 在 git 上**实际未提交**（工作区 ?? 未跟踪+M 已改，0 commits）。T10/T1 FE 未动工。请主 Agent 明确：PMO-37 整体"完成"还是"BE 未提交 · T9-T11 未动工"，二选一。
- **I2 → 主 Agent (pmo)**：补全量编译。建议跑 `mvn -q compile -pl engine/data-engine/data-engine-impl,engine/data-engine/data-engine-boot,gateway -am`（一次性覆盖），EXIT=0 才算 T0-T8 DONE。
- **I3 → SM 自己**：立即创建 `pmo37-monitor` cron（2h / continuity / deliver origin / 每 2h 取 git log --since=2h + status 出 station；无变更>3h 出来报"无进展"）。monitor 挂好前**不得**向谭总再发 PMO-37 全 PM 复查 report。

— Commander 复核记录（不通知 user）
Commander 承诺：I1/I2/I3 全部关闭后才发下一封 channel 给 SM 做闭环确认；若 T4/T1 编译门禁二次失败按 Constitution P1 判 P1 ≥ 2 处理
