# PMO-38 后端 P0 端点补齐 — 批次 1（sysman / kb-cognitive）

> **架构铁律**: 必须遵循 [ECOS架构铁律](../../.trae/rules/架构铁律.md)
> 来源: 肖国荣 | 日期: 2026-09-05 | 责任人: backend-fullstack (fullstack-implementer)
> 铁律:
> 1. API 只增不改（§0.2/§5.1.1）— 3 项 sysman 端点 / 1 项 kb 端点纯新增
> 2. 新增 Controller 必须同步更新三滤波器：VersionPrefixRewriteFilter + SecurityConfig.permitAll + ClearanceInterceptor（双路径 /api/v1/ 与 /api/ 各写一遍），缺任何一层 = 403/404 = 验收失败（§1.2）
> 3. 编译 = `mvn install -DskipTests`（非 compile），否则 Gateway 加载 .m2 旧 JAR（§1.5）

## §背景

来源：`docs/9-checks/2026-09-05-前端界面操作实现情况检查报告.md` §2.1（26 个前端 P0 端点指向后端不存在端点）+ §6（协调请求 1/3）。

本批次覆盖批次 1（批次 2/3/4/5 见 PMO-39/40/41/42）：
- **3 项 sysman 用户管理端点补齐**（对应报告 §2.1 #1/#2/#3）：用户强制下线、密码重置生成、批量创建
- **1 项 kb 端点补齐**（对应 §2.1 #12）：`GET /api/v1/knowledge-bases` — 前端 `fetchKnowledgeBases` 当前 404
- **3 项 cognitive 端点补齐**（对应 §2.1 #13/#16/#14 的部分）：`GET /api/v1/cognitive/blueprint`、`POST /api/v1/cognitive/optimize`、`PUT /api/v1/sysconfig/{key}/reset`（归 sysman）

> 注：本批次仅落地 5 个 Task。sysconfig/{key}/reset 与 cognitive 的 /plan 相关端点分别在 PMO-38（T4）/ PMO-39（T2-T3）中。

## §禁止清单（铁律继承 + 本指令特有）

1. ❌ 不跨 Phase 预创建文件（PMO 未下发的批次 2/3 端点）
2. ❌ 不改已有 API 路径或参数签名（`PUT /{id}/password` 保留；新增 `POST /{id}/reset-password` 独立端点）
3. ❌ 不新增 Maven 模块（基线 13）/ 不新增 Docker 容器
4. ❌ 不 implements 已有 Service 接口（多 Bean 冲突）
5. ❌ 新增 Controller 漏更新三滤波器（最高频踩坑，§1.2）
6. ❌ 用 `mvn compile` 替代 `mvn install`（§1.5）
7. ❌ 不注册 auth.whitelist.paths（§1.1 路径铁律）
8. ❌ 不在引擎内重复实现权限/脱敏/审计逻辑（安全接入铁律 §2.4，强制走 security-engine REST）

## §Task（原子：单文件 + 验收）

| Task | 文件 | 操作 | 验收 |
|:--|------|------|------|
| T1 | `ecos_backend/sysman/src/main/java/com/chinacreator/gzcm/sysman/controller/UserManagementController.java` | 新增 3 个端点方法（单 Controller 内追加方法，非新 Controller）：① `POST /api/v1/system/users/{id}/force-logout` 调 sysman 现有会话管理 → 清除该用户所有 JWT；② `POST /api/v1/system/users/{id}/reset-password` 生成 12 位随机密码（禁复现 `PUT /{id}/password` 的入参语义），返回一次性 token，审计日志异步发；③ `POST /api/v1/system/users/batch` 接收 `UserBatchSaveDTO`（Lombok `@Data`），单条校验 + Count ≤100，对应 pg `sys_user` 行级 insert（事务，MyBatis `foreach` 批量） | `bash ~/start-gateway.sh` → 登录 → curl：① `POST /api/v1/system/users/{id}/force-logout` 返回 200，原 token 后续请求 401；② `POST /api/v1/system/users/{id}/reset-password` 返回 200 + token，`pg_dump` 旧 pwd_hash 已更新；③ `POST /api/v1/system/users/batch` 提交 3 用户 → `GET /api/v1/system/users?pageSize=20` 新增 3 行 |
| T2 | `ecos_backend/sysman/src/main/java/com/chinacreator/gzcm/sysman/dto/UserBatchSaveDTO.java` | 新增 DTO（Lombok `@Data`）：`List<UserSaveDTO> users`，单条 `UserSaveDTO` 复用现有（@Data），批量上限校验 `@Size(max=100)` | javac 编译通过；单测 `UserManagementControllerTest.batchCreate_max200_rejected` 返回 400 |
| T3 | `ecos_backend/engine/kb-engine-impl/src/main/java/com/chinacreator/gzcm/kb/controller/KnowledgeListController.java` | 新增 `GET /api/v1/knowledge-bases` 端点（前端 `src/services/agentConfig.ts:199` 调用此路径），返回 `List<KnowledgeBaseVO>`（name/description/owner/version），数据来源 kb-engine 现有 `KnowledgeBaseServiceImpl.list()` | curl `GET /api/v1/knowledge-bases` 200；返回 JSON 数组；空库 → 空数组不 500；同路径 `/api/knowledge-bases` 也通（双路径） |
| T4 | `ecos_backend/sysman/src/main/java/com/chinacreator/gzcm/sysman/controller/SysConfigController.java` | 新增 `PUT /api/v1/sysconfig/{key}/reset` 端点（前端 `src/api.ts:2611 resetSysConfig` 调用），接收 `key: String` 路径变量；逻辑：从现有 `sys_config` 表取该 key 默认值 → 覆盖当前值 → 异步 audit log。路径别名 `/api/sysconfig/{key}/reset` 也通 | curl `PUT /api/v1/sysconfig/{key}/reset` 200；`GET /api/v1/sysconfig/{key}` 值回到默认值；3 滤波器双路径注册 |
| T5 | 三滤波器配置同步（3 文件） | `ecos_backend/gateway/src/main/java/.../VersionPrefixRewriteFilter.java`：加 `/api/v1/knowledge-bases` 的 V1_REWRITE_MAP（按现状 KEEP/REMOVE 判定）；`ecos_backend/sysman/src/main/java/.../security/SecurityConfig.java` permitAll 加 `/api/v1/system/users/{id}/force-logout`、`/api/v1/system/users/{id}/reset-password`、`/api/v1/system/users/batch`、`/api/v1/knowledge-bases`、`/api/v1/sysconfig/{key}/reset`（+ 双路径 `/api/...`）；`ecos_backend/sysman/src/main/java/.../security/ClearanceInterceptor.java` 豁免列表同上双路径 | `mvn install -DskipTests` 0 ERROR；`bash ~/start-gateway.sh` 启动后 curl 上述 5 个端点均 200（非 401/403/404） |

## §目标工时

3 小时（含三滤波器同步 0.5h + 5 端点 2h + 自测 0.5h）。

## §依赖关系

- **输入**：`docs/9-checks/2026-09-05-前端界面操作实现情况检查报告.md` §2.1 #1-#13（本批次涉及 5 项）
- **前置**：无（可与其他批次并行；与 PMO-39/40/41/42 无文件冲突）
- **后置**：PMO-44（前端 KnowledgeView 修正，消费 T3 的 `/api/v1/knowledge-bases`）；PMO-46（QA 验收，消费 T1-T4 端点）
- **Worker**: `fullstack-*`，**Verifier**: `reviewer-*`，**Synthesizer**: `pm-*`

## §分支

`feature/pmo-38-backend-p0-batch1`

## §验收清单（PM 主持，QA 复核）

- [ ] `mvn install -DskipTests -q` 0 ERROR 0 WARN（archunit 守护断言无新违规）
- [ ] `bash ~/start-gateway.sh` 启动成功（`lsof -ti:8080` 拿到 PID）
- [ ] `curl -s http://localhost:8080/api/v1/knowledge-bases | jq '.length'` ≥ 0
- [ ] `curl -s -X POST "http://localhost:8080/api/v1/system/users/{id}/force-logout" -H "Authorization: Bearer $TOKEN" | jq .code == 0`
- [ ] `curl -s -X POST "http://localhost:8080/api/v1/system/users/{id}/reset-password" -H "Authorization: Bearer $TOKEN" | jq '.data.token.length' == 12`
- [ ] `curl -s -X POST "http://localhost:8080/api/v1/system/users/batch" -d '{"users":[...]}' | jq .code == 0`
- [ ] `curl -s -X PUT "http://localhost:8080/api/v1/sysconfig/{key}/reset" | jq .code == 0`
- [ ] 所有端点 `/api/...` 裸路径返回同样 200（双路径验证）
- [ ] 安全审计日志（`GET /api/v1/security/audit/log`）有对应写操作记录
- [ ] Reviewer 代码审查（reviewer-code-review Skill）deliverable_allowed=true
