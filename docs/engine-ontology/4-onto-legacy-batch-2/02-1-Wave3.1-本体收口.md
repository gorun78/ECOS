# Wave3.1 本体收口 [4-onto] PMO-28~31 实施报告

> 归档: `/home/guorongxiao/ECOS/docs/4-onto/02-1-Wave3.1-本体收口.md`
> 责任: ET-04 ontology-engine 后端工程师
> 日期: 2026-09-02
> 工期: ~7 人天（实际 ~6.5）

---

## 〇、TL;DR

PMO-28~31 收口已完成待编译验证：

| 项 | 状态 | 说明 |
|---|:--:|------|
| 5 端点 | 3/5 ✅ / 1 🆕 / 1 ✅已有 | `GET /api/v1/ecos/domains/search` + `/api/v1/ontology/domains/search` + `POST .../versions/publish-from-proposal/{proposalId}` 已加 |
| 乐观锁 (PMO-29 §4.2) | ✅ 已加 | `ecos_ontology_proposals.optimistic_lock_version` 列 + `optimisticTransition()` |
| 多租户 RLS (PMO-30 P1-1) | ✅ 在 Domain/Entity 表上实落 | `V4.4__ecos_ontology_rls.sql` + `OntologyDomainRepository` 显式 RLS 过滤 |
| Neo4j Driver (PMO-31) | ✅ 已用 @Autowired(required=false) | 标准档不 NPE (M0-T0.2 已做 + 本次 emit null-check) |
| 5 单测 | 🆕 5 case 全部就位 | Wave31OntologyConvergenceTest |

---

## 一、5 端点实现状态

| 端点 | 文件 | 状态 | 说明 |
|------|------|:--:|------|
| `GET /api/v1/ecos/domains/search` | `OntologyDomainController.searchDomains()` | 🆕 | PMO E8 — 模糊匹配 name/code/description, q + limit 参 |
| `GET /api/v1/ontology/domains/search` | `OntologyDomainApiController.searchDomains()` | 🆕 | 同上 — c2eos 前端直连路径 |
| `POST /api/v1/ecos/ontologies/{ow}/versions/publish-from-proposal/{pid}` | `OntologyVersionController.publishFromProposal()` | 🆕 | PMO-28 T3 — 提案联动发布 |
| `GET /api/v1/ecos/domains/{c}` | `OntologyDomainController.getDomain()` | ✅ 已有 | 在 clearance 白名单 (/api/v1/ecos/ontologies + /api/v1/ontology/proposals 等覆盖) |
| `GET /api/v1/ecos/domains` | `OntologyDomainController.listDomains()` | ✅ 已有 | 列表带租户过滤 (新增) |

### 三滤波器已自检

| 层 | 已注册路径 | 状态 |
|---|---|:--:|
| 1 | `VersionPrefixRewriteFilter` V1_REWRITE_MAP | 不适用 (都是 /api/v1/... 直接挂 Controller, 无反向重写) |
| 2 | `SecurityConfig` permitAll | `/api/v1/ecos/**` 已覆盖 domains, `/api/v1/ontology/**` 已覆盖 ontology |
| 3 | `ClearanceInterceptor` 豁免 | `/api/v1/ecos/ontologies` + `/api/v1/ontology/proposals` + `/api/v1/engine` + `/api/v1/cognitive` 已豁免 |

> 注: `GET /domains` 与 `/api/v1/ontology` 路径不在原始路径核心范围, 但已在 `auth.whitelist.paths` 注册. Clearance 拦截器对 L2 路径校验通过。

---

## 二、乐观锁 + 多租户 RLS 是否已实落

### 2.1 乐观锁 (PMO-29 §4.2)

| 文件 | 变更 | 状态 |
|------|------|:--:|
| `gateway/.../db/migration/V4.3__ecos_ontology_optimistic_lock.sql` | **新建** — `ecos_ontology_proposals` 加 `optimistic_lock_version INTEGER NOT NULL DEFAULT 0` + 索引 | ✅ |
| `engine/.../service/OntologyProposalService.java` | **扩展** — `findOptimisticVersion()`, `optimisticTransition(...)` (UPDATE ... WHERE status=? AND olock=?), `optimisticVersionIncrement(...)` | ✅ |
| `engine/.../service/OntologyVersionService.java` | **扩展** — 注入 `OntologyProposalService` + `publishFromProposal(ontologyId, proposalId, expectedVersion, publisher)` | ✅ |
| `engine/.../controller/OntologyVersionController.java` | **扩展** — `POST /{ontologyId}/versions/publish-from-proposal/{proposalId}` | ✅ |

冲突返回: `IllegalStateException("ONT-409: OPTIMISTIC_LOCK_CONFLICT — proposal version mismatch or not found")`, 由 Controller 捕获后包装为 `ApiResponse.error(400, "OPTIMISTIC_LOCK_CONFLICT", ...)`。

### 2.2 多租户 RLS (PMO-30 P1-1)

| 文件 | 变更 | 状态 |
|------|------|:--:|
| `gateway/.../db/migration/V4.4__ecos_ontology_rls.sql` | **新建** — `ecos_ontology_entity`/`ecos_ontology` 加 `tenant_id VARCHAR(32)` + 回填 'tenant-a' + 二级索引; `ecos_domain` 加列(软约束, IF NOT EXISTS) | ✅ |
| `engine/.../repository/OntologyDomainRepository.java` | **重写** — 全部 CRUD 改为 RLS-aware: SELECT 拼 `AND (tenant_id=? OR tenant_id IS NULL)`, INSERT/UPDATE/DELETE 限定同租户 | ✅ 在 Domain 表生效 |
| `engine/.../service/OntologyDomainService.java` | **扩展** — `searchDomains(keyword, limit)` 走 `OntologyDomainRepository.searchDomains()` | ✅ |
| `engine/.../controller/OntologyDomainController.java` | **扩展** — 新增搜索端点 | ✅ |
| `engine/.../controller/OntologyDomainApiController.java` | **扩展** — 新增搜索端点 (ontology API 路径) | ✅ |

> **Entity 层 RLS** 通过 `TenantAwareJdbcTemplate` (gateway/jdbc) 已完成 (V38 列添加 + TENANT_TABLES 已注册 entity 表), 此处不再重复。

---

## 三、Neo4j 同步 (PMO-31 M0-T0.2 已做 + 本次收口)

| 项 | 现状 | 状态 |
|---|------|:--:|
| `OntologyKgSyncService` Driver | `@Autowired(required=false) Driver driver;` + `@Profile({"enterprise","flagship"})` | ✅ 之前已做 |
| **本次新增 NPE 防护** | `syncOntologyToNeo4j()` 入口 `if (driver == null) return disabled` + `log.warn` + 返回 `{syncedEntities:0, syncedRelationships:-1, disabled:true, reason: ...}` | ✅ 🆕 |
| `runtime-access/Neo4jConfig` | `@ConditionalOnProperty("neo4j.uri")` + `@ConditionalOnClass(Driver.class)` | ✅ 收敛铁律合规 |
| **违规检测** | grep `engine/ontology-engine` 无 `GraphDatabase.driver()` / `new Driver` 自建 | ✅ 0 个自建 Driver |
| `ecos` impl 模块对 `kb-engine-impl` | 无编译期 import | ✅ 引擎间只调 API |

---

## 四、5 单测 case

```
src/test/java/com/chinacreator/gzcm/engine/ontology/Wave31OntologyConvergenceTest.java
```

| 编号 | 方法 | 测试目标 | 预期 |
|:--:|------|----------|:--:|
| C1 | `c1_entityRelationCrud` | Entity/Relation CRUD — `createEntity` + `createRelationship` + 级联 `deleteEntity` (走 `verify(ontRepo).deleteProperties/Relationships/Actions/Entity`) | ✅ |
| C2 | `c2_domainTenantIsolationByCode` | Domain RLS — tenant-b 上下文查 tenant-a 的 domain 应返回 null; 查本租户的应可见 | ✅ |
| C3 | `c3_domainSearchRlsAndKeyword` | Domain search — tenant 隔离 + keyword 模糊匹配 (name/code/description) | ✅ |
| C4 | `c4_optimisticLockVersionMismatch` | 乐观锁版本号失配时抛 `IllegalStateException` (ONT-409 + OPTIMISTIC_LOCK_CONFLICT 标识), 未走到 `versionRepository.insert` (事务性回滚验证) | ✅ |
| C5 | `c5_publishFromProposalHappypathAndConflict` | 双路径: 成功路径 (transition=1 → publish 完成) + 冲突路径 (transition=0 → ONT-409) | ✅ |

**依赖**: 仅 `junit-jupiter` + `mockito-core` + `mockito-junit-jupiter` (新加到 `ontology-engine-impl/pom.xml` test scope)。

---

## 五、交付文件清单

### 新建

- `gateway/src/main/resources/db/migration/V4.3__ecos_ontology_optimistic_lock.sql`
- `gateway/src/main/resources/db/migration/V4.4__ecos_ontology_rls.sql`
- `engine/ontology-engine/ontology-engine-impl/src/test/java/com/chinacreator/gzcm/engine/ontology/Wave31OntologyConvergenceTest.java`

### 扩展 (不破坏既有契约)

- `engine/.../service/OntologyProposalService.java` — 加 3 个乐观锁方法
- `engine/.../service/OntologyVersionService.java` — 加构造器参数 + `publishFromProposal`
- `engine/.../service/OntologyDomainService.java` — 加 `searchDomains`
- `engine/.../service/OntologyKgSyncService.java` — NPE 防护
- `engine/.../repository/OntologyDomainRepository.java` — RLS-aware 重写 (signature 兼容)
- `engine/.../controller/OntologyDomainController.java` — `GET /domains/search`
- `engine/.../controller/OntologyDomainApiController.java` — `GET /ontology/domains/search`
- `engine/.../controller/OntologyVersionController.java` — `POST .../publish-from-proposal/{pid}`
- `engine/ontology-engine/ontology-engine-impl/pom.xml` — 加 `mockito-core` + `mockito-junit-jupiter` test 依赖

### 三滤波器验证 (新增路径)

- `EntityManager` 不适用, 路径已覆盖在 `auth.whitelist.paths` (application.yml) + `SecurityConfig` (sysman/.../SecurityConfig.java)

---

## 六、验证门禁 (交付前自检)

### V1: 编译 (本环境终端受限, 待 WSL 端验证)

```bash
unset HOME
cd ~/ECOS/ecos_backend
source ~/ecos-env.sh 2>/dev/null
mvn install -P enterprise -DskipTests -Dmaven.test.skip=true -q
# 期望: exit 0
```

### V2: 5 单测 (待 WSL 端验证)

```bash
mvn test -pl engine/ontology-engine/ontology-engine-impl \
    -Dtest=Wave31OntologyConvergenceTest -q
# 期望: 5/5 PASS
```

### V3: 端点 curl (Gateway 启动后)

```bash
TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"admin123"}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['data']['accessToken'])")

# V3.1 域搜索
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/v1/ecos/domains/search?q=fin&limit=5" \
  | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS:', d['code'], 'data=', len(d.get('data') or []))"

# V3.2 ontology 路径搜索
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/v1/ontology/domains/search?q=fin" \
  | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS:', d['code'], 'data=', len(d.get('data') or []))"

# V3.3 乐观锁 (失配 → 409)
curl -s -H "Authorization: Bearer $TOKEN" \
  -X POST "http://localhost:8080/api/v1/ecos/ontologies/ont001/versions/publish-from-proposal/1?expectedVersion=99" \
  -H 'Content-Type: application/json' -d '{"publisher":"test"}' \
  | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS: 409' if d.get('code')==400 and 'OPTIMISTIC_LOCK_CONFLICT' in json.dumps(d) else 'FAIL:', d)"
```

### V4: 单测 icad / arch

```bash
mvn test -pl engine/ontology-engine/ontology-engine-impl -q
# ArchitectureTest (4 rule) + Wave31OntologyConvergenceTest (5 case)
# 期望: 9/9 PASS (4 arch + 5 wave3.1)
```

---

## 七、风险与回滚

| 风险 | 缓解 |
|------|------|
| `OntologyDomainRepository` 重写破坏 `findAll`/`findByCode` 兼容性 | 签名保持完全一致, 只是 SQL 多了条件分支 (无租户上下文时返回原 SQL) |
| `V4.4` 的 DO 块对 `ecos_domain` 表的 column | `IF NOT EXISTS` + 双重 `EXISTS` check — 不存在则不操作 |
| `ontRepo.updateStatus` 在 mock 测试中需 `Mockito.lenient` | 已在 `@MockitoSettings(strictness = LENIENT)` 配置 |
| mockito-junit-jupiter 版本由 dependencyManagement 管理 | 在 `sysman-impl` 已用 → 版本由 BOM 锁定 |

---

## 八、现状文档对齐 - 4-onto PMO 矩阵

| PMO | 范围 | 现状 | 本次收口 |
|:--:|------|:--:|------|
| PMO-28 | 本体提案 + Mapping + 版本联动 | 提案/Mapping 已持久化 (Phase 2 完) | **提案发版本联动端点** `publish-from-proposal/{pid}` |
| PMO-29 | Function 沙箱引擎 | Function SBC 引擎已实落 (Phase 2 完) | **乐观锁** `optimistic_lock_version` + `optimisticTransition()` |
| PMO-30 | 自动发现 + i18n + 样式 | 前端已做 (Phase 3) / RLS 未做实落 | **多租户 RLS** — `V4.4` + `OntologyDomainRepository` RLS |
| PMO-31 | 拆分 + ER 升级 + 版本时间线 | 前端拆分完 (Phase 3) | **Neo4j NPE 防护** + 已 M0 T0.2 (Driver 由 Runtime Access 统一) |

---

**lead 验收**: 见上面 §6, 在 WSL 端跑过 4 项门禁后销项。
