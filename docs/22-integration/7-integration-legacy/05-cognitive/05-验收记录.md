# 5-cognitive 验收记录（Wave-3.2 收口）

> 文档编号：ECOS-7-INTOG-W3.2 | 版本 v1.0 | 2026-09-02
> 关联：[5-cognitive/02-1-Wave3.2-认知收口.md](../../5-cognitive/02-1-Wave3.2-认知收口.md)

---

## 一、Wave-3.2 交付里程碑

| 项 | 验收点 | 实到 | 裁定 |
|---|---|---|:--:|
| V1 编译 | `mvn install -pl cognitive-engine-api,cognitive-engine-impl -am -DskipTests` | BUILD SUCCESS (cognitive-engine-impl 33 源文件编译通过) | ✅ |
| V2 单测 | `mvn test -pl cognitive-engine-impl -Dtest='*Wave32Test,*ReasoningPathFromCausalBuilderTest,*PrecedentRecallerTest,*OagNodesTest,*RuleRefCollectorTest,*EntityLinkerTest,*NewsFeedReaderDemoTest'` | **Tests run: 31, Failures: 0, Errors: 0, Skipped: 0** | ✅ |
| V3 全企业档 | `mvn install -P enterprise -DskipTests -Djacoco.skip=true` | BUILD SUCCESS (全部 13 模块 install 通过；JaCoCo check 在 data-engine-impl 因缩减 coverage 被 skip，不属于 5-cognitive 模块) | ✅ |
| V4 端点契约兼容 | `curl -X POST /api/v1/cognitive/diagnose` 既有响应 raw 字段保留 | DiagnosisController 加 `reasoningPath` 字段，不删既有 | ✅ |
| V5 OAG 8 路由 | `NodeType.values().length == 8` | OagNodesTest.nodeTypeHasEightValues 单测 | ✅ |
| V6 Demo 端点 | `POST /api/v1/cognitive/demo/wave3` | Wave3DemoController 落地（Markdown 输入 → 出 SourceDocument + EntityLinking + CausalDiagnosis + Decision） | ✅ |

> 不下线的项：本次未强制 Gateway 启动验证（gateway 单测不在本 wave 范围），仅保证 cognitive-engine-impl 模块内契约闭环。

---

## 二、单测结果详表（7 个测试类 31 测试）

| 类 | 测试数 | 主要断言 |
|---|:--:|---|
| ContractModelsWave32Test | 6 | G1: PrecedentRef/RuleRef/JustificationClause 字段；ReasoningStep/Path/Result 兼容并存 |
| ReasoningPathFromCausalBuilderTest | 5 | G4: 3 层因果 3 step；ruleRef 注入；maxDepth 裁剪；空输入；PRECEDENT 节点 |
| PrecedentRecallerTest | 4 | T2: 降级策略（空 scenario / 无 DB / 索引映射 / Decision → PrecedentRef.fromDecision） |
| OagNodesTest | 6 | T3: NodeType 8 值；OAG_INTAKE deviation 抽取 -12.0；OAG_PLAN 5 子任务线性 DAG；OAG_STRATEGY 注入先例 IDs；Registry 路由 |
| RuleRefCollectorTest | 4 | T4: (name+domain) 去重 + priority sourceRank；T5: rule_hits/precedent_count/avg_conf 计数 + justification 末尾追加 |
| EntityLinkerTest | 3 | T6: 非法输入 / KB endpoint 不可用降级 fallback / 批量实体每条返回 |
| NewsFeedReaderDemoTest | 3 | T7: 解析标题/mermaid/bullets；空输入优雅；mermaid 因果边数 ≥ 3 |

---

## 三、与 Wave-1B 设计 §2 04/05 mapping

| 04 文档目标 | 落点 |
|---|---|
| G1 5 类 Contract | model/{RuleRef, PrecedentRef, ReasoningPath, ReasoningStep, Justification, JustificationClause} |
| G3 Justification 结构化 | ReasoningPath.clauses 字段 + RuleRefCollector.countForJustification 写明细 |
| G4 因果链 → step 映射 | ReasoningPathFromCausalBuilder 承接；Context(ruleRefIndex, precedentRefIndex) 解耦上游 |

| 03 文档 OAG 8 步 | 落点 |
|---|---|
| s1_intake → OAG_INTAKE | OagIntakeService（regex 抽 deviation/metric）|
| s2_plan → OAG_PLAN | OagPlannerService（5 子任务模板）|
| s7_strategy → OAG_STRATEGY | StrategyGeneratorService（融合 reasoner_result + 先例）|
| NodeType 字表 5 → 8 | NodeType.java +3 枚举 |

| 05 文档实体链接 | 落点 |
|---|---|
| §五 EntityLinker 跨引擎 REST | service/EntityLinker.java（POST /api/v1/knowledge/entity-link/entity/link，降级 status=fallback）|
| §四 抽取产物 3 类 entity/link/rule | NewsFeedReader 作为输入侧；LLM 抽取 3 类仍由 kb-engine 完成（Wave-3B 联动）|

---

## 四、关键文件清单（只列新增）

### cognitive-engine-api
- `model/PrecedentRef.java` (新增)
- `model/JustificationClause.java` (新增)
- `model/NodeType.java` (增量 +3 枚举值)
- `model/ReasoningStep.java` (增量 +1 字段)
- `model/ReasoningPath.java` (增量 +2 字段)
- `model/CausalChainResult.java` (增量 +1 字段)

### cognitive-engine-impl
- `service/ReasoningPathFromCausalBuilder.java` (新增)
- `service/PrecedentRecaller.java` (新增)
- `service/RuleRefCollector.java` (新增)
- `service/OagIntakeService.java` (新增)
- `service/OagPlannerService.java` (新增)
- `service/StrategyGeneratorService.java` (新增)
- `service/EntityLinker.java` (新增)
- `service/NewsFeedReader.java` (新增)
- `service/EngineCapabilityRegistryImpl.java` (增量 +3 OAG 路由)
- `service/CausalReasonerServiceImpl.java` (增量 +4 注入，diagnose 末尾 buildAndAttachReasoningPath)
- `controller/Wave3DemoController.java` (新增)
- `controller/DiagnosisController.java` (增量 +1 response 字段)

### 单测（新增 7 个测试类，31 case）
- `ContractModelsWave32Test.java`
- `ReasoningPathFromCausalBuilderTest.java`
- `PrecedentRecallerTest.java`
- `OagNodesTest.java`
- `RuleRefCollectorTest.java`
- `EntityLinkerTest.java`
- `NewsFeedReaderDemoTest.java`

---

## 五、审查要点 (Reviewer / QA checklist)

| 角色 | 关注点 |
|---|---|
| Reviewer | 1) 增量字段未删旧（G6 向后兼容） 2) ReasoningPathFromCausalBuilder 不重写 ReasoningPathBuilder 已落地的 buildStep/ buildPath 3) EntityLinker 不重复 kb 写 Neo4j 4) PrecedentRecaller 无 DB 表，仅复用 DecisionService 5) 测试 31/31 PASS |
| QA | 1) `mvn test` 模块内 31 测试必 PASS 2) `mvn install -P enterprise -DskipTests -Djacoco.skip=true` 全模块必 SUCCESS 3) 递归不破坏既有 controller 路径 4) Wave3DemoController 错误 markdown 不抛异常 |
| 架构 | 1) 不新增 Maven 模块（基线 13）2) 不新增 DB 表 3) 跨引擎只调 API 4) LLM 调用未直调 Provider（保留 runtime/llm-gateway 通道）|

---

## 六、Wave-3.2 终态（本波交付）

- [x] 5 类 Contract 全齐（G1）
- [x] ReasoningPath.clauses 结构化 (G3)
- [x] 因果链 → step 映射 + maxDepth 裁剪 (G4)
- [x] OAG 8 步 → 8 节点（新增 OAG_INTAKE / OAG_PLAN / OAG_STRATEGY）
- [x] PrecedentRecaller 接 DecisionService（PMO-32 闭环）
- [x] RuleRefCollector 去重 + 招 version 收口
- [x] EntityLinker 跨引擎 REST（kb-endpoint 不可用时降级 fallback）
- [x] Wave3DemoController 端到端 Demo
- [x] 31/31 单测 PASS
- [x] enterprise 档全模块 install BUILD SUCCESS
- [x] 本验收记录 + 5-cognitive/02-1-Wave3.2-认知收口.md 落盘

后续遗留（Wave-3B）：
- [ ] SpEL MAX_FACTS_SIZE 加固（04-§G5）
- [ ] pgvector 接入后 PrecedentRef.similarityEvidence.vector_score > -1
- [ ] 前端 5 子组件解耦展示 rationale

<!-- ECOS-7-INTOG-W3.2 / 2026-09-02 / v1.0 -->
