# Phase 4 ontology 测试用例

> 来源: 肖国荣 | 日期: 2026-08-25 | 对应: 02-测试方案.md
> 编号规则: TC-{主线}-{序号}，优先级 P0/P1/P2

## 一、提案链路（F1 + L1 修复）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:---:|
| TC-P-01 | T0 表已建 | `POST /api/v1/ontology/proposals` 创建 CREATE_ENTITY 提案 | 200，返回 proposal_id | P0 |
| TC-P-02 | TC-P-01 | `SELECT * FROM ecos_ontology_proposals WHERE id=?` | 提案落库（非内存） | P0 |
| TC-P-03 | TC-P-01 | `POST /api/v1/ontology/proposals/{id}/approve` | 200，status=APPROVED | P0 |
| TC-P-04 | TC-P-03 | `POST /api/v1/ontology/proposals/{id}/publish`（或 approve-and-publish） | 200，版本自动创建 | P0 |
| TC-P-05 | TC-P-04 | `SELECT * FROM ecos_ontology_version` 查新增版本 | 版本 +1，version_id 回填提案 | P0 |
| TC-P-06 | TC-P-04 | 重启 Gateway → 查提案 | 提案仍在（非内存丢失） | P0 |
| TC-P-07 | — | 清理测试提案 + 版本 | 无残留 | P0 |

## 二、Function 沙箱链路（F2）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:---:|
| TC-F-01 | — | `POST /api/v1/ontology/functions/compile` 传 `SUM(amount) FROM fin_revenue WHERE period='2026-07'` | 返回编译后 SQL + params | P0 |
| TC-F-02 | — | `POST /api/v1/ontology/functions/test` 传合法表达式 | 返回单值 + 类型（LONG/DOUBLE/NUMERIC） | P0 |
| TC-F-03 | TC-F-02 | `SELECT * FROM ecos_function_audit_log` | 审计记录落库（表达式/结果/耗时/调用者） | P0 |
| TC-F-04 | — | test 传 `DROP TABLE x` / `Runtime.exec` | 被白名单拒绝，不执行 | P0 |
| TC-F-05 | — | test 传慢查询（如大表全扫） | 5s 超时返回 | P1 |
| TC-F-06 | — | test 传无 FROM 的表达式 `1+1` | 编译失败，报"无法解析 FROM 子句" | P1 |

## 三、自动发现链路（F3）

| 编号 | 前置 | 步骤 | 预期 | 优先级 |
|------|------|------|------|:---:|
| TC-A-01 | 数据源有元数据 | `POST /api/v1/ecos/domains/{domainCode}/auto-discover` 传 datasourceId+resourceNames | 200，返回候选实体 | P0 |
| TC-A-02 | TC-A-01 | `SELECT * FROM ecos_ontology_entity` | 实体落库 | P0 |
| TC-A-03 | TC-A-01 | `SELECT * FROM ecos_ontology_property` | 属性落库 | P0 |
| TC-A-04 | TC-A-01 | `SELECT * FROM ecos_entity_table_mapping` | 映射落库 | P0 |
| TC-A-05 | — | 前端自动发现面板：选数据源 → 预览 → 确认 | 闭环可用，实体出现在列表 | P0 |

## 四、前端 dogfood（F4-F9）

| 编号 | 步骤 | 预期 | 优先级 |
|------|------|------|:---:|
| TC-UI-01 | `/#/ontology_workbench` 加载 | 5 类管理（对象/链接/动作/函数/接口） | P0 |
| TC-UI-02 | 提案面板创建提案 | 列表显示，可审批 | P0 |
| TC-UI-03 | 自动发现面板 | 选数据源 → 预览 → 生成 | P0 |
| TC-UI-04 | Function 详情 → 测试 | 显示结果 | P0 |
| TC-UI-05 | 版本时间线 | 显示版本历史 | P1 |
| TC-UI-06 | 对象类型详情 | 属性/链接/映射/动作 Tab | P0 |
| TC-UI-07 | 中英文切换 | 0 raw key（i18n P0 档） | P1 |
| TC-UI-08 | 4 主题切换 | 正常，0 硬编码颜色 | P1 |
| TC-UI-09 | 全程控制台 | 0 错误 | P0 |

## 五、架构合规 + 编译（F5/F6/F10 + stub）

| 编号 | 步骤 | 预期 | 优先级 |
|------|------|------|:---:|
| TC-C-01 | `wc -l DomainDesignerView.tsx` | <300（已拆分） | P1 |
| TC-C-02 | `grep ConcurrentHashMap` OntologyProposalController/OntologyMappingController | =0（已持久化） | P0 |
| TC-C-03 | `grep 硬编码颜色` ontology/ + OntologyWorkbenchLayout | =0 | P1 |
| TC-C-04 | `grep 硬编码中文` DomainDesignerView/DomainListView/OntologyWorkbenchLayout | =0（i18n P0） | P1 |
| TC-C-05 | `mvn install -Pstandard -DskipTests` | BUILD SUCCESS | P0 |
| TC-C-06 | `mvn test -pl common/common-api` | ArchUnit 全绿 | P0 |
| TC-C-07 | 核实 PgGraphService/AutoDiscoverService/Neo4jGraphService 是否 stub | 该实现→补；真缺口→记 | P1 |

## 六、执行顺序

```
T0 修表（L1）→ 提案链路（TC-P-01~07）→ Function 链路（TC-F-01~06）→ 自动发现链路（TC-A-01~05）
→ 前端 dogfood（TC-UI-01~09）→ 架构合规（TC-C-01~07）
```

提案/Function/自动发现三条链路独立可并行，前端 dogfood 依赖后端三条链路就绪。
